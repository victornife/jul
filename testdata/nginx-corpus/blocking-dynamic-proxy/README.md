# blocking-dynamic-proxy

Repository-authored AGPL-3.0-only negative fixture. A variable-derived
`proxy_pass` destination must remain blocking and must never be treated as a
safe generated migration candidate.
