events {}
http {
  upstream websocket_runtime_pool {
    server 127.0.0.1:19103;
  }
  server {
    listen 18098;
    server_name websocket-runtime.test;
    location / {
      proxy_pass http://websocket_runtime_pool;
    }
  }
}
