# approximate-alias

Repository-authored AGPL-3.0-only fixture derived from the documented NGINX
`alias` migration boundary. It must generate a valid Jul candidate while
retaining an explicit `NGX_LOCATION_ALIAS` approximation.
