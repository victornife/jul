server {
    listen 18080;
    server_name corpus.test;

    location = /health {
        add_header X-Corpus "supported" always;
        return 204;
    }

    location = /redirect {
        return 302 /health;
    }
}
