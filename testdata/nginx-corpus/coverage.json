{
  "schema_version": 1,
  "categories": [
    {
      "id": "cache-compression",
      "status": "represented",
      "fixtures": [
        "cache-runtime",
        "compression-runtime",
        "security-cache-boundaries"
      ],
      "evidence": [
        "NGX_HTTP_CACHE_PATH",
        "NGX_HTTP_GZIP",
        "NGX_LOCATION_CACHE",
        "NGX_LOCATION_CACHE_VALID",
        "issue #365 real-Jul gzip engagement + decoded-content E2E",
        "issue #365 real-Jul stateful cache MISS/HIT/BYPASS E2E"
      ],
      "rationale": "The corpus proves direct gzip classification, a real-backend Jul gzip engagement/decoded-content E2E (#365), and the bounded proxy_cache_path/proxy_cache -> Jul [cache] translation: exactly one declared cache zone referenced consistently by every proxy_cache in the file translates onto Jul's single process-wide cache (cache-runtime, with a real MISS/HIT/no-store-BYPASS E2E), while an undeclared or multi-zone reference is rejected as a synthetic NGX_CACHE_ZONE_CONFLICT rather than silently guessed (security-cache-boundaries).",
      "deferred_dimensions": [
        "Vary/Range/stale-serving cache dimensions (already covered by internal/handler's own dedicated cache test suite; runtime behavior independent of anything the importer translates, so re-proving it here would be confirmatory, not migration-specific)",
        "proxy_cache_bypass/proxy_no_cache/proxy_cache_key (expression-driven; not statically representable)",
        "shared/distributed cache directives (multiple proxy_cache_path zones - a genuine architectural mismatch with Jul's single process-wide cache, not a scope gap)"
      ],
      "revisit_trigger": "Revisit only if Jul's cache model itself grows multiple named zones; the single-zone bounded translation already covers the representable case."
    },
    {
      "id": "core-http-routing",
      "status": "represented",
      "fixtures": [
        "approximate-alias",
        "blocking-dynamic-proxy",
        "core-multifile-return",
        "proxy-pass-uri-runtime",
        "routing-cors-policy",
        "routing-precedence-runtime",
        "websocket-runtime"
      ],
      "evidence": [
        "issue #365 pinned real-NGINX reference confirmation for the precedence order",
        "issue #365 real-Jul H1 WebSocket upgrade/echo/close E2E",
        "issue #365 real-Jul exact/prefix/regex/root location-precedence E2E",
        "issue #365 real-Jul proxy_pass URI prepend-vs-location-prefix-replacement E2E"
      ],
      "rationale": "Core fixtures cover multi-file servers, exact/prefix/regex/root location-precedence order proven through both a real Jul instance and the pinned real-NGINX reference container (#365), static files, returns, redirects, method constraints, response headers, CORS, safe blocking of dynamic proxy targets, H1 WebSocket migration evidence through a real backend, and a real-Jul E2E precisely characterizing proxy_pass URI-rewriting semantics: nginx strips the matched location prefix and replaces it with the proxy_pass path, while Jul's proxy (net/http/httputil.ProxyRequest.SetURL) always prepends the proxy_pass path to the client's full incoming request path without stripping anything first. H2/H3 Extended CONNECT WebSocket remains explicitly out of scope (#435).",
      "deferred_dimensions": [
        "full NGINX location-precedence cross-product beyond the four-tier order already proven",
        "rewrite control-flow beyond static supported flags"
      ],
      "revisit_trigger": "Expand when a supported importer mapping changes or a representative source estate exposes a routing distinction not already classified."
    },
    {
      "id": "operations",
      "status": "represented",
      "fixtures": [
        "core-multifile-return",
        "operations-boundaries"
      ],
      "rationale": "The corpus covers bounded include provenance plus explicit ignored/blocking evidence for process settings, event limits, custom logs, resolver behavior, and variable maps.",
      "deferred_dimensions": [
        "live log-sink parity",
        "resolver/DNS runtime replay",
        "worker/process tuning equivalence"
      ],
      "revisit_trigger": "Operational runtime replay requires a separately selected, deterministic local dependency or a concrete importer support change."
    },
    {
      "id": "protocol-gateways",
      "status": "represented",
      "fixtures": [
        "fastcgi-gateway",
        "protocol-boundaries",
        "stream-named-upstream",
        "stream-security-boundaries",
        "stream-sni-bounded",
        "stream-tcp-basic",
        "stream-udp"
      ],
      "evidence": [
        "issue #259 H1/H2/H3 client-identity E2E",
        "issue #426 bounded NGINX stream translation"
      ],
      "rationale": "FastCGI translation is represented by a strict-valid candidate. NGINX stream now has a bounded translated subset (#426): direct/named-upstream TCP and UDP targets, proxy_timeout/proxy_connect_timeout, outbound PROXY protocol, and server_name+ssl_preread bounded SNI routing merged into one listener. Stream TLS termination, inbound stream PROXY protocol (nginx's stream module has no trusted-source directive for it), udp+proxy_protocol, unrecognized listen options, arbitrary stream maps, and mail remain explicit blocking evidence rather than silently omitted capability claims.",
      "deferred_dimensions": [
        "H2/H3 migration replay",
        "L4 runtime replay",
        "WebSocket runtime replay",
        "gRPC/uWSGI protocol backends"
      ],
      "revisit_trigger": "Add protocol-specific local backends (real NGINX-vs-Jul stream/gRPC/FastCGI/uWSGI E2E) under #367, which consumes this bounded stream translation; existing product protocol E2E remains authoritative for runtime capability itself."
    },
    {
      "id": "security",
      "status": "represented",
      "fixtures": [
        "mtls-runtime",
        "proxy-protocol-runtime",
        "realip-blocking",
        "realip-proxy-protocol-supported",
        "realip-supported",
        "security-cache-boundaries",
        "stream-security-boundaries"
      ],
      "evidence": [
        "fixture admission secret/private-key scan",
        "issue #259 client-address runtime and spoofing E2E",
        "issue #366 real-Jul PROXY-protocol trust-boundary accept/refuse E2E",
        "issue #366 real-Jul mTLS handshake accept/reject E2E",
        "safe replay sensitive-header rejection"
      ],
      "rationale": "The corpus covers IPv4/IPv6 trusted proxies, supported and blocking real-IP forms, TLS references/protocols, security headers, blocking authentication/ACL/body-limit/rate-limit/cache controls, #426's bounded HTTP inbound PROXY-protocol identity (listen token + real_ip_header proxy_protocol + set_real_ip_from all required together) alongside its stream-side security boundaries, a real-Jul E2E proving that boundary's trust decision end to end (a trusted relay's PROXY-asserted client is honored, an untrusted peer is refused, #366), and the new bounded ssl_verify_client/ssl_client_certificate/ssl_crl -> tls.client_auth translation with a real-Jul mTLS E2E (an ephemeral CA-signed client certificate is accepted, no certificate and a different-CA certificate are both rejected, #366).",
      "deferred_dimensions": [
        "WAF/module-specific migration replay (no nginx WAF directive exists to translate from; Jul's [waf] block has nothing on the nginx side to map onto, so this is a boundary to document, not a translation gap)",
        "multi-proxy forwarded-chain runtime comparison"
      ],
      "revisit_trigger": "Add a security runtime fixture when generated test credentials and asserted dimensions can remain bounded, synthetic, and independent of product-level E2E already owned elsewhere."
    },
    {
      "id": "upstreams-resiliency",
      "status": "represented",
      "fixtures": [
        "blocking-dynamic-proxy",
        "timeout-runtime",
        "unix-http-upstream",
        "upstream-failover-runtime",
        "upstream-weighted",
        "upstream-weighted-runtime"
      ],
      "evidence": [
        "issue #365 real-Jul deterministic weighted-round-robin distribution E2E",
        "issue #365 real-Jul passive-failover/circuit-breaker E2E",
        "issue #365 real-Jul proxy_connect_timeout/proxy_read_timeout enforcement E2E"
      ],
      "rationale": "The corpus covers named weighted upstreams, least-connections translation, ignored connection-pool tuning, proxy routing, blocking variable-derived destinations, a real Unix-socket HTTP upstream (#407), a real-Jul weighted-round-robin E2E proving the exact 3:1 distribution (#365), the bounded per-backend max_fails/fail_timeout -> upstream-wide [upstreams.resilience] translation with a real-Jul passive-failover E2E (#365), the proxy_connect_timeout/proxy_read_timeout -> location translation with a real-Jul E2E proving a stalling backend is cut off at the configured bound (504, #365), and the bounded proxy_next_upstream_tries -> retry_attempts translation (an explicit bound of 2+ only; nginx's 0/1 forms collide with Jul's retry_attempts=0 \"inherit\" sentinel and stay blocking).",
      "deferred_dimensions": [
        "WebSocket/gRPC upstream runtime replay",
        "active-health migration replay",
        "backend TLS/private-CA migration replay",
        "circuit-breaker recovery/half-open-probe migration replay beyond the dedicated product-level resilience evidence",
        "proxy_next_upstream_tries real-E2E (translation is unit-tested; a dedicated retry-count E2E is deferred as confirmatory of the already-proven passive-failover retry mechanism)"
      ],
      "revisit_trigger": "Expand after a stable runtime contract and a deterministic local backend can assert one additional migration semantic without becoming a general gateway conformance suite."
    }
  ]
}
