events {}
http {
  gzip on;
  upstream compression_runtime_pool {
    server 127.0.0.1:19102;
  }
  server {
    listen 18097;
    server_name compression-runtime.test;
    location / {
      proxy_pass http://compression_runtime_pool;
    }
  }
}
