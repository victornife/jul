events {}
http {
  upstream local_app {
    server unix:/tmp/jul407/backend.sock;
  }
  server {
    listen 18086;
    server_name unix-http.test;
    location / {
      proxy_pass http://local_app;
    }
  }
}
