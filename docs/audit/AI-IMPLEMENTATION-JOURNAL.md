# AI implementation journal

> This is an execution log, not the final closure report. Never mark an audit item formally closed here.

## Program metadata

- Repository: victornife/jul
- Target branch: latest
- Initial remote SHA: 50052f9d5377488a6160935008e9e028fd08eca2
- Bootstrap branch: audit/ws01-managed-ledger
- Bootstrap resulting SHA: recorded in the bootstrap slice completion report (this initialization commit)
- Started at: 2026-07-28
- Coordinator agent identifier: bootstrap agent (Claude Opus 4.8, Act/Agent mode)

## Environment baseline

- OS: Windows 11 (amd64)
- Go version: go1.26.5 windows/amd64
- Node version: v24.4.0
- pnpm version: 11.8.0 (update to 11.17.0 available; non-blocking)
- Git version: 2.51.1.windows.1
- GitHub CLI/connector availability: gh 2.95.0 available
- Full build tags available: standard go test toolchain available; full build-tag matrix not exercised in this bootstrap (only the prescribed baseline packages were run)
- Known environment limitations: none known

## Baseline commands

| Command | Result | Notes |
|---|---|---|
| git rev-parse --show-toplevel | pass | Working tree at repo root |
| git remote -v | pass | origin = https://github.com/victornife/jul.git (fetch/push) |
| git branch --show-current | pass | latest at preflight |
| git rev-parse HEAD | pass | 50052f9d5377488a6160935008e9e028fd08eca2 |
| git status --short | pass | Clean working tree |
| git log -1 --oneline | pass | 50052f9d docs(audit): AC-15/AC-16 configuration audit closure report keyed to green SHAs |
| git fetch origin --no-write-fetch-head | pass | Refs fetched without touching working files |
| git rev-parse origin/latest | pass | 50052f9d5377488a6160935008e9e028fd08eca2 (equals EXPECTED_PARENT_SHA; no drift) |
| git merge-base --is-ancestor HEAD origin/latest | pass | Fast-forward confirmed (FF_OK) |
| git diff --check | pass | No whitespace/conflict markers (DIFF_CHECK_CLEAN) |
| go test -count=1 ./internal/admin/... | pass | ok jul/internal/admin 3.897s |
| go test -count=1 ./internal/app/... | pass | ok jul/internal/app; ok jul/internal/app/apps |
| go test -count=1 ./internal/config/... | pass | ok jul/internal/config 2.088s |
| pnpm --dir internal/admin/ui install --frozen-lockfile | pass | Already up to date |
| pnpm --dir internal/admin/ui run typecheck | pass | tsc --noEmit clean |
| pnpm --dir internal/admin/ui run lint | pass | eslint --max-warnings=0 clean |
| pnpm --dir internal/admin/ui run test | pass | vitest run - 37 files, 451 tests passed |

---

## Workstream / slice entry template

### workstream / slice

- Parent SHA:
- Resulting SHA:
- Branch:
- Agent role/context:
- Files changed:
- Production path verified:
- Behavior implemented:
- Tests added:
- Commands run:
- Commands unavailable:
- Deviations:
- Self-review findings:
- Independent review status:
- Reviewer blockers:
- Blocker-fix SHA:
- Accepted SHA:
- Next execution file:

---

### BOOTSTRAP / 03_REPOSITORY_BOOTSTRAP.md

- Parent SHA: 50052f9d5377488a6160935008e9e028fd08eca2
- Resulting SHA: this journal-initialization commit (see bootstrap slice completion report)
- Branch: audit/ws01-managed-ledger
- Agent role/context: bootstrap agent (Claude Opus 4.8, Act/Agent mode); establishes baseline and journal only
- Files changed: docs/audit/AI-IMPLEMENTATION-JOURNAL.md (new)
- Production path verified: n/a - bootstrap slice does not touch production code
- Behavior implemented: reproducible baseline verification + persistent journal initialization
- Tests added: none (no audit finding implemented in this slice)
- Commands run: see Baseline commands table above
- Commands unavailable: none
- Deviations: none
- Self-review findings: no production code touched; no audit finding implemented; only the journal was added and committed
- Independent review status: not required for bootstrap
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: WS01 Slice 01 (audit/ws01-managed-ledger)

---

### WS01_MANAGED_LEDGER / 01_BOOT_SCOPED_IDS_AND_REGISTRY_TYPES.md

- Parent SHA: e707c41587a335cc92b643068b1c46053eff2621
- Resulting SHA: recorded in this slice's completion report (the commit that includes this entry)
- Branch: audit/ws01-managed-ledger
- Agent role/context: implementation agent (Claude Opus 4.8, Act/Agent mode, highest reasoning); WS01 Slice 1
- Files changed:
  - internal/app/config_apply.go — boot-scoped apply IDs (rl_<boot-id>_<sequence>) on ConfigApplyCoordinator
  - internal/admin/managed_apply_registry.go — structured ID parser, ManagedApplyFinalizing state, Deadline + OwnerTokenID fields, enriching BeginPending, ClaimFinalization, metadata-preserving Complete
  - internal/app/serve.go — parseReloadSeq accepts boot-scoped + legacy IDs (trailing sequence)
  - internal/app/managed_apply_id_test.go (new) — boot-scoped generation, monotonicity, per-process uniqueness, seq parsing
  - internal/admin/managed_apply_registry_slice1_test.go (new) — ID validity/parse, finalizing lifecycle, enrichment, metadata preservation, non-serialization of OwnerTokenID
- Production path verified: coordinator.nextID() (sole apply-ID source) → server.ReloadRequest.ID → terminal res.ApplyID → serve.go OnManagedApplyComplete → managedApplies.Complete + managedApplySeqGuard(parseReloadSeq). All production IDs now carry the boot-scoped prefix; registry accepts boot-scoped and legacy IDs; the sequence guard sequences on the trailing field.
- Behavior implemented: boot-scoped monotonic apply IDs that prevent cross-restart reuse; legacy rl_<sequence> still accepted; registry gains an explicit finalizing claim state, an absolute Deadline for deadline-aware polling, and private non-serialized OwnerTokenID ownership metadata; BeginPending enriches an existing pending shell without downgrading finalizing/terminal; ClaimFinalization claims finalization exactly once; Complete accepts pending or finalizing and preserves private/lifecycle metadata.
- Tests added: see Files changed (2 new focused test files); all existing managed-apply/seq-guard tests preserved unchanged.
- Commands run: gofmt -w (clean); go build ./... (pass); go vet ./internal/admin/... ./internal/app/... (pass); go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... (pass); git diff --check (clean, only benign LF→CRLF notices)
- Commands unavailable: go test -race ./internal/admin/... and ./internal/app/... — UNVERIFIED: -race requires cgo and no C toolchain (gcc/clang) is installed in this environment (KNOWN_ENVIRONMENT_LIMITATION). Residual risk: data-race detection deferred to the CI race matrix; new registry logic reuses the existing mutex discipline and adds no new shared state outside the locked critical sections.
- Deviations:
  1. Requested parseCanonicalApplySequence bound `len(raw) > 20` → implemented `len(raw) > 19` → rationale: the repository's existing TestManagedApplyRegistry_InvalidIDs asserts a 20-digit sequence is invalid; keeping the 19-digit bound preserves that assertion instead of weakening it.
  2. Requested a structured registry-side parser reused by the app-layer sequence guard → implemented the boot-scoped/legacy trailing-sequence parse inside serve.go's existing parseReloadSeq → rationale: parseManagedApplyID is unexported in package admin and importing it would cross package boundaries; the repository-native equivalent extracts the trailing underscore-delimited sequence, which is correct because all IDs in one process share one boot instance.
  3. Retained BeginFinalization and the finalized map alongside the new ClaimFinalization → rationale: production (serve.go) still calls BeginFinalization; this slice introduces the finalizing state and claim API without rewiring that call site (scope minimization); the WS2 finalization-claim wiring is a later slice.
- Self-review findings: single-purpose diff across 3 production files + 2 test files; no generated bundles, closure report, secrets, or bearer/token material touched; OwnerTokenID is json:"-" and a test proves it never serializes; no error swallowing; no unwired helper other than ClaimFinalization which is the deliberate WS2 seam introduced by this slice.
- Independent review status: pending
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: WS01 Slice 02 (audit/ws01-managed-ledger)

---

### WS01_MANAGED_LEDGER / 02_PENDING_PRODUCTION_WIRING.md

- Parent SHA: cf7bd0211d52e0acaede2cea2d593499ebc56263
- Resulting SHA: recorded in this slice's completion report (the commit that includes this entry)
- Branch: audit/ws01-managed-ledger
- Agent role/context: implementation agent (Claude Opus 4.8, Act/Agent mode, highest reasoning); WS01 Slice 2
- Files changed:
  - internal/admin/server.go — new ManagedApplyStart{Context, Result} carrier type for the pending-registration signal
  - internal/app/config_apply.go — OnManagedApplyStarted hook on ConfigApplyCoordinator; notifyManagedApplyStarted helper; applyCandidate registers the exact-ID pending record after persist+enqueue and before the synchronous 202; notifyManagedApplyComplete now threads a finalizationErr into ManagedApplyFinalization.FinalizationError; the two complete call sites updated
  - internal/app/serve.go — wired coordinator.OnManagedApplyStarted = managedApplies.BeginPending(...) inside the coordinator!=nil block (before admin.New copies deps and starts the listener); added errors import
  - internal/admin/api_managed_apply.go — publicManagedApplyRecord gains Deadline (json:"deadline,omitempty") and toPublic() projects rec.Deadline
  - internal/admin/ui/src/api/client.ts — ManagedApplyRecordSchema gains deadline: z.string().optional()
  - internal/admin/assets/dist/** — regenerated Console bundle (index + lazy CodeEditor chunk rehash) via `pnpm run build`; never hand-edited
  - internal/app/config_apply_pending_test.go (new) — production-wired pending-registration evidence
- Production path verified: HTTP apply → deps.ApplyConfigRaw/ApplyConfig → coordinator.ApplyRaw → applyCandidate persists candidate (atomicfile.Write) → SubmitReload enqueues the correlated reload → notifyManagedApplyStarted projects the provisional saved_not_live result (provisionalResult) → serve.go's OnManagedApplyStarted resolves the ApplyID (Result.ApplyID, falling back to Result.Reload.ID) and calls managedApplies.BeginPending, so the exact-ID pending record exists BEFORE the synchronous wait can hand a 202 back to the caller. Terminal finalizer → notifyManagedApplyComplete(...finalizationErr) → serve.go OnManagedApplyComplete → managedApplies.Complete updates the same record in place. GET /api/config/applies/{id} → publicManagedApplyRecord.toPublic() now surfaces Deadline; the Console client schema accepts it.
- Behavior implemented: a real 202 saved_not_live is always preceded by an exact-ID pending ledger record (closes the 202→404 window that stalls ConfigPanel); the record carries Operation, StartedAt, absolute Deadline (deadline-aware polling), the provisional Result, and private OwnerTokenID; a post-persistence pending-registration failure never rolls back the accepted apply nor reports the apply as failed — it is carried into ManagedApplyFinalization.FinalizationError and surfaced through the ledger/overview; the public API and the TypeScript schema expose deadline.
- Tests added: internal/app/config_apply_pending_test.go — TestApplyRegistersPendingRecordBeforeSavedNotLive (package integration: drives the real coordinator through the saved_not_live path with the production BeginPending/Complete wiring; asserts the exact-ID pending record exists at 202 with correct operation/deadline/owner/result, that the terminal finalizer transitions the SAME record pending→terminal preserving owner/deadline, and that a later unrelated transaction never evicts it), TestApplyStartedErrorSurfacesInFinalization (failure injection: a failing OnManagedApplyStarted does not roll back the persisted candidate nor fail the live apply, and its wrapped error surfaces in ManagedApplyFinalization.FinalizationError). No existing test weakened.
- Commands run: gofmt -w on the two changed files (clean; remaining gofmt -l output is the repository-wide benign LF/CRLF condition, not these edits); go build ./... (pass); go vet ./internal/admin/... ./internal/app/... (pass); go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... (pass); pnpm --dir internal/admin/ui run typecheck (pass); run lint (pass); run test (37 files, 451 tests pass); pnpm --dir internal/admin/ui run build (deterministic bundle; asset-drift gate reflects only the schema change); git diff --check (clean, only benign LF→CRLF notices)
- Commands unavailable: go test -race ./internal/admin/... and ./internal/app/... — UNVERIFIED: -race requires cgo and no C toolchain (gcc/clang) is installed in this environment (KNOWN_ENVIRONMENT_LIMITATION). Residual risk: data-race detection deferred to the CI race matrix; the new hook reuses the existing coordinator locking discipline (registration runs after c.mu is released, exactly like the pre-existing enqueue-failure completion path) and the registry's own mutex, adding no new shared state outside locked critical sections.
- Deviations:
  1. Requested `OnManagedApplyStarted func(admin.ManagedApplyStart) error` on ConfigApplyCoordinator plus a helper that projects ApplyResult via toAdminConfigApplyResult → implemented exactly, with the helper named notifyManagedApplyStarted mirroring the existing notifyManagedApplyComplete → rationale: matches the repository-native callback/notify pairing already present on the coordinator.
  2. Requested "the finalizer closure must capture trackingErr and append it to ManagedApplyFinalization.FinalizationError" → implemented by adding a finalizationErr parameter to notifyManagedApplyComplete and threading it into the existing admin.ManagedApplyFinalization{...} construction (the enqueue-failure call site passes "") → rationale: the coordinator builds ManagedApplyFinalization in exactly one place; parameterizing that single constructor avoids duplicating the finalization struct and keeps FinalizationError authoritative, and serve.go already forwards fin.FinalizationError onto both the runtime-overview outcome and the durable ledger record.
- Self-review findings: single-purpose diff — one new carrier type, one coordinator hook + helper, one registration call site, one composition-root wiring, one public-API field + its TS mirror, one regenerated bundle, one new focused test file. No unwired helpers (notifyManagedApplyStarted is called by applyCandidate; OnManagedApplyStarted is wired in serve.go). No error swallowing: the registration error is wrapped and carried to FinalizationError, never dropped; the accepted apply is never rolled back for a tracking failure (decision hierarchy: preserve the currently serving runtime / report exact truth after persistence). No secrets, bearer/token material, or raw TOML added; OwnerTokenID remains json:"-" and Deadline is low-cardinality metadata. The Console bundle was regenerated by `pnpm run build`, not hand-edited. The final audit-closure report was not touched.
- Independent review status: pending
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: WS01 Slice 03 (audit/ws01-managed-ledger)

---

### WS01_MANAGED_LEDGER / 03_LOOKUP_API_AND_AUTHORIZATION.md

- Parent SHA: 039bd0ee0e6573bcdb757ed45c561a0de3d8e0e9
- Resulting SHA: recorded in this slice's completion report (the commit that includes this entry)
- Branch: audit/ws01-managed-ledger
- Agent role/context: implementation agent (Claude Opus 4.8, Act/Agent mode, highest reasoning); WS01 Slice 3
- Files changed:
  - internal/admin/route_catalog.go — RouteSpec gains AnyPermissions []rbac.Permission (logical-OR authorization); the /api/config/applies/{id} entry migrated from Permission: rbac.StatusRead to AnyPermissions: {rbac.StatusRead, rbac.ConfigApply} with an updated explanatory comment
  - internal/admin/routes.go — routes() dispatcher converted to a switch; new case `len(spec.AnyPermissions) > 0 → s.requireAnyPermission(...)` inserted before the Permissions and single-Permission cases (Public still first)
  - internal/admin/rbac.go — new requireAnyPermission middleware (authenticate once, authorize by looping over the accepted permissions) mirroring requirePermission's snapshot-mode dispatch (Blocked→503, RBAC→authn+authz-any, Legacy/Open→token/identity); new writeForbiddenAny 403 helper that lists the accepted permissions under required_any without leaking other principals/tokens
  - internal/admin/route_catalog_test.go — TestCatalogNoRouteDefaultsToPublic and TestCatalogPermissionsInCatalog extended to recognize AnyPermissions; new TestCatalogExactlyOneAuthorizationMode guard proving exactly one of Public/Permission/Permissions/AnyPermissions is set per route
  - internal/admin/api_managed_apply_test.go (extended) — RBAC HTTP-integration evidence for the AnyPermissions endpoint
- Production path verified: HTTP GET /api/config/applies/{id} → s.routes() builds the mux from Catalog → the entry's AnyPermissions selects s.requireAnyPermission([]{StatusRead, ConfigApply}, handleManagedApplyGet) → the middleware loads the single immutable authSnapshot and dispatches on mode: RBAC authenticates the bearer once via snap.policy.Authenticate, then authorizes by iterating the accepted permissions with snap.policy.Authorize (first match serves the handler with the identity in context); Legacy/Open reuse checkLegacyToken + legacyIdentity exactly as requirePermission; Blocked fails closed with 503. On no match the request receives the structured writeForbiddenAny 403. handleManagedApplyGet is unchanged and still emits the secret-free public projection (no actor/source IP/token digest) with Cache-Control: no-store.
- Behavior implemented: a principal permitted to APPLY configuration (config:apply) can now retrieve the secret-free result of its own class of operation at /api/config/applies/{id} even without status:read, because a more-privileged mutate capability implies the less-privileged read (AC-02, §2.8 Step 5). status:read continues to authorize the same endpoint. Authentication still happens exactly once; only the authorization decision is OR-combined. A principal holding neither permission receives a structured 403 listing the accepted permissions under required_any; the endpoint still never exposes raw config, actor, source IP, or token digest.
- Tests added: internal/admin/api_managed_apply_test.go — TestManagedApplyGet_AnyPermissionAuthorizes (HTTP integration through the full routes() stack under a real rbac.Policy with four principals: viewer=status:read→200, automation=custom config:apply-only role→200, metrics-only=unrelated permission→403, no token→401) and TestManagedApplyGet_ForbiddenBodyListsAcceptedPermissions (HTTP integration: asserts the 403 body error=forbidden and required_any = {status:read, config:apply} and leaks no token_digest/token_id/principals); internal/admin/route_catalog_test.go — TestCatalogExactlyOneAuthorizationMode (unit guard). No existing test weakened; the two extended catalog guards became strictly more permissive only for the new AnyPermissions mode.
- Commands run: gofmt -w on the five changed files (clean); go build ./... (pass); go vet ./internal/admin/... ./internal/app/... (pass); go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... (pass); git diff --check (clean, only benign LF→CRLF notices)
- Commands unavailable: go test -race ./internal/admin/... and ./internal/app/... — UNVERIFIED: -race requires cgo and no C toolchain (gcc/clang) is installed in this environment (KNOWN_ENVIRONMENT_LIMITATION). Residual risk: data-race detection deferred to the CI race matrix; requireAnyPermission introduces no new shared mutable state — it reads the same atomically-loaded authSnapshot as requirePermission and only iterates a read-only permission slice.
- Console commands: none required — this slice is backend route authorization only (§2.8 Step 5). No internal/admin/ui source changed, so no typecheck/lint/test/build or asset-drift gate was needed; internal/admin/assets/dist was not touched.
- Deviations:
  1. Requested a single `snap.policy.Authenticate(r.Header.Get("Authorization"), now)` skeleton with a generic err→401 branch → implemented the repository-native two-branch authentication used by requirePermission, distinguishing rbac.ErrDisabled (structured "principal is disabled or expired" 401) from other errors → rationale: preserves the existing disabled/expired contract and keeps requireAnyPermission behaviourally identical to requirePermission except for the OR-authorization loop (decision hierarchy: prevent security weakening; reuse repository-native abstractions).
  2. Skeleton showed the legacy branch as the switch default with no explicit Open handling → implemented the same `default: // authModeLegacy, authModeOpen` fallthrough requirePermission already uses, where checkLegacyToken returns true for an empty token (Open) → rationale: reuses the exact existing behaviour so Legacy and Open modes are handled identically to every other admin route.
- Self-review findings: single-purpose diff across 3 production files + 2 test files, all under internal/admin. No unwired helpers — requireAnyPermission is reached via the routes() dispatcher and the /api/config/applies/{id} catalog entry; writeForbiddenAny is called only by requireAnyPermission. No error swallowing (authn errors return 401; unauthorized returns the structured 403). No secret/actor/source-IP/token-digest exposure (handler unchanged; the 403 body reports only the accepted permissions and the caller's own role). No generated bundle, closure report, or authentication/history/transaction truthfulness weakened — the change strictly widens which principals may READ an already-secret-free projection and never relaxes authentication or any mutating route.
- Independent review status: pending
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: WS01 Slice 04 (audit/ws01-managed-ledger)

---

### WS01_MANAGED_LEDGER / 04_VERTICAL_TESTS_AND_HARDENING.md

- Parent SHA: 51ee2a58d698d032d6e2dee31941be35304d3da4
- Resulting SHA: recorded in this slice's completion report (the commit that includes this entry)
- Branch: audit/ws01-managed-ledger
- Agent role/context: implementation agent (Claude Opus 4.8, Act/Agent mode, highest reasoning); WS01 Slice 4
- Files changed:
  - internal/admin/server.go — new exported `(*Server).Handler() http.Handler` returning `s.routes()`; `New` now builds the listener's `http.Server.Handler` via `s.Handler()` so the exported accessor is the exact production handler, not a test-only shim. This lets the cross-package vertical test drive the real admin route stack (including route authorization) without reconstructing routes or bypassing the auth chokepoint.
  - internal/app/managed_apply_vertical_test.go (new) — the §2.9 production-lifecycle vertical test: a real ConfigApplyCoordinator apply → real 202 saved_not_live → GET /api/config/applies/<id> through the real admin.Server.Handler() observing 202/pending → release the withheld terminal reload → GET observes 200/terminal, without manually seeding the registry.
  - internal/admin/managed_apply_registry_slice4_test.go (new) — two registry hardening tests from §2.9 not previously covered: BeginPending-after-terminal must not downgrade, and Complete preserves start/deadline carried on the pending record.
  - internal/admin/api_managed_apply_authz_test.go (new) — the two remaining §2.9 authorization cases: disabled/expired credential → 401 (fail closed before the handler), and Blocked RBAC (cfg.RBAC.Enabled with nil policy) → 503, both asserting the secret-free projection is never served.
- Production path verified: HTTP GET /api/config/applies/{id} is served by the exact handler the listener runs. New builds `http.Server{Handler: s.Handler()}` and Handler() returns `s.routes()`; the vertical test issues the GET through that same `s.Handler()`. The apply path is unchanged from Slices 1–3: ApplyRaw persists the candidate, enqueues the reload, calls notifyManagedApplyStarted (OnManagedApplyStarted → registry.BeginPending) BEFORE the bounded synchronous wait returns the provisional saved_not_live 202, then the finalizer delivers the terminal result and calls OnManagedApplyComplete → registry.Complete. The test wires OnManagedApplyStarted/OnManagedApplyComplete with the exact field mapping serve.go installs (via the existing wireProductionLedger helper) so it exercises the real composition-root wiring.
- Behavior implemented: no production behavior change beyond exposing the already-constructed admin handler. This slice's substance is closure evidence: it proves end-to-end that a real 202 saved_not_live is immediately observable as 202/pending at the exact-ID endpoint (never a 404), that releasing the reload transitions the same record to 200/terminal with matching id/operation/result, that the generated ID is boot-scoped (rl_<12-hex>_<seq>), that the pending response carries the absolute deadline for deadline-aware polling, and that neither the pending nor terminal projection leaks actor/source IP/token digest/owner token. It also hardens the registry (no terminal→pending downgrade; start/deadline preserved at completion) and the authorization contract (disabled/expired→401, Blocked RBAC→503).
- Tests added: internal/app/managed_apply_vertical_test.go — TestManagedApplyLifecycle_202PendingThen200Terminal (HTTP integration through the full admin handler + real coordinator; no seeded registry; terminal reload withheld behind a channel; no sleeps). internal/admin/managed_apply_registry_slice4_test.go — TestBeginPendingAfterTerminalNoDowngrade, TestCompletePreservesStartAndDeadline (unit). internal/admin/api_managed_apply_authz_test.go — TestManagedApplyGet_DisabledOrExpiredCredential401 (HTTP integration, disabled + expired principals), TestManagedApplyGet_BlockedRBAC503 (HTTP integration, Blocked snapshot). No existing test weakened; existing coordinator-level pending evidence (config_apply_pending_test.go) and Slice-1/3 registry/authorization tests remain intact.
- Commands run: gofmt -w on the four changed files (clean); go build ./... (pass); go vet ./internal/admin/... ./internal/app/... (pass); go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... (pass); targeted -v runs of all five new tests (pass); git diff --check (clean, only a benign LF→CRLF notice on server.go).
- Commands unavailable: go test -race ./internal/admin/... and ./internal/app/... — UNVERIFIED: `-race requires cgo` and no C toolchain (gcc/clang) is installed in this environment (KNOWN_ENVIRONMENT_LIMITATION). Residual risk: the vertical test's goroutine interleaving (finalizer vs synchronous waiter) and the registry concurrency test are not race-verified locally; deferred to the CI race matrix. The added tests introduce no new shared mutable production state — Handler() only returns the existing routes() mux and reads no server state beyond it.
- Console commands: none required — this slice is backend + Go test evidence only. The deadline field of the TS ManagedApplyRecord schema was already present from a prior slice; no internal/admin/ui source changed, so no typecheck/lint/test/build or asset-drift gate was needed; internal/admin/assets/dist was not touched.
- Deviations:
  1. The slice's §2.9 vertical test is specified as using "the actual coordinator and admin HTTP handler". There was no exported way to obtain the admin handler (routes() is unexported and admin cannot import app). Requested design (implicit): reach the handler directly → implemented design: add a minimal exported `(*Server).Handler()` and route `New` through it so the accessor IS the production handler → rationale: preserves the single auth chokepoint and avoids an unwired test-only seam (decision hierarchy: prefer repository-native abstractions; minimize scope; do not bypass route authorization).
  2. Several §2.9 checklist items (boot-scoped/legacy/malformed IDs, ClaimFinalization pending→finalizing, duplicate claim, OwnerTokenID preservation/serialization, pending-never-evicted, coordinator-level pending registration) were already implemented and tested by Slices 1–3. Requested design: add them here → implemented design: NOT re-added; only the genuinely-missing evidence (vertical HTTP lifecycle, no-downgrade, start/deadline preservation, disabled/expired-401, Blocked-503) was added → rationale: minimize scope and avoid duplicate truth; do not reimplement accepted earlier slices.
- Self-review findings: single-purpose diff — 1 production file (+11/-1) exposing the existing handler, plus 3 new test files. No unwired helpers: Handler() is called by New (production) and by the vertical test. No error swallowing. No secret/actor/source-IP/token-digest exposure — the tests assert the projections omit them, and Handler() changes no serialization. No authentication/authorization weakening — Handler() returns the same routes() mux that already applies the auth chokepoint; the 401/503 tests prove fail-closed behavior is unchanged. No generated bundle, closure report, or history/transaction truthfulness touched.
- Independent review status: pending
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: WS02 (or the next coordinator-assigned slice)

---

### WS02_FINALIZATION / 01_COMPLETION_CONTRACT.md

- Parent SHA: bb666a655a14d25b4a3eaeaee5b5d6234b26224e
- Resulting SHA: recorded in this slice's completion report (the commit that includes this entry)
- Branch: audit/ws02-finalization
- Agent role/context: implementation agent (Act/Agent mode, highest reasoning); WS02 Slice 1
- Files changed:
  - internal/admin/server.go — new ManagedApplyCompletion{Context, Result, PreviousRaw} carrier type unifying the previously split history-write and completion-notification signals into ONE completion object handed to the composition root
  - internal/admin/config_apply.go — ConfigApplyResult gains FinalizationError string `json:"-"` (transport-only provenance; never serialized so the AC-05 invariant that the serialized result carries no history provenance is preserved)
  - internal/app/config_apply.go — removed the WriteManagedHistory hook; OnManagedApplyComplete is now `func(admin.ManagedApplyCompletion) admin.ManagedApplyFinalization`; notifyManagedApplyComplete takes the completion object and returns the fin under panic recovery (nil→zero fin); completeManagedApply forwards the exact previous on-disk bytes via ManagedApplyCompletion.PreviousRaw and threads HistorySnapshotID/HistoryError/FinalizationError from the returned fin onto the terminal result; the finalizer sets terminal.FinalizationError before completeManagedApply so a post-persistence tracking failure is echoed by the callback; toAdminConfigApplyResult projects FinalizationError
  - internal/app/serve.go — removed `coordinator.WriteManagedHistory = adminSrv.RecordManagedHistory`; OnManagedApplyComplete now derives ctx/res from the completion object, keeps the BeginFinalization single-claim guard, itself calls adminSrv.RecordManagedHistory(comp.Context, comp.Result, comp.PreviousRaw) AFTER claiming, and returns a ManagedApplyFinalization carrying the snapshot id / history error / echoed finalization error; toAdminConfigApplyResult projects FinalizationError
  - internal/app/config_apply_pending_test.go — wireProductionLedger and TestApplyStartedErrorSurfacesInFinalization migrated to the unified completion-object callback signature
  - internal/app/config_apply_test.go — all seven completion callbacks migrated to the unified signature; provenance-threading and panic tests updated to return/produce the fin themselves
- Production path verified: the async finalizer in applyCandidate (and the applyStageRestart + enqueue-failure terminal paths) route through completeManagedApply, which now hands the composition root a single ManagedApplyCompletion (request context, serialized terminal result, exact previous on-disk bytes). serve.go's OnManagedApplyComplete claims the transaction once via BeginFinalization, then performs the trusted history write itself (RecordManagedHistory) and returns the ManagedApplyFinalization; the coordinator threads that provenance back onto the terminal ApplyResult. History-writing and terminal finalization are now driven from one claim (H-05); the split WriteManagedHistory/OnManagedApplyComplete hooks are gone.
- Behavior implemented: unified managed-apply completion contract — one completion-object callback returns provenance instead of a void notification plus a separate history-write hook. The composition-root callback is the sole producer of the finalization (it calls RecordManagedHistory by itself after claiming). FinalizationError threads through the app result → admin projection (json:"-") → callback so a post-persistence pending-registration failure ("boom") surfaces in the returned fin. The serialized ConfigApplyResult never carries HistorySnapshotID/HistoryError/FinalizationError (AC-05 preserved); PreviousRaw (sensitive prior bytes) is forwarded only to the trusted in-process history writer.
- Tests added: none net-new; the existing WS01 completion/provenance/panic tests were migrated to the unified callback signature (TestManagedApplyFinalizationProvenanceThreaded, TestCompletionCallbackPanicDoesNotBlockHTTP, TestManagedApplyOutcomeCallbackFired, TestFastRestorationHTTPAndCallbackResultsMatch, TestShutdownReturnsCorrelatedSavedNotLive, TestSlowRestorationReturnsSavedNotLiveThenOneTerminalResult, TestApplyRegistersPendingRecordBeforeSavedNotLive, TestApplyStartedErrorSurfacesInFinalization). No test weakened; the provenance and panic-safety assertions were preserved and re-expressed against the returned fin.
- Commands run: go build ./... (pass); go vet ./internal/admin/... ./internal/app/... (pass); gofmt -d internal/app/config_apply.go internal/app/serve.go internal/admin/server.go internal/admin/config_apply.go (empty — no formatting diffs; repo-wide gofmt -l output is the pre-existing benign CRLF condition, not these edits); go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... (all three packages ok: admin, app, config).
- Commands unavailable: go test -race ./internal/admin/... and ./internal/app/... — UNVERIFIED: -race requires cgo and no C toolchain (gcc/clang) is installed in this environment (KNOWN_ENVIRONMENT_LIMITATION). Residual risk: the finalizer/waiter interleaving and the single-claim guard are not race-verified locally; deferred to the CI race matrix. The refactor adds no new shared mutable state — the completion object is built and consumed on the finalizer goroutine, and BeginFinalization retains its existing single-claim discipline.
- Console commands: none required — this slice is backend Go only. No internal/admin/ui source changed; no typecheck/lint/test/build or asset-drift gate was needed; internal/admin/assets/dist was not touched.
- Deviations:
  1. Requested a single completion object returning provenance → the spec code blocks are normative for behavior, not text. Implemented ManagedApplyCompletion with Context/Result/PreviousRaw and OnManagedApplyComplete returning admin.ManagedApplyFinalization; the composition root itself calls RecordManagedHistory after claiming → rationale: this makes the callback the sole producer of the fin and removes the separate WriteManagedHistory hook, satisfying the one-claim requirement (H-05) with repository-native types.
  2. FinalizationError surfacing: admin.ConfigApplyResult.FinalizationError uses `json:"-"` and the coordinator sets terminal.FinalizationError before completeManagedApply; the callbacks echo comp.Result.FinalizationError into the returned fin → rationale: preserves the AC-05 non-serialization invariant while letting the post-persistence "boom" tracking error reach fin.FinalizationError (required by TestApplyStartedErrorSurfacesInFinalization) without weakening the test.
- Self-review findings: single-purpose diff across 4 production files + 2 test files. No unwired helpers (notifyManagedApplyComplete/completeManagedApply are called by the three terminal paths; OnManagedApplyComplete is wired in serve.go and the tests). No error swallowing — the tracking error is threaded to FinalizationError; the callback panic is recovered so it cannot wedge the coordinator. No secrets serialized — PreviousRaw is forwarded only to the in-process history writer; FinalizationError/HistorySnapshotID/HistoryError all stay json:"-" / non-serialized. No generated bundle, closure report, or authentication/history/transaction truthfulness weakened.
- Independent review status: pending
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: WS02 Slice 02 (or the next coordinator-assigned slice)

---

### WS02_FINALIZATION / 02_EXPLICIT_FAILURE_HANDLING.md

- Parent SHA: fac342ad727f74f2e9460c0b0c86879119b597b7
- Resulting SHA: recorded in this slice's completion report (the commit that includes this entry)
- Branch: audit/ws02-finalization
- Agent role/context: implementation agent (Act/Agent mode, highest reasoning); WS02 Slice 2
- Files changed:
  - internal/app/config_apply.go — added the `OnManagedApplyFinalizationError func(applyID string, err error)` coordinator hook; replaced the silent `defer func() { _ = recover() }()` in notifyManagedApplyComplete with EXPLICIT panic handling (WS02 §3.6): the recovered panic is reconstructed into `fin.FinalizationError` ("managed apply finalization panic: %v") via a named return and, when wired, reported to `OnManagedApplyFinalizationError(comp.Result.ApplyID, errors.New(fin.FinalizationError))`.
  - internal/app/serve.go — added a process-lifetime `lastManagedApplyFinalizationErr atomic.Pointer[string]` advisory-health flag; extended deps.AdminHealth to surface it as an advisory `managed_finalization` degradation on /readyz/overview after the existing admin-reload check; wired coordinator.OnManagedApplyFinalizationError to (1) write a structured error log, (2) increment the finalization-error metric, (3) set the advisory health flag, and (4) best-effort write a terminal ledger record carrying FinalizationError.
  - internal/observability/metrics.go — added the unlabeled `jul_managed_apply_finalization_errors_total` counter (registered on the private registry) with the `ObserveManagedApplyFinalizationError()` wrapper; the counter is deliberately label-free so a callback-panic signal cannot leak apply IDs, actors, or config versions as unbounded cardinality.
  - internal/observability/cardinality_test.go — registered `jul_managed_apply_finalization_errors_total` (labels: none) in the frozen TestMetricLabelPolicy map and exercised its hook in exerciseAllMetrics, keeping the cardinality-regression guard authoritative (test strengthened, not weakened).
  - internal/app/config_apply_test.go — strengthened TestCompletionCallbackPanicDoesNotBlockHTTP: it still asserts the committed apply stays OK and the HTTP path is not blocked, and now additionally asserts the recovered panic is threaded onto result.FinalizationError and that OnManagedApplyFinalizationError is invoked exactly once with the apply ID and the reconstructed panic error.
- Production path verified: the async finalizer in applyCandidate (and the applyStageRestart + enqueue-failure terminal paths) call completeManagedApply → notifyManagedApplyComplete. When the composition-root OnManagedApplyComplete callback panics, the deferred recover now (a) sets fin.FinalizationError, which completeManagedApply threads onto the terminal ApplyResult, and (b) invokes the coordinator's OnManagedApplyFinalizationError. In serve.go that hook logs the failure, increments jul_managed_apply_finalization_errors_total, stores the advisory lastManagedApplyFinalizationErr surfaced by deps.AdminHealth, and best-effort completes a per-ID ledger record carrying FinalizationError. The committed apply is never rolled back — the raw configuration stays roll-back-able — and the finalizer goroutine is never wedged.
- Behavior implemented: a managed-apply completion-callback panic is now EXPLICIT rather than silently discarded (WS02 §3.2 defect 3). The recovered panic surfaces four ways: threaded onto the terminal result's FinalizationError, a structured error log, the jul_managed_apply_finalization_errors_total metric, and an advisory managed_finalization admin-health degradation, plus a best-effort ledger record. No serialized ConfigApplyResult gains a new serialized field (FinalizationError remains json:"-"; the new metric is unlabeled), so the AC-05 non-serialization invariant and the metric-cardinality policy are both preserved.
- Tests added: TestCompletionCallbackPanicDoesNotBlockHTTP strengthened (package integration: coordinator, no HTTP server) — proves a finalization-callback panic does not block the HTTP path, does not fail the committed apply, is threaded onto FinalizationError, and invokes OnManagedApplyFinalizationError exactly once with the apply ID and reconstructed error. TestMetricLabelPolicy/exerciseAllMetrics extended (unit: observability policy) to cover the new unlabeled counter so the cardinality guard stays authoritative. No existing test weakened.
- Commands run: go build ./... (pass); go vet ./internal/admin/... ./internal/app/... ./internal/observability/... (pass); gofmt -l internal/app/config_apply.go internal/app/serve.go internal/app/config_apply_test.go internal/observability/metrics.go internal/observability/cardinality_test.go (empty — no formatting diffs); git diff --check (clean, only benign LF→CRLF working-copy warnings, the pre-existing environment condition); go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... ./internal/observability/... (all four packages ok: admin, app, config, observability).
- Commands unavailable: go test -race ./internal/admin/... ./internal/app/... ./internal/observability/... — UNVERIFIED: -race requires cgo and no C toolchain (gcc/clang) is installed (KNOWN_ENVIRONMENT_LIMITATION); deferred to the CI race matrix. Residual risk: the lastManagedApplyFinalizationErr atomic pointer and the best-effort ledger Complete are exercised on the finalizer goroutine and read by the admin-health/HTTP goroutine; both use existing concurrency-safe primitives (atomic.Pointer, the registry's internal mutex), so no new unsynchronized shared state was introduced.
- Console commands: none required — this slice is backend Go only. No internal/admin/ui source changed; no typecheck/lint/test/build or asset-drift gate was needed; internal/admin/assets/dist was not touched.
- Deviations:
  1. The §3.6 skeleton references completion.Result.ApplyID; the repository-native completion object is admin.ManagedApplyCompletion, so the implementation uses comp.Result.ApplyID (the same field on the repository-native carrier) → rationale: the skeleton is normative for behavior, not identifiers; the field and semantics match exactly.
  2. "Set an advisory health state" and "best-effort write a terminal ledger record" are realized with repository-native surfaces (deps.AdminHealth via a new atomic.Pointer[string], and ManagedApplyRegistry.Complete) rather than a new subsystem → rationale: reuses existing, semantically-equivalent abstractions and avoids scope creep, per the decision hierarchy (prefer repository-native abstractions, minimize scope).
- Self-review findings: single-purpose diff across 3 production files + 2 test files. No unwired helpers — OnManagedApplyFinalizationError is invoked by notifyManagedApplyComplete and wired in serve.go; ObserveManagedApplyFinalizationError is called by the serve.go hook and the cardinality exerciser. No error swallowing — the previously discarded panic is now surfaced four ways. No secrets serialized — the new metric is unlabeled, the advisory detail and the ledger FinalizationError carry only the panic message (never PreviousRaw, TOML, or credentials); FinalizationError remains json:"-". No authentication/history/transaction truthfulness weakened; no generated bundle or closure report touched.
- Independent review status: pending
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: WS02 Slice 03 (or the next coordinator-assigned slice)

---

### WS02_FINALIZATION / 03_FINALIZER_ORCHESTRATOR.md

- Parent SHA: 9807214ee122b068eed495ad3d3fa54d0b5fce29
- Resulting SHA: recorded in this slice's completion report (the commit that includes this entry)
- Branch: audit/ws02-finalization
- Agent role/context: implementation agent (Act/Agent mode, highest reasoning); WS02 Slice 3
- Files changed:
  - internal/app/managed_apply_finalizer.go (new) — the single terminal-finalization orchestrator `managedApplyFinalizer` with `Finalize(admin.ManagedApplyCompletion) admin.ManagedApplyFinalization` (WS02 §3.7 Step 4). It CLAIMS the transaction via `ManagedApplyRegistry.ClaimFinalization` BEFORE the trusted history write (fixing §3.2 defect 2), performs `RecordManagedHistory`, emits `ObserveManagedApplyFinalized`/`ObserveManagedApplyHistory`, records the terminal audit via `RecordManagedApplyOutcome`, publishes the durable per-ID ledger record via `Complete` and — unlike the former inline callback — no longer ignores the `Complete` error (§3.2 defect 4): it threads it onto FinalizationError, reports it, and retries once. A claim error fails closed with an explicit FinalizationError (§3.2 defect 5); FinalizationError is reliably threaded onto the terminal result (§3.2 defect 6). Ships the skeleton helpers `appendFinalizationError`, `managedReloadOutcome`, `managedRestoredLabel`, `projectManagedApplyOutcome`, plus `updateLatestIfNewest` (the AC-04 high-water guard) and `reportFinalizationError` (structured log + finalization-error metric + advisory-health flag).
  - internal/app/serve.go — replaced the ~110-line inline `coordinator.OnManagedApplyComplete` body with construction of the `managedApplyFinalizer` (registry, adminSrv, metrics, log, the existing `lastManagedApply`/`lastManagedApplySeq`/`lastManagedApplyMu` high-water trio, and a `setAdvisoryHealth` sink that stores into the existing `lastManagedApplyFinalizationErr` pointer) and assigned `coordinator.OnManagedApplyComplete = finalizer.Finalize`. The §3.6 `OnManagedApplyFinalizationError` panic-abort hook and `deps.AdminHealth` advisory surface from Slice 2 are unchanged.
  - internal/observability/metrics.go — added the bounded `jul_managed_apply_history_total{operation,result}` counter (registered on the private registry) with the `ObserveManagedApplyHistory(operation, result)` wrapper; `result` is the snapshot disposition (recorded/skipped/failed) and both labels are bounded, low-cardinality values — never an apply ID, actor, path or version.
  - internal/observability/cardinality_test.go — registered `jul_managed_apply_history_total` (labels: operation, result) in the frozen TestMetricLabelPolicy map and exercised its hook in exerciseAllMetrics, keeping the cardinality-regression guard authoritative (test strengthened, not weakened).
  - internal/app/managed_apply_finalizer_test.go (new) — TestManagedApplyFinalizerExactlyOnce.
- Production path verified: HTTP apply → ConfigApplyCoordinator.ApplyRaw persists the candidate and enqueues the reload; the async finalizer goroutine builds the terminal ApplyResult and calls `completeManagedApply → notifyManagedApplyComplete → coordinator.OnManagedApplyComplete`, which is now `managedApplyFinalizer.Finalize`. Finalize claims the ID (ClaimFinalization), writes the configuration-history snapshot (adminSrv.RecordManagedHistory, consuming the sensitive PreviousRaw in-process only), emits the finalized + history metrics, records the terminal audit (adminSrv.RecordManagedApplyOutcome), publishes the durable per-ID ledger record (registry.Complete) carrying HistorySnapshotID/HistoryError/FinalizationError, and advances the singular latest-outcome pointer under the monotonic high-water guard. A duplicate terminal callback is deduplicated by the claim and repeats no side effect. This slice creates and wires the orchestrator only; routing the coordinator's hot/enqueue/stage terminal paths through completeManagedApply is WS02 Slice 4 (§3.8) and was deliberately not started.
- Behavior implemented: every managed-apply terminal callback now flows through ONE orchestrator that claims-before-history and runs each side effect exactly once per apply ID. The terminal-ledger Complete error is surfaced (log + finalization-error metric + advisory health + FinalizationError + one retry) instead of silently ignored (§3.2 defect 4); a claim error fails closed (§3.2 defect 5); history no longer precedes the finalization claim (§3.2 defect 2). No serialized ConfigApplyResult gains a new field (FinalizationError stays json:"-"); the new metric is bounded to operation×result, so the AC-05 non-serialization invariant and the metric-cardinality policy are both preserved.
- Tests added: TestManagedApplyFinalizerExactlyOnce (package integration: real ConfigApplyCoordinator + real admin.Server history writer + real ManagedApplyRegistry; no HTTP server, no sleeps, no manually seeded ledger state) — drives a real applied_live apply through the production wiring (coordinator.OnManagedApplyComplete = finalizer.Finalize) and proves exactly-once terminal finalization: exactly one raw history snapshot on disk whose id is threaded onto the returned finalization AND the durable per-ID ledger record, the latest-outcome pointer advanced, no advisory-health flag set on a clean finalize, and a duplicate terminal callback deduplicated by the ClaimFinalization guard (no second snapshot, no new FinalizationError, same recorded provenance). TestMetricLabelPolicy/exerciseAllMetrics extended (unit: observability policy) to cover the new bounded counter so the cardinality guard stays authoritative. No existing test weakened.
- Commands run: go build ./... (pass); go vet ./internal/admin/... ./internal/app/... ./internal/observability/... (pass); gofmt -l internal/app/managed_apply_finalizer.go internal/app/managed_apply_finalizer_test.go internal/app/serve.go internal/observability/metrics.go internal/observability/cardinality_test.go (empty after gofmt -w on the two new files — no formatting diffs); git diff --check (clean, only benign LF→CRLF working-copy warnings, the pre-existing environment condition); go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... ./internal/observability/... (all four packages ok: admin, app, config, observability).
- Commands unavailable: go test -race ./internal/admin/... ./internal/app/... ./internal/observability/... — UNVERIFIED: -race requires cgo and no C toolchain (gcc/clang) is installed (KNOWN_ENVIRONMENT_LIMITATION); deferred to the CI race matrix. Residual risk: the finalizer runs on the coordinator's async finalizer goroutine and touches the latest-outcome pointer/seq (guarded by the existing latestMu + atomic.Uint64 CAS), the registry (its internal mutex), and the advisory-health pointer (atomic.Pointer) — all existing concurrency-safe primitives, so no new unsynchronized shared state was introduced.
- Console commands: none required — this slice is backend Go only. No internal/admin/ui source changed; no typecheck/lint/test/build or asset-drift gate was needed; internal/admin/assets/dist was not touched.
- Deviations:
  1. The §3.7 skeleton types the latest-outcome guard fields as embedded pointers and references `completion.Result.ApplyID`; the implementation uses the repository-native `admin.ManagedApplyOutcome`/`ManagedApplyRegistry`/`ManagedApplyCompletion` carriers and the process-lifetime `lastManagedApply*` trio already present in serve.go → rationale: the skeleton is normative for behavior, not identifiers; reuses existing high-water state instead of introducing a parallel one (prefer repository-native abstractions, minimize scope).
  2. The skeleton calls `f.metrics.ObserveManagedApplyHistory(...)`, which did not exist. Added it as a bounded `jul_managed_apply_history_total{operation,result}` counter rather than inventing per-ID labels → rationale: makes the history side effect observable exactly as the skeleton specifies while keeping the metric-cardinality policy authoritative (registered in the frozen cardinality test).
  3. Advisory health and the terminal ledger record are realized with the repository-native surfaces wired in Slice 2 (the `lastManagedApplyFinalizationErr atomic.Pointer[string]` behind deps.AdminHealth, and `ManagedApplyRegistry.Complete`) via a `setAdvisoryHealth` sink on the finalizer, not a new subsystem → rationale: reuses existing, semantically-equivalent abstractions and avoids scope creep. The §3.6 panic-abort hook remains the writer of the terminal record for the panic path; the finalizer owns the normal-path Complete.
- Self-review findings: single-purpose diff across 2 production files + 1 production-adjacent metric file + 2 test files (1 new). No unwired helpers — managedApplyFinalizer.Finalize is assigned to coordinator.OnManagedApplyComplete in serve.go; ObserveManagedApplyHistory is called by Finalize and the cardinality exerciser; every skeleton helper (appendFinalizationError, managedReloadOutcome, managedRestoredLabel, projectManagedApplyOutcome, updateLatestIfNewest, reportFinalizationError) is reachable from Finalize. No error swallowing — the previously ignored registry.Complete error is now surfaced five ways (log, metric, advisory health, FinalizationError, one retry). No secrets serialized — PreviousRaw is consumed only by the in-process history writer; the new metric is bounded to operation×result; the advisory detail and ledger FinalizationError carry only the bounded error message; FinalizationError stays json:"-". No authentication/history/transaction truthfulness weakened; no generated bundle or closure report touched.
- Independent review status: pending
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: WS02 Slice 04 (or the next coordinator-assigned slice)

---

### WS02_FINALIZATION / 04_ROUTE_ALL_TERMINAL_PATHS.md

- Parent SHA: b95f8d642f931700b7fef034fc344e7f0ba4f594
- Resulting SHA: recorded in this slice's completion report (the commit that includes this entry)
- Branch: audit/ws02-finalization
- Agent role/context: implementation agent (Act/Agent mode, highest reasoning); WS02 Slice 4
- Files changed:
  - internal/app/config_apply.go — completed the stage_restart terminal result (WS02 §3.8 "Stage success" / §3.2 defect 8) so a committed stage carries the same first-class persistence truth as the hot path before it is routed through the single `completeManagedApply` helper: added `Persisted: true` (the candidate bytes are on disk), `FinalDiskVersion: persistedVersion` (the staged candidate now on disk), and `FinalServingVersion: liveVersion` (the still-serving live version, unchanged since a stage does not touch the running runtime). The three §3.8 terminal paths — hot completion, enqueue failure, and stage success — were already routed through `completeManagedApply` by the WS02 completion-contract refactor (commit fac342ad); this slice's remaining §3.8 work was the stage result's persistence fields, so no `recordManagedHistory(...)` + `notifyManagedApplyComplete(...)` inline pair remained to replace.
  - internal/app/config_apply_stage_finalize_test.go (new) — TestStageRestartTerminalFinalizedThroughLedger.
- Production path verified: HTTP stage apply → ConfigApplyCoordinator.ApplyRaw → applyStageRestart persists the candidate atomically, writes+promotes the planned-restart sidecar (no live reload is enqueued), builds the complete terminal ApplyResult (OK, Persisted, PersistedVersion/DesiredVersion, FinalDiskVersion = the staged candidate, FinalServingVersion = the still-serving live version), and routes it through the SINGLE `completeManagedApply → notifyManagedApplyComplete → coordinator.OnManagedApplyComplete` helper — the same terminal orchestrator the hot and enqueue-failure paths use. In production OnManagedApplyComplete is `managedApplyFinalizer.Finalize` (WS02 Slice 3), which claims-before-history and publishes exactly one durable per-ID terminal ledger record. Because a stage submits no reload, OnManagedApplyStarted (the provisional saved_not_live pending registration) is not fired; the terminal record is created directly by the completion helper.
- Behavior implemented: every persisted mutation — hot apply, enqueue failure, and committed stage_restart (create and update) — reaches its terminal state through one finalization helper, and the stage terminal result now reports complete persistence truth (Persisted/FinalDiskVersion/FinalServingVersion) instead of omitting it (§3.2 defect 8). No serialized secret or raw config is added; FinalDiskVersion/FinalServingVersion are bounded canonical versions already carried by the hot path. No reload-history separate write precedes the finalization claim on the stage path (there is no separate stage history write; the single completeManagedApply owns it).
- Tests added: TestStageRestartTerminalFinalizedThroughLedger (package integration: real ConfigApplyCoordinator + file-backed PlannedRestartStore + real stage preflight, wired to a real admin.ManagedApplyRegistry through the shared production-fidelity harness wireProductionLedger — the same OnManagedApplyStarted/OnManagedApplyComplete field mapping serve.go installs; no HTTP server, no sleeps, no manually seeded ledger state, no reload goroutine). It drives a real committed stage_restart and proves: the terminal result and the durable per-ID ledger record both report Persisted=true, FinalDiskVersion=the staged candidate, and FinalServingVersion=the still-serving live version (asserting disk≠serving so FinalServingVersion is not a copy of the on-disk candidate); OnManagedApplyStarted did NOT fire (a stage enqueues no reload); the terminal completion callback fired exactly once with no FinalizationError; and exactly one terminal record exists for the apply ID (TerminalCount()==1). No existing test weakened.
- Commands run: go build ./... (pass); go vet ./internal/admin/... ./internal/app/... ./internal/observability/... (pass); gofmt -l internal/app/config_apply.go internal/app/config_apply_stage_finalize_test.go (empty — no formatting diffs); git diff --check (clean, only the benign LF→CRLF working-copy warning, the pre-existing environment condition); go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... ./internal/observability/... (all four packages ok: admin, app, config, observability); go test -count=1 -run TestStageRestartTerminalFinalizedThroughLedger ./internal/app/ (PASS).
- Commands unavailable: go test -race ./internal/admin/... ./internal/app/... — UNVERIFIED: -race requires cgo and no C toolchain (gcc/clang) is installed (KNOWN_ENVIRONMENT_LIMITATION); deferred to the CI race matrix. Residual risk: the stage_restart terminal path is synchronous on the caller's goroutine (no reload finalizer goroutine, no restoration), and it only reuses existing concurrency-safe primitives (applyMu/c.mu for the file write, the registry's internal mutex for Complete), so no new unsynchronized shared state was introduced.
- Console commands: none required — this slice is backend Go only. No internal/admin/ui source changed; no typecheck/lint/test/build or asset-drift gate was needed; internal/admin/assets/dist was not touched.
- Deviations:
  1. Requested design (§3.8 "Hot terminal completion"): replace an inline `terminal = c.recordManagedHistory(...)` + `c.notifyManagedApplyComplete(...)` pair with a single `completeManagedApply(...)` call on the hot path. Implemented design: no change was needed — the hot, enqueue-failure, and stage terminal paths already route through `completeManagedApply` (introduced by the WS02 completion-contract refactor, commit fac342ad, before this slice's parent). Rationale: the skeleton is normative for the end-state behavior (one helper owns every terminal path), which is already satisfied; re-introducing and re-replacing an inline history+notify pair would be a pointless churn that violates minimize-scope. The remaining, not-yet-satisfied §3.8 behavior was the stage result's persistence fields (§3.2 defect 8), which this slice implements.
- Self-review findings: single-purpose production diff (three added struct fields + an explanatory comment in applyStageRestart) plus one new package-integration test. No unwired code — the new fields flow through the existing completeManagedApply → toAdminConfigApplyResult → registry.Complete path and are asserted on both the returned result and the durable ledger record. No error swallowing — no error handling was touched. No secrets serialized — FinalDiskVersion/FinalServingVersion are bounded canonical versions already emitted by the hot path; no raw TOML/secret/actor/token added; FinalizationError stays json:"-". No authentication/authorization/history/transaction truthfulness weakened; no generated Console bundle or closure report touched. The new test uses only the production wiring (wireProductionLedger), no sleeps, and no manually seeded ledger state.
- Independent review status: pending
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: the next coordinator-assigned slice (WS02 §3.8 terminal routing complete)

---

### WS02_FINALIZATION / 05_ADVISORY_HEALTH_TESTS_REVIEW.md

- Parent SHA: ec2fce451e52e505f2d9d26145124f3f20294294
- Resulting SHA: recorded in this slice's completion report (the commit that includes this entry)
- Branch: audit/ws02-finalization
- Agent role/context: implementation agent (Act/Agent mode, highest reasoning); WS02 Slice 5
- Files changed:
  - internal/admin/server.go — added the `ManagedApplyAdvisory{Healthy, At, ApplyID, Detail}` type (WS02 §3.9) and the `Deps.ManagedApplyFinalizationHealth func() *ManagedApplyAdvisory` hook. The advisory carries only bounded, low-cardinality metadata (never raw TOML, secrets, or actor tokens) and is documented as non-readiness.
  - internal/admin/projection_types.go — added `RuntimeOverview.ManagedApplyFinalization *ManagedApplyAdvisory` with json tag `managed_apply_finalization,omitempty`, rendered as an advisory finalization banner distinct from `admin_health` and `last_managed_apply`.
  - internal/admin/api.go — `handleRuntimeOverview` now populates `out.ManagedApplyFinalization` from `s.deps.ManagedApplyFinalizationHealth()` when wired, independently of `adminHealthProjection()`.
  - internal/app/serve.go — replaced the process-lifetime `lastManagedApplyFinalizationErr atomic.Pointer[string]` with `lastManagedApplyFinalization atomic.Pointer[admin.ManagedApplyAdvisory]`; REMOVED the `managed_finalization` branch from `deps.AdminHealth` (decoupling the finalization advisory from `/readyz`, per §3.9 "Do not connect it to readiness"); wired `deps.ManagedApplyFinalizationHealth` to read that pointer; updated the `OnManagedApplyFinalizationError` panic hook to publish an unhealthy advisory (ApplyID+detail) instead of a `*string`; changed the finalizer field from `setAdvisoryHealth(detail string)` to `setAdvisory(admin.ManagedApplyAdvisory)`.
  - internal/app/managed_apply_finalizer.go — replaced `setAdvisoryHealth func(detail string)` with `setAdvisory func(admin.ManagedApplyAdvisory)`; added `publishAdvisory` (called on every terminalization: healthy after a clean finalize — clearing any prior degradation — else unhealthy) and `advisoryDetail` (FinalizationError precedence, else a `configuration history: <err>` degradation); `reportFinalizationError` now publishes an unhealthy advisory on claim/complete failure. History snapshot/metadata failures are now tracked as an advisory degradation (§3.9's three tracked sources: finalization panic, terminal-ledger completion failure, history snapshot/metadata failure).
  - internal/app/managed_apply_finalizer_test.go — updated the existing exactly-once test to the new `setAdvisory` signature and strengthened its clean-finalize assertion: a clean finalize now publishes a HEALTHY advisory carrying the apply ID and no detail (not nil).
  - internal/admin/managed_apply_advisory_test.go (new) — TestManagedApplyFinalizationAdvisoryNonReadiness.
  - internal/app/managed_apply_advisory_test.go (new) — TestManagedApplyFinalizationAdvisoryTracksHistoryDegradationAndClears.
- Production path verified: async terminal finalization → coordinator.OnManagedApplyComplete = managedApplyFinalizer.Finalize → publishAdvisory → serve.go's `setAdvisory` stores the process-lifetime `lastManagedApplyFinalization` pointer → admin `handleRuntimeOverview` reads it through `deps.ManagedApplyFinalizationHealth` and serializes `managed_apply_finalization`. The finalization-callback panic path (coordinator recover → OnManagedApplyFinalizationError) publishes the same advisory shape. `/readyz` (admin.handleReadyz → Server.AdminHealthStatus → deps.AdminHealth) no longer observes any finalization degradation — the `managed_finalization` reason was removed — so a finalization/history degradation is visible in the Overview without ever failing readiness or rolling back a committed apply.
- Behavior implemented: WS02 §3.9 advisory, non-readiness managed-apply finalization health. It tracks the three degradation sources (finalization panic; terminal-ledger completion failure; configuration-history snapshot/metadata failure), is cleared only by a subsequent managed transaction that finalizes without finalization/history degradation, is exposed through the Runtime Overview as `managed_apply_finalization`, and is NOT connected to readiness. A finalization failure is now visible without falsely rolling back a committed runtime apply (§3.11).
- Tests added: TestManagedApplyFinalizationAdvisoryNonReadiness (package admin, HTTP integration over the real route stack s.routes(): drives GET /api/runtime/overview and GET /readyz through httptest; proves the advisory is omitted before any apply, that a Healthy=false advisory is surfaced in the overview but leaves /readyz=200 and admin_health absent, and that a subsequent Healthy=true advisory is surfaced — the §3.9 non-readiness invariant). TestManagedApplyFinalizationAdvisoryTracksHistoryDegradationAndClears (package app, package integration: real ConfigApplyCoordinator + real admin.Server history writer + real ManagedApplyRegistry, no HTTP server, no sleeps, no manually seeded ledger state, no mocked writer; the history failure is injected deterministically by making the configured history-dir path a regular file so the real RecordManagedHistory os.MkdirAll fails). It proves a history snapshot failure publishes an UNHEALTHY advisory carrying the apply ID and a `configuration history` detail while the committed apply stays OK=true and the durable ledger record carries the history_error, then that un-poisoning the dir and running a second clean apply publishes a HEALTHY advisory that clears the prior degradation. Existing TestManagedApplyFinalizerExactlyOnce updated (not weakened): its clean-finalize assertion now proves the healthy advisory is published. No existing test weakened.
- Commands run: go build ./... (pass); go vet ./internal/admin/... ./internal/app/... ./internal/observability/... (pass); gofmt -l on all changed files (empty after gofmt -w — no formatting diffs); go test -count=1 -run TestManagedApplyFinalizerExactlyOnce|TestManagedApplyFinalizationAdvisoryTracksHistoryDegradationAndClears ./internal/app/ (PASS); go test -count=1 -run TestManagedApplyFinalizationAdvisoryNonReadiness ./internal/admin/ (PASS); go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... ./internal/observability/... (all four packages ok); git diff --check (clean, only the benign LF→CRLF working-copy warning, the pre-existing environment condition).
- Commands unavailable: go test -race ./internal/admin/... ./internal/app/... — UNVERIFIED: -race requires cgo and no C toolchain (gcc/clang) is installed (KNOWN_ENVIRONMENT_LIMITATION); deferred to the CI race matrix. Residual risk: the advisory is a single process-lifetime atomic.Pointer[admin.ManagedApplyAdvisory] published under the finalizer's existing terminal path (already serialized per ID by the ledger claim) and read locklessly by the overview handler; it introduces no new unsynchronized shared state (atomic store/load only).
- Console commands: none required — this slice is backend Go only. No internal/admin/ui source changed; the new `managed_apply_finalization` overview field is additive JSON (omitempty) consumed by a future Console banner; no typecheck/lint/test/build or asset-drift gate was needed; internal/admin/assets/dist was not touched.
- Deviations:
  1. Requested design (§3.9 skeleton `ManagedApplyAdvisory`): implemented verbatim (Healthy/At/ApplyID/Detail with the shown json tags). Additional repository-native decisions: (a) the advisory is surfaced through a dedicated `Deps.ManagedApplyFinalizationHealth` hook + a new `RuntimeOverview.ManagedApplyFinalization` field rather than reusing `AdminHealthStatus`, because the parent commit routed the finalization error through `deps.AdminHealth` (which gates `/readyz`) — §3.9 forbids readiness coupling, so this slice DECOUPLED it. Rationale follows the decision hierarchy (make degradation explicit without gating readiness) and §3.9 ("Do not connect it to readiness").
  2. §3.10 test suite: this slice adds the advisory-specific tests (non-readiness surfacing + history-degradation-and-clear). The exactly-once, stage-terminal, callback-panic, and ordering tests required by §3.10 were already added by the accepted WS02 Slices 1–4 (TestManagedApplyFinalizerExactlyOnce, TestStageRestartTerminalFinalizedThroughLedger, the callback-panic and ordering tests in config_apply_test.go) and were preserved, not re-implemented, per the slice-entry condition "Do not reimplement or redesign accepted earlier slices."
- Self-review findings: single-purpose diff — one new advisory type + one dep + one overview field + one handler read, the serve.go readiness decoupling and advisory publish, and the finalizer's publishAdvisory/advisoryDetail. No unwired code: `ManagedApplyFinalizationHealth` is both written (serve.go panic hook + finalizer setAdvisory) and read (api.go overview). No error swallowing — history/complete/claim failures now additionally drive the advisory. No secrets serialized — the advisory carries only Healthy/At/ApplyID/Detail where Detail is a bounded finalization/history error message; no raw TOML/secret/actor/token. No authentication/authorization/history/transaction truthfulness weakened; readiness is now MORE truthful (a committed-but-history-degraded apply no longer fails /readyz). No generated Console bundle or closure report touched. Tests use only production wiring, no sleeps, and no manually seeded ledger state; the history failure is injected through the real filesystem writer.
- Independent review status: pending
- Reviewer blockers: none
- Blocker-fix SHA: n/a
- Accepted SHA: pending coordinator acceptance
- Next execution file: the next coordinator-assigned slice (WS02 finalization workstream — advisory health + tests complete)

---

## Program-level open items

- Exact-head CI pending: yes - exact-head CI not yet run against the bootstrap commit
- Independent final re-audit pending: yes
- Security/concurrency sign-off pending: yes
- Frontend sign-off pending: yes (baseline Console suite green; per-slice sign-off pending)
- Closure report status: not started (bootstrap only; no audit item closed)

---

## Workstream acceptance records

### Workstream acceptance record - WS01_MANAGED_LEDGER

- Workstream: WS01_MANAGED_LEDGER - Managed apply identity and pending ledger lifecycle
- Branch: audit/ws01-managed-ledger
- Base SHA: e707c41587a335cc92b643068b1c46053eff2621
- Final reviewed SHA: a1c7a3e11195477601b30a067e8a2ed0cf509b91
- Independent reviewer verdict: APPROVE
- Reviewer context/identifier: Independent APPROVE, 0 required blocker fixes; all 5 objectives verified on the real production path (pending-before-202 closed; boot-scoped IDs w/ strict grammar; secret-free projection, OwnerTokenID json:"-"; config:apply OR status:read fail-closed 401/503; registry no-downgrade + start/deadline preserved). Non-blocking: ClaimFinalization/ManagedApplyFinalizing tested-but-unwired, correctly staged for WS02.
- Focused test evidence: go build ./...; go vet ./internal/admin/... ./internal/app/...; go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/... (all pass); five new WS01 tests PASS (vertical 202->terminal, no-downgrade, start/deadline preserved, disabled/expired-401, Blocked-503).
- Race evidence: UNVERIFIED - go test -race unavailable locally (no cgo/C toolchain); deferred to CI race matrix at exact SHA.
- Console evidence: no additional Console change in this acceptance; the Console client schema (deadline) and the prior green Console suite baseline remain intact from earlier slices; internal/admin/assets/dist not touched during acceptance.
- Commands unavailable and deferred to CI: (1) -race on finalizer/waiter interleaving + registry concurrency; (2) exact-SHA CI green.
- Non-blocking follow-ups: ClaimFinalization/ManagedApplyFinalizing tested-but-unwired, correctly staged for WS02.
- Next workstream branch: audit/ws02-finalization
- Next expected parent SHA: a1c7a3e11195477601b30a067e8a2ed0cf509b91

### Workstream acceptance record - WS02_FINALIZATION

- Workstream: WS02_FINALIZATION - Unified exactly-once terminal finalization
- Branch: audit/ws02-finalization
- Base SHA: bb666a655a14d25b4a3eaeaee5b5d6234b26224e
- Final reviewed SHA: 10c432fafd86de5d33891beada59886e72baccc8
- Independent reviewer verdict: APPROVE
- Reviewer context/identifier: Independent reviewer (fresh Act/Agent task, file edits DISABLED, read + safe-terminal only). Verdict: APPROVE. Range bb666a655a14d25b4a3eaeaee5b5d6234b26224e..10c432fafd86de5d33891beada59886e72baccc8 = exactly 5 commits (S01-S05), base is ancestor of head, 16 files, +1702/-188; branch audit/ws02-finalization; HEAD == Head SHA verified BEFORE and AFTER review, working tree clean throughout (read-only integrity confirmed). Production wiring verified by file:line, not trusted: coordinator.OnManagedApplyComplete = managedApplyFinalizer.Finalize (serve.go:766); all three §3.8 terminal paths route through the single completeManagedApply helper (hot config_apply.go:1084, enqueue-failure :1023, stage-success :836); claim-before-history ordering in managed_apply_finalizer.go (ClaimFinalization:97 -> RecordManagedHistory:126 -> Complete:168); registry.Complete error surfaced five ways and retried once (not ignored, defect 4); callback panic recovered into FinalizationError + OnManagedApplyFinalizationError (committed apply never rolled back, defect); stage-success persistence truth Persisted/FinalDiskVersion/FinalServingVersion (defect 8); §3.9 advisory surfaced only via ManagedApplyFinalizationHealth/managed_apply_finalization overview field and DECOUPLED from /readyz (serve.go:419-425, §3.11). Security: ConfigApplyResult.FinalizationError is json:"-"; PreviousRaw forwarded only to the trusted in-process history writer, never logged/retained; advisory carries only bounded Healthy/At/ApplyID/Detail. Metric cardinality enforced by TestMetricLabelPolicy (finalization-errors counter unlabeled; history counter bounded to {operation,result}). Required blocker fixes: NONE. Regressions: NONE. Recommended accepted SHA: 10c432fafd86de5d33891beada59886e72baccc8.
- Focused test evidence: go build ./... -> OK. go vet ./internal/app/ ./internal/admin/ ./internal/observability/ -> OK. go test ./internal/app/ ./internal/admin/ ./internal/observability/ -run "ManagedApply|Finaliz|StageRestartTerminal|Advisory|Readyz|Cardinality|MetricLabel|History" -count=1 -> all ok. go test ./internal/app/ ./internal/admin/ ./internal/observability/ -count=1 (full packages) -> all ok. Test-evidence quality HIGH: TestManagedApplyFinalizerExactlyOnce, TestManagedApplyFinalizationAdvisoryTracksHistoryDegradationAndClears, and TestStageRestartTerminalFinalizedThroughLedger drive the REAL managedApplyFinalizer.Finalize + real admin.Server history writer + real ManagedApplyRegistry. Exactly-once proven by counting raw history-snapshot files (=1 after a duplicate callback). History failure injected through the REAL filesystem (poisoned history dir -> real os.MkdirAll failure), not a mock. Advisory non-readiness test drives the real s.routes() stack (no HTTP/authorization bypass). Concurrency ordering uses channel synchronization, not sleeps-for-assertions. No manually seeded ledger state; no existing test weakened.
- Race evidence: UNVERIFIED locally. go test -race is genuinely unavailable in this environment: CGO_ENABLED=0 and no C toolchain (go test -race fails with "-race requires cgo"; gcc returns exit 255). Deferral is HONEST (a real environment limitation, not a masked failure). Residual risk is bounded: the finalization advisory is a single process-lifetime atomic.Pointer[admin.ManagedApplyAdvisory] published under the finalizer's already-per-ID-serialized terminal path (ledger claim) and read locklessly by the overview handler (atomic store/load only); no new unsynchronized shared state introduced. Deferred to the WS07/91 exact-SHA CI race matrix.
- Console evidence: none required - WS02 is backend Go only; no internal/admin/ui source changed and internal/admin/assets/dist not touched. The new managed_apply_finalization overview field is additive JSON (omitempty) consumed by a future Console banner; the prior green Console suite baseline remains intact.
- Commands unavailable and deferred to CI: (1) go test -race across internal/app, internal/admin, internal/observability at the exact accepted SHA 10c432fafd86de5d33891beada59886e72baccc8 (WS07/91 CI race matrix); (2) full GitHub Actions certification / multi-OS (macOS + Linux) CI matrix at the exact accepted SHA (final CI-certification task, 91).
- Non-blocking follow-ups: NONE (no required blocker fixes; no regressions).
- Next workstream branch: audit/ws03-absolute-deadline
- Next expected parent SHA: 10c432fafd86de5d33891beada59886e72baccc8

### Workstream acceptance record - WS03_ABSOLUTE_DEADLINE

- Workstream: WS03_ABSOLUTE_DEADLINE - One absolute managed-apply deadline
- Branch: audit/ws03-absolute-deadline
- Base SHA: 4e9f8077b613cb1f803afd8a1b21dcd9b828ccf6
- Final reviewed SHA: e22e06e19b5c173455b57f2a6afe721c782d4299
- Independent reviewer verdict: APPROVE
- Reviewer context/identifier: Independent re-review after the Blocker-1 fix (commit e22e06e1), superseding the prior REJECT at head afa24d2a; report at .github/copilot-audit-state/reviews/WS03_ABSOLUTE_DEADLINE.md. Range 4e9f8077..e22e06e1 = 5 commits (4 slices + blocker fix), base is ancestor of head, 22 files; branch audit/ws03-absolute-deadline, HEAD == Head SHA, working tree clean (only the untracked, Git-excluded continuation pack present). Verdict: APPROVE. Backend one-deadline invariant (defects 1-6) fully wired on the real HTTP -> coordinator -> persistence -> reload path: prepareMutationCandidateContext at all five managed write entry points; coordinator previous-config resolution bound via config.NewCandidateContext(pctx, ...); preflightContext derives one bounded deadline from admission (serving reload_timeout only a fallback); applyCandidate reuses reqCtx.Deadline verbatim (reset block removed); RequestContext threads into pctx then nil'd before the finalizer; runPreflight checks pctx.Err() unconditionally after the gate. Timeout audit (defect 9): recordTimeoutAudit records a phase-named config.<operation> failure audit at all five routes (apply ID only when allocated; no secrets/config bytes). Console (defects 7-8) correct in source AND shipped in the regenerated embedded bundle (index-DTVTuTBo.js embeds timed_out_phase, errorKind==="timeout", preflight-timeout, "Nothing was persisted"). Blocker 1 (stale embedded bundle) independently reproduced clean at head: a fresh pnpm run build left the tree clean (byte-identical committed bundle, drift-guard filename set matches) and go build -tags console succeeds. Required blocker fixes: NONE remaining. Regressions: NONE. Recommended accepted SHA: e22e06e19b5c173455b57f2a6afe721c782d4299.
- Focused test evidence: go build -tags "console" ./cmd/jul -> exit 0. go test -count=1 ./internal/admin/... ./internal/config/... -> ok (admin 3.134s, config 2.251s). go test -count=1 -run 'TestManagedApply|TestCoordinator' ./internal/app/ -> ok (242.574s). Six WS03 core app tests PASS (TestCoordinatorReloadReusesAdmissionDeadline, TestCoordinatorBoundDeadlineGovernsPreflightOverServingTimeout, TestCoordinatorAbortsBeforePersistWhenDeadlineExpiredDuringGates, TestManagedApplyEnforcesSingleReloadTimeoutBudget, TestManagedApplyClientCancelBeforePersistLeavesDiskUntouched, TestManagedApplyClientCancelAfterPersistStillTerminalizes). Three WS03 admin HTTP tests PASS (TestManagedWriteRoutesRecordTimeoutAudit five-route 504 + audit, deadline-binding, deadline-omitted). Test-evidence quality HIGH: tests drive real production paths (s.routes().ServeHTTP admission; real ConfigApplyCoordinator.ApplyRaw); cancellation interleavings are barrier-driven (entered/persisted/proceed/stopReload channels), not sleeps; the single-budget test uses time.After only to simulate consumed preflight work and asserts termination near the absolute deadline (~200-300 ms), not preflight+budget (~365 ms); disk state asserted in every cancellation test; no test weakened or deleted.
- Race evidence: UNVERIFIED locally. go test -race genuinely unavailable in this environment: CGO_ENABLED=0 and no C toolchain (-race requires cgo). Deferral is HONEST (a real environment limitation, not a masked failure). Deferred to the exact-SHA CI race matrix.
- Console evidence: internal/admin/ui suite green - pnpm --dir internal/admin/ui run typecheck (clean), run lint (clean), run test (37 files / 454 tests pass). Defects 7-8 shipped: the regenerated git-tracked embedded bundle internal/admin/assets/dist/assets/index-DTVTuTBo.js embeds timed_out_phase, errorKind==="timeout", the preflight-timeout outcome, and the "Nothing was persisted" copy. Drift guard reproduced clean at head: a fresh pnpm --dir internal/admin/ui run build left git status --short with no tracked-file change (byte-identical committed bundle), so the CI drift-guard filename set matches.
- Commands unavailable and deferred to CI: (1) go test -race across internal/app, internal/admin, internal/config at the exact accepted SHA e22e06e19b5c173455b57f2a6afe721c782d4299 (CI race matrix); (2) full multi-OS GitHub Actions CI including the Console drift guard (source profile) at the exact accepted SHA. The drift guard was reproduced locally (clean); exact-SHA CI evidence remains required for final audit closure.
- Non-blocking follow-ups: (1) internal/app/preflight.go:257 still calls config.NewCandidate(prev) (context.Background) in the LiveSnapshot == nil fallback rebind check; not taken in production (LiveSnapshot is set; prev already resolved), no slow-secret-provider exposure - consider binding to the preflight context in a future pass. (2) The single-budget and cancellation vertical tests are coordinator-level rather than full-HTTP; adequate given the separate HTTP admission-binding coverage, but a full-HTTP single-budget test would strengthen the evidence.
- Next workstream branch: audit/ws04-console-exact-id
- Next expected parent SHA: this WS03 acceptance commit SHA (recorded in the excluded continuity state after commit; not embeddable in the committed entry)

### Workstream acceptance record - WS04_CONSOLE_EXACT_ID

- Workstream: WS04_CONSOLE_EXACT_ID - Exact-ID Console completion and finalization advisories
- Branch: audit/ws04-console-exact-id
- Base SHA: 0b36689d5dda5b3c978272921cf576bf51891c1e
- Final reviewed SHA: 4c3333e822bc9823d760fd6843cb922f54feb807
- Independent reviewer verdict: APPROVE
- Reviewer context/identifier: Independent re-review after the blocker-fix commit 4c3333e8, superseding the prior REJECT at head 0e538186; report at .github/copilot-audit-state/reviews/WS04_CONSOLE_EXACT_ID.md. Range 0b36689d..4c3333e8 = 5 commits (S01-S04 + blocker fix), base is ancestor of head, branch audit/ws04-console-exact-id, HEAD == Head SHA, working tree clean (only the untracked, Git-excluded continuation pack present). Verdict: APPROVE. The blocker-fix commit closes all four prior blockers: ConfigPanel and HistoryPanel now consume the single shared useManagedApplyRecord hook as their only exact-ID poll path (both old divergent hand-rolled poll loops, attempt-count constants, and per-panel grace/expiry refs removed; grep confirms zero occurrences of the old config-apply-record / rollback-apply-record query keys). Polling is deadline-bounded in production via computeEffectiveDeadlineMs (ledger deadline +5 s margin, or a 60 s compatibility ceiling), not attempt counts; both panels share one read-after-write 404 grace (MISSING_GRACE_ATTEMPTS). New production-path tests drive the real panels through a past-deadline record (expiry) and a brief 404 (grace), asserting final banner/dialog state. The previously-good sub-fixes (discriminated ManagedApplyLookup, full-record retention, immediate-result ledger fetch, shared finalization advisory, no discarded provenance) remain intact. Required blocker fixes: NONE remaining. Regressions: NONE. Recommended accepted SHA: 4c3333e822bc9823d760fd6843cb922f54feb807.
- Focused test evidence: pnpm --dir internal/admin/ui run typecheck (clean); pnpm --dir internal/admin/ui run lint (clean); pnpm --dir internal/admin/ui run test (39 files, 494 tests pass). Test-evidence quality HIGH: the grace test (404 for the first two reads then a correlated terminal record) asserts the "Saved - final outcome pending" state persists, recordReads >= 3, and that "Final result still unavailable" never appears; the ConfigPanel deadline-expiry test drives expiry through the shared hook from a past absolute deadline (2000-01-01) and asserts the "Final result still unavailable" banner with no success claim; the HistoryPanel deadline-expiry test surfaces "final rollback result is still unavailable" and keeps the dialog open - all deterministic (no fake timers, no sleeps) because expiry is decided from the record deadline, not a wall-clock attempt count. The managed-apply-record.test.tsx hook/pure-function tests are now genuine production-path evidence because both panels consume the hook.
- Race evidence: UNVERIFIED / not applicable locally. go test -race remains unavailable in this environment (CGO_ENABLED=0, no C toolchain), but WS04 is a Console-only workstream with no Go source changed, so no Go race surface is introduced. Any Go race matrix is deferred to the exact-SHA CI.
- Console evidence: pnpm --dir internal/admin/ui run typecheck (clean), run lint (clean), run test (39 files / 494 tests pass). The regenerated git-tracked embedded bundle reproduced with no drift: a fresh pnpm --dir internal/admin/ui run build left git status --short with no tracked-file change (byte-identical committed bundle index-Clkbpio6.js, CodeEditor-BJniLeGR.js), so the CI drift-guard filename set matches. The fix commit touches only the two panels, the panel test file, the journal, and the regenerated dist/** bundle - no Go source and no other Console source.
- Commands unavailable and deferred to CI: (1) go test -race - not material to this Console-only workstream (no Go source changed) and unavailable locally (CGO disabled); (2) exact-SHA multi-OS GitHub Actions CI (Console drift guard + race matrix) at the exact accepted SHA 4c3333e822bc9823d760fd6843cb922f54feb807. The drift guard was reproduced locally (clean); exact-SHA CI evidence remains required for final audit closure.
- Non-blocking follow-ups: (1) the hook's optional currentInstanceID (boot-mismatch fast expiry) is still not supplied by either panel, since the Console exposes no current-instance/boot ID source; a persistently-missing record with no deadline degrades to the bounded 60 s compatibility ceiling instead of a faster restart-detected expiry - honestly documented, no fabricated data source, never converts a missing record into success. Wire it if/when the Console surfaces the serving instance ID. (2) The §5.11 HistoryPanel "expiry allows close but not repeat apply" full-flow test is now deterministically expressible via the shared hook's deadline logic; consider adding the repeat-apply-blocked assertion as a follow-up.
- Next workstream branch: audit/ws05-patch-candidate
- Next expected parent SHA: this WS04 acceptance commit SHA (recorded in the excluded continuity state after commit; not embeddable in the committed entry)

### Workstream acceptance record - WS05_PATCH_CANDIDATE

- Workstream: WS05_PATCH_CANDIDATE - Truthful structured-patch candidate presentation
- Branch: audit/ws05-patch-candidate
- Base SHA: b85369b8c5838b0a55608ed0c69eb50c85c32151
- Final reviewed SHA: e9232e1cce1cb3731ada7673d5c3291f0413d593
- Independent reviewer verdict: APPROVE
- Reviewer context/identifier: Independent workstream review at .github/copilot-audit-state/reviews/WS05_PATCH_CANDIDATE.md. Range b85369b8c5838b0a55608ed0c69eb50c85c32151..e9232e1cce1cb3731ada7673d5c3291f0413d593 = 3 commits (7833d809 Slice 01 candidate API client + honest preview; 4c97e5c5 Slice 02 ConfigPanel candidate state + truthful labels; e9232e1c Slice 03 test-only verification), base is ancestor of head, branch audit/ws05-patch-candidate, HEAD == Head SHA, working tree clean (only the untracked, Git-excluded continuation pack present). Verdict: APPROVE. The config source panel is now truthful: the Console no longer copies the always-undefined preview candidate (12 producers verified), fetches the real candidate only from the config:raw-gated POST /api/config/patch/candidate endpoint (server-side rbac.ConfigRaw confirmed in internal/admin/route_catalog.go), presents it read-only, and labels every source state honestly via a four-state sourceView (persisted-editable / candidate-readonly / persisted-baseline / diff-only) with "Live configuration" removed. Traced HTTP -> fetchPatchCandidate -> ConfigPanel query/effect -> source label + apply gate. A stale (409) candidate keeps the persisted baseline and blocks apply; a 403 degrades to diff-only without ever showing or requesting candidate bytes; a config:write-only principal never triggers a candidate request (candidateCalls === 0). The embedded bundle reproduces with no drift. Required blocker fixes: NONE. Regressions: NONE. Recommended accepted SHA: e9232e1cce1cb3731ada7673d5c3291f0413d593.
- Focused test evidence: pnpm --dir internal/admin/ui run typecheck (clean); pnpm --dir internal/admin/ui run lint (clean); pnpm --dir internal/admin/ui exec vitest run src/test/console-v2-write.test.tsx src/test/patch-config.test.ts (2 files, 53 tests pass); pnpm --dir internal/admin/ui run test (39 files, 504 tests pass, +10 vs the prior 494 baseline with no weakened assertions); go test -count=1 ./internal/admin/... (ok; validates the embedded bundle). Test-evidence quality HIGH: client tests drive the real fetchPatchCandidate/patchConfig/patchConfigBatch against a mocked fetch (the natural HTTP seam), asserting request URL, request body (ops + base_version), and typed-error mapping (403/409/400); panel tests render the real ConfigPanel and route real HTTP through a mocked fetch, asserting final observable state (source-view label, editor value, editor.readOnly, Apply enablement, pinned request body, and call counts: candidateCalls === 0 without config:raw, === 1 on the 403 path). Authorization honored via a real 403 on /api/config, not bypassed; all waits use waitFor on assertions (no sleep-based synchronization).
- Race evidence: UNVERIFIED / not applicable locally. go test -race remains unavailable in this environment (CGO_ENABLED=0, no C toolchain), but WS05 is a Console-only workstream with no Go source changed and no new concurrency (single-threaded React state), so no Go race surface is introduced. Any Go race matrix is deferred to the exact-SHA CI.
- Console evidence: pnpm --dir internal/admin/ui run typecheck (clean), run lint (clean), run test (39 files / 504 tests pass). The regenerated git-tracked embedded bundle reproduced with no drift: a fresh pnpm --dir internal/admin/ui run build left git status --short with no tracked-file change (byte-identical committed bundle index-BUAyNVum.js, CodeEditor-CTLZY3bn.js), so the CI drift-guard filename set matches. Slice 03 was intentionally test-only and left the bundle untouched (unchanged source <-> unchanged bundle; the admin drift guard still passes).
- Commands unavailable and deferred to CI: (1) go test -race - not material to this Console-only workstream (no Go source changed) and unavailable locally (CGO disabled, no C toolchain); (2) exact-SHA multi-OS GitHub Actions CI (Console drift guard + race matrix) at the exact accepted SHA e9232e1cce1cb3731ada7673d5c3291f0413d593. The drift guard was reproduced locally (clean); exact-SHA CI evidence remains required for final audit closure.
- Non-blocking follow-ups: (1) the generic candidateFailed path (a non-403/409 candidate error) blocks apply and shows a "Candidate unavailable" notice per section 6.8, but has no dedicated test (only 409 and 403 are covered); a small addition would round out failure coverage. (2) the 403-degradation test models a principal that can read /api/config yet is refused the candidate - a defense-in-depth scenario hard to reach in production since both are gated on rbac.ConfigRaw; it correctly exercises the diff-only degradation but is somewhat contrived (harmless).
- Next workstream branch: audit/ws06-history-observability
- Next expected parent SHA: this WS05 acceptance commit SHA (recorded in the excluded continuity state after commit; not embeddable in the committed entry)

### Workstream acceptance record - WS06_HISTORY_OBSERVABILITY

- Workstream: WS06_HISTORY_OBSERVABILITY - History provenance projection and bounded finalization/history observability
- Branch: audit/ws06-history-observability
- Base SHA: 22babc9972e9af7d0d8ebd7232f9745dd412c09d
- Final reviewed SHA: 970cfee1d6f1321d4fc674c85d12fe54972e816d
- Independent reviewer verdict: APPROVE
- Reviewer context/identifier: Independent workstream review at .github/copilot-audit-state/reviews/WS06_HISTORY_OBSERVABILITY.md. Range 22babc9972e9af7d0d8ebd7232f9745dd412c09d..970cfee1d6f1321d4fc674c85d12fe54972e816d = 4 bounded slice commits (dfc1282f Slice 01 safe history provenance projection + _N collision-suffix fix; 2a8ab911 Slice 02 bounded finalization/history metrics + restoration/finalization reporting; 0303b228 Slice 03 remove duplicate mutation-machine truth + document result-query permission contract; 970cfee1 Slice 04 test-only verification of the history/observability contracts), base is ancestor of head, branch audit/ws06-history-observability, HEAD == Head SHA, tracked working tree clean (only the untracked, Git-excluded continuation pack present). Verdict: APPROVE. All six §7.1 objectives are complete and production-wired: (1) history rows project low-cardinality provenance from each snapshot's sidecar with a per-row MetadataError degrade and no raw TOML/secret serialization; (2) parseHistoryID strips the real _N collision suffix guarded by isDecimalDigits + prefix re-parse; (3) bounded metrics jul_managed_apply_finalization_errors_total{component}, jul_managed_apply_terminal_registry_entries gauge, jul_managed_apply_terminal_lookup_total{result}, and existing jul_managed_apply_history_total{operation,result} — every label a fixed enum, no apply ID/actor/IP/path/version, with the frozen cardinality guard tightened not weakened; (4) restoration/pending/registry/callback_panic finalization failures reported through ReportManagedApplyError → structured log.Error + ObserveManagedApplyFinalizationError with the registry gauge kept in sync; (5) duplicate mutation-machine truth removed (single authoritative confirmAdminForOperation, redundant post-success ref resets removed, cancelOperation also resets postApplyPollAttemptsRef); (6) /api/config/applies/{id} = AnyPermissions{StatusRead, ConfigApply} documented in docs/specs/console-rbac.md matching the route. Embedded bundle regenerated from source (contains new "Metadata unavailable" string; index.html references present index-Cen6-DIT.js / index-DcvuO_7e.css). Required blocker fixes: NONE. Regressions: NONE. Recommended accepted SHA: 970cfee1d6f1321d4fc674c85d12fe54972e816d.
- Focused test evidence: go test -count=1 ./internal/observability/... ./internal/admin/... (ok); go test -count=1 -run 'TestReportManagedApplyError|TestManagedApplyFinalizer|TestManagedApplyGet|TestParseHistoryID|TestHistoryList' ./internal/app/ ./internal/admin/ (ok); go test -count=1 ./internal/app/... (ok; 426.739s); pnpm --dir internal/admin/ui run typecheck (clean); pnpm --dir internal/admin/ui run lint (clean, --max-warnings=0); pnpm --dir internal/admin/ui run test (40 files, 508 tests pass, +1 vs the prior 507 baseline with no weakened assertions). Test-evidence quality HIGH: new Go tests drive real production paths, not manual state seeding — config_apply_report_error_test.go drives the real ApplyRaw coordinator and forces the pending-registration failure (OnManagedApplyStarted error) and restoration failure (deterministic beforeRestore barrier corrupting the on-disk digest) with channel/barrier synchronization and no sleeps; managed_apply_finalizer_test.go TestManagedApplyFinalizerHistoryDisposition exercises the real Finalize terminal entry point for recorded/skipped/failed (the failed case forced cross-platform by pointing HistoryDir at a regular file so os.MkdirAll fails) and asserts via the public /metrics exposition; api_managed_apply_test.go lookup tests run through the full s.routes() stack asserting exactly one bounded observation per request including the nil-registry missing case; use-config-mutation-machine.test.tsx proves confirmAdminForOperation lands in reducer state and mirrors the ref and that start/cancel clear scoped state. No assertion was weakened or deleted; the tests-and-review slice is purely additive (test files only, 0 deletions).
- Race evidence: UNVERIFIED / deferred to CI. go test -race remains unavailable in this environment (CGO_ENABLED=0, no C toolchain). Residual risk low: the workstream adds no new unsynchronized shared state; the gauge/lookup/error paths reuse existing mutex-guarded registry/metrics primitives and the new Go tests reuse those same primitives. Reported unverified; the Go race matrix is deferred to the exact-SHA CI.
- Console evidence: pnpm --dir internal/admin/ui run typecheck (clean), run lint (clean, --max-warnings=0), run test (40 files / 508 tests pass). The git-tracked embedded bundle was regenerated from the changed source (Slices 01–03): the rebuilt index-Cen6-DIT.js contains the new "Metadata unavailable" provenance string proving regeneration rather than hand-editing, and index.html references index-Cen6-DIT.js / index-DcvuO_7e.css (both present). Slice 04 was intentionally test-only and left the bundle untouched.
- Commands unavailable and deferred to CI: (1) go test -race ./internal/admin/... ./internal/app/... - unavailable locally (CGO disabled, no C toolchain); (2) exact-SHA multi-OS GitHub Actions CI (Console asset-drift gate + race matrix) at the exact accepted SHA 970cfee1d6f1321d4fc674c85d12fe54972e816d; (3) full source→bundle drift rebuild (pnpm build) was not re-run at acceptance — bundle internal consistency and presence of new source strings were verified instead. Exact-SHA CI evidence remains required for final audit closure.
- Non-blocking follow-ups: (1) the component value set {restoration, pending, registry, callback_panic} and the omission of a forbidden lookup value deviate from the runbook's suggested enums — both documented and correct (they mirror the phases production actually emits; route-level requireAnyPermission returns 403 before the handler so forbidden is unreachable); no action required. (2) the metric namespace jul_managed_apply_* (vs. the runbook's suggested jul_config_apply_*) keeps a single finalization/history/registry/lookup family — a sound deviation; no action required.
- Next workstream branch: audit/ws07-docs-certification
- Next expected parent SHA: this WS06 acceptance commit SHA (recorded in the excluded continuity state after commit; not embeddable in the committed entry)

---

## Cline to GitHub Copilot handoff

- Recorded at: 2026-07-30
- Handoff agent: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; materializes the Cline-to-Copilot bridge only (no WS03 implementation).
- Tool transition: WS01 and WS02 were implemented and independently accepted under Cline; execution continues under GitHub Copilot from WS03 onward.

### Remote historical baseline

- Remote: `https://github.com/victornife/jul.git`
- Baseline branch `latest` / `origin/latest`: `50052f9d5377488a6160935008e9e028fd08eca2` (verified after `git fetch --prune origin`; no drift). It is an ancestor of the current HEAD.

### Verified cumulative workstream SHAs

- WS01_MANAGED_LEDGER
  - Base SHA: `e707c41587a335cc92b643068b1c46053eff2621`
  - Reviewed head SHA: `a1c7a3e11195477601b30a067e8a2ed0cf509b91`
  - Cumulative head SHA (branch `audit/ws01-managed-ledger`, incl. acceptance-record commit): `bb666a655a14d25b4a3eaeaee5b5d6234b26224e`
- WS02_FINALIZATION
  - Base SHA: `bb666a655a14d25b4a3eaeaee5b5d6234b26224e` (= WS01 cumulative head)
  - Reviewed head SHA: `10c432fafd86de5d33891beada59886e72baccc8` (range `bb666a65..10c432fa` = exactly 5 commits, S01–S05)
  - Cumulative head SHA (branch `audit/ws02-finalization`, incl. acceptance-record commit): `d343694efe660a3b5e86be94c6f9fdd34dcebd26`
- Lineage verified linear via `git merge-base --is-ancestor` (all exit 0): `50052f9d → e707c415 → a1c7a3e1 → bb666a65 → 10c432fa → d343694e`. WS01 head is an ancestor of WS02 head; HEAD contains exactly the accepted WS02 work plus its journal-only acceptance-record commit (`d343694e`), with no unknown commits (12 commits `baseline..HEAD`, all identified).

### Review evidence paths

- `docs/audit/AI-IMPLEMENTATION-JOURNAL.md` → "Workstream acceptance record - WS01_MANAGED_LEDGER" (independent verdict APPROVE, 0 blockers).
- `docs/audit/AI-IMPLEMENTATION-JOURNAL.md` → "Workstream acceptance record - WS02_FINALIZATION" (independent verdict APPROVE, 0 blockers).
- `.github/copilot-audit-state/BRIDGE-AUDIT.md` (bridge verdict PASS; excluded from Git — local operational evidence).

### Environment limitations

- `go test -race` UNVERIFIED locally: `CGO_ENABLED=0` and no C toolchain; `-race` fails with "-race requires cgo". Deferred to the CI race matrix at the exact accepted SHA.
- Exact-SHA CI certification (multi-OS GitHub Actions) not yet run; a program-level closure gate, not a bridge blocker.

### Next workstream

- WS03_ABSOLUTE_DEADLINE on branch `audit/ws03-absolute-deadline`, created from the accepted cumulative WS02 head `d343694efe660a3b5e86be94c6f9fdd34dcebd26` (never from `origin/latest`). This handoff commit is WS03's base. Next prompt: `/jul-audit-ws03-s01`.

---

## WS03_ABSOLUTE_DEADLINE / Slice 01 — handler timing and context-aware candidate preparation

- Parent SHA: `4e9f8077b613cb1f803afd8a1b21dcd9b828ccf6`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws03-absolute-deadline`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; single-slice implementation.
- Files changed:
  - `internal/admin/server.go`: added `ApplyRequestContext.RequestContext context.Context` (`json:"-"`); replaced `prepareMutationCandidate` with `prepareMutationCandidateContext` (bounds secret resolution under an operation context); added `(*Server).bindManagedApplyDeadline` (derives the single absolute deadline from the serving reload_timeout at admission) and `managedApplyPrePersistenceContext` (bounded/cancel-only pre-persistence context); wired both legacy handlers (`handleConfigRaw`, `handleConfigSettings`).
  - `internal/admin/api.go`: `applyRequestContext` now binds `StartedAt` and `RequestContext` at admission; `handleConfigApply` binds the deadline and resolves the candidate under the bounded context.
  - `internal/admin/patch_http.go`: `handleConfigPatchApply` binds the deadline and resolves under the bounded context.
  - `internal/admin/api_history.go`: `rollbackToSnapshot` binds the deadline and resolves under the bounded context.
  - `internal/app/config_apply.go`: `ApplyRaw` clears `ctx.RequestContext` alongside `Baseline`/`Candidate` so the async finalizer copy never retains the request-scoped context.
  - `internal/admin/managed_apply_deadline_test.go`: new focused HTTP-path tests.
- Production path verified: HTTP admission (`applyRequestContext`) → `bindManagedApplyDeadline` (serving reload_timeout, R15-01) → `managedApplyPrePersistenceContext` → `prepareMutationCandidateContext` → `config.NewCandidateContext` → coordinator `ApplyConfigRaw`/`ApplyConfig` (reqCtx carries StartedAt/Deadline/RequestContext/Candidate). Verified for all five managed mutation entry points: config apply, legacy raw, settings, structured-patch apply, and rollback.
- Behavior implemented: one absolute deadline is derived once at admission from the currently serving reload_timeout and carried on the request context; handler-side candidate secret resolution is bounded by that deadline and by client cancellation; the request context is never serialized and is cleared before any async audit/ledger copy.
- Tests added: `TestManagedApplyBindsAbsoluteDeadlineFromServingReloadTimeout` (HTTP integration — proves Deadline = StartedAt + serving reload_timeout with a divergent candidate reload_timeout, RequestContext carried, candidate resolved); `TestManagedApplyDeadlineOmittedWithoutServingTimeout` (HTTP integration — proves StartedAt bound but no deadline when serving reload_timeout is not positive).
- Commands run: `go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/...` (all ok); `go vet ./internal/admin/... ./internal/app/...` (ok); `gofmt -l` (clean); `git diff --check` (clean).
- Commands unavailable: `go test -race` — `CGO_ENABLED=0`, no C toolchain; deferred to the exact-SHA CI race matrix.
- Deviations: none — helper names, signatures, and wiring follow the runbook skeleton; `config.NewCandidateContext` already existed and was reused.
- Self-review findings: all three new helpers are wired at every call site (no unwired helper); no error swallowing (candidate errors propagate as before); no duplicate persistence-side truth (coordinator deadline derivation unchanged, Slice 02 scope); no test weakened; `RequestContext` is `json:"-"` and cleared in the async copy (no secret/context exposure); struct comment updated.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws03-s02`.

---

## WS03_ABSOLUTE_DEADLINE / Slice 02 — coordinator deadline propagation and gate checks

- Parent SHA: `e2db3a037fb31eeeb796d45bc0766d2b3d09652c`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws03-absolute-deadline`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; single-slice implementation.
- Files changed:
  - `internal/app/config_apply.go`:
    - `preflightContext` now takes `admin.ApplyRequestContext` and derives the bounded pre-persistence context from the ONE absolute deadline bound at admission — `context.WithDeadline(base, reqCtx.Deadline)` when a deadline is bound, else the serving `reload_timeout` fallback, else cancel-only. The base is the request context (client cancellation) with `c.BaseCtx`/`context.Background()` fallbacks (§4.7).
    - `ApplyRaw` no longer clears `ctx.RequestContext` early; it now derives `pctx` first, then clears `ctx.RequestContext = nil` immediately after so the request-scoped context is never retained past derivation or handed to terminal callbacks. Previous-config resolution moved below the derivation and now uses `config.NewCandidateContext(pctx, raw)` so a stalled previous-config secret provider is bounded (§4.7).
    - `applyStageRestart` staged-update base resolution switched from `config.NewCandidate` to `config.NewCandidateContext(pctx, raw)` for the same bound.
    - `applyCandidate` no longer restarts the transaction clock or grants a fresh full timeout: `transactionStarted` = `reqCtx.StartedAt` (fallback `time.Now().UTC()`); `deadline` = `reqCtx.Deadline`, and only when unbound falls back to `transactionStarted.Add(servingReloadTimeout)`. The original `deadline` is passed to `server.ReloadRequest` (§4.8). The synchronous wait uses an explicit `time.NewTimer(time.Until(deadline)+waitMargin)`; a zero deadline yields a nil wait channel (block until the finalizer delivers) instead of the previous accidental one-second collapse.
    - `runPreflight` now checks `pctx.Err()` after the gate returns even when the gate error is nil, attributing a deadline breach to the observed phase and aborting before persistence (§4.9).
  - `internal/app/config_apply_deadline_test.go`: new focused coordinator tests.
- Production path verified: HTTP admission binds `StartedAt`/`Deadline`/`RequestContext` (Slice 01) → coordinator `ApplyRaw` → `preflightContext(reqCtx, cfg)` derives one bounded `pctx` from that deadline → previous-config resolution and every preflight gate run under `pctx` → `runPreflight` aborts before persistence on any post-gate expiry → `applyCandidate` reuses the same `reqCtx.Deadline` for `server.ReloadRequest.Deadline` and the synchronous wait. The single admission deadline now governs preflight, persistence, and reload with no reset; a candidate's own `reload_timeout` never affects the transaction that submits it (R15-01).
- Behavior implemented: exactly one absolute deadline flows from admission through preflight and reload. Client cancellation propagates via the request context into pre-persistence work. A deadline breach detected after any gate (even one that returned success) aborts cleanly with disk untouched and no reload enqueued. An unbounded transaction (no serving timeout, no bound deadline) waits for the finalizer rather than timing out after one second.
- Tests added: `TestCoordinatorReloadReusesAdmissionDeadline` (package integration — asserts `ReloadRequest.Deadline` equals the admission deadline verbatim while the serving `reload_timeout` would have produced a different value, proving no reset); `TestCoordinatorBoundDeadlineGovernsPreflightOverServingTimeout` (package integration — a 40 ms bound deadline aborts a wedged handler build within the phase while the serving timeout is 10 s, proving `preflightContext` derives from `reqCtx.Deadline`); `TestCoordinatorAbortsBeforePersistWhenDeadlineExpiredDuringGates` (package integration/failure-injection — an already-expired deadline on the prepared-candidate path, where every gate returns nil, still aborts before persistence with disk untouched and no reload enqueued, proving the §4.9 post-gate expiry check; this test fails under the previous `err != nil && pctx.Err() != nil` guard).
- Commands run: `go test -count=1 ./internal/app/... ./internal/admin/... ./internal/config/...` (EXIT=0: app ok 306.198s, admin ok, config ok); `go test -count=1 -v -run TestCoordinator...` for the three new + two adjacent timeout tests (all PASS); `go vet ./internal/app/... ./internal/admin/...` (ok); `git diff --check` (clean).
- Commands unavailable: `go test -race` — `CGO_ENABLED=0`, no C toolchain (`-race requires cgo`); deferred to the exact-SHA CI race matrix. `gofmt -l` reports repo-wide entries because the Windows/OneDrive working tree is CRLF while gofmt normalizes to LF (untouched files are listed too); `gofmt -d` on the two changed files shows only line-ending differences, no formatting change, and `git diff --check` is clean.
- Deviations: (1) also bound `applyStageRestart`'s staged-update base resolution with `config.NewCandidateContext(pctx, raw)` — the runbook §4.7 names only the `ApplyRaw` previous-config path, but this is the same coordinator "previous-config resolution" defect (§4.2 defect 2) and shares `pctx`, so binding it preserves the single-deadline invariant on the stage path. (2) `reqCtx.RequestContext` is cleared right after `preflightContext` derivation (per §4.7 "before it is passed to terminal callbacks or retained") rather than at the top of `ApplyRaw`; Slice 01 cleared it early, which would have made the request context unavailable to the derivation.
- Self-review findings: no unwired helper — `preflightContext` is the sole derivation site and both `applyStageRestart` and `applyCandidate` consume `pctx`/`reqCtx.Deadline`; no error swallowing — validation errors still propagate, and a bounded-context expiry is surfaced as a phase-specific `TimedOutPhase` (504) rather than hidden; no duplicate truth — the reset-computing block in `applyCandidate` was removed so the deadline has a single origin (admission); a zero deadline is handled explicitly (nil wait channel) rather than collapsing to one second; no secret/context exposure — `RequestContext` remains `json:"-"` and is cleared post-derivation, never serialized or logged; no test weakened; comments updated to describe the single-deadline flow.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws03-s03`.

---

## WS03_ABSOLUTE_DEADLINE / Slice 03 — post-persistence obligations, Console timeout contract, and timeout audit

- Parent SHA: `b1b7b4fe52a26563bb768843488549db3f2c7308`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws03-absolute-deadline`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; single-slice implementation.
- Files changed:
  - `internal/admin/config_apply.go`: added `(*Server).recordTimeoutAudit` — the single per-handler helper that records a `config.<operation>` failure audit naming the timed-out preflight phase and, when the coordinator allocated one before the timeout, the apply ID. The detail is transaction metadata only (phase + apply ID); it never carries config bytes, secrets, or token material.
  - `internal/admin/api.go`: `handleConfigApply` records the timeout audit (via `reqCtx.Operation`) after the validation branch; the shared status mapping writes the 504.
  - `internal/admin/patch_http.go`: `handleConfigPatchApply` records the timeout audit after the validation branch.
  - `internal/admin/server.go`: `handleConfigRaw` and `handleConfigSettings` record the timeout audit in the non-OK result branch before writing the status.
  - `internal/admin/api_history.go`: `handleConfigRollback` records the timeout audit in the `!result.OK` branch (via `ApplyOperationRollback`, since `rollbackToSnapshot` builds its own `reqCtx`).
  - `internal/admin/config_apply_timeout_audit_test.go` (new): HTTP-integration table test across all five managed write routes.
  - `internal/admin/ui/src/api/client.ts`: added top-level `timed_out_phase` to `ConfigApplyResultBaseSchema`; added the `"timeout"` `ConfigApplyErrorKind`; `classifyApplyFailure` throws `ConfigApplyOutcomeError(..., "timeout", ...)` when a top-level `timed_out_phase` is present.
  - `internal/admin/ui/src/lib/applyOutcome.ts`: added the `"preflight-timeout"` `ApplyOutcomeKind`, the `preflightTimedOut` input, and the derive branch that renders the phase, "Nothing was persisted", and the raise-`global.reload_timeout` guidance without implying the candidate serves.
  - `internal/admin/ui/src/features/config/ConfigPanel.tsx`: routes `errorKind === "timeout"` to the preflight-timeout outcome, passing the top-level `timed_out_phase`.
  - `internal/admin/ui/src/test/client-write.test.ts`, `internal/admin/ui/src/test/apply-outcome.test.tsx`: new focused tests.
- Production path verified:
  - Step 7 (post-persistence obligations): verified — no code change required. `ApplyRaw` nils `ctx.RequestContext` (config_apply.go) immediately after `preflightContext` derivation (Slice 02), so `applyCandidate` never holds the browser-request context. The finalizer goroutine and synchronous wait use only `resultCh`, `c.mu`, `c.BaseCtx`, and the single admission-deadline timer, so a browser disconnect cannot abandon reload/restoration and no second deadline is created.
  - Steps 8/9: HTTP 504 with top-level `timed_out_phase` → `classifyApplyFailure` → `ConfigApplyOutcomeError("timeout")` → `ConfigPanel` `errorKind==="timeout"` branch → `deriveApplyOutcome({preflightTimedOut})` → `preflight-timeout` banner. Backend: coordinator `timedOutResult` → each handler → `recordTimeoutAudit` → `recordAudit` ring/sink, with the 504 from `configApplyResultStatus`. Proven for all five managed routes (apply, legacy raw, settings, structured-patch apply, rollback).
- Behavior implemented: every managed write route now records a phase-named failure audit when a pre-persistence timeout occurs (apply ID included only when one exists); the Console classifies the 504 distinctly and renders a truthful "configuration not changed — preflight timed out" outcome that never claims the candidate is serving.
- Tests added: `TestManagedWriteRoutesRecordTimeoutAudit` (HTTP integration — five routes: 504 + `config.<op>` failure audit naming the phase, with apply_id present or absent per coordinator allocation); `client-write.test.ts` "classifies a pre-persistence preflight timeout 504 as kind timeout" (Console client contract); `apply-outcome.test.tsx` two `preflight-timeout` cases (phase rendered; phase-absent; message never claims serving).
- Commands run: `go test -count=1 ./internal/admin/... ./internal/config/...` (ok); `go test -count=1 ./internal/app/...` (ok jul/internal/app 306.805s); `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui run test` (37 files, 454 tests pass); `git diff --check` (clean, only benign CRLF notices); `gofmt` on EOL-normalized copies of all changed Go files (clean).
- Commands unavailable: `go test -race` — `CGO_ENABLED=0`, no C toolchain (`-race requires cgo`); deferred to the exact-SHA CI race matrix. `gofmt -l` reports repo-wide entries because the Windows/OneDrive working tree is CRLF while gofmt normalizes to LF; EOL-normalized `gofmt -d` on the changed files is clean and `git diff --check` is clean.
- Deviations: (1) Step 9 enumerates "raw, patch, settings, and rollback" handlers but omits the primary `handleConfigApply`; the timeout audit was added there too so timeout-audit evidence is complete per defect 9 — the primary managed apply path must not be the single silent gap. (2) Introduced the shared `recordTimeoutAudit` helper (used at all five call sites) instead of inlining the runbook snippet five times — one formatting/`apply_id` policy, no duplicate truth. (3) Step 7 required no code change (already satisfied by Slices 01/02); it was verified against the production path and documented rather than re-implemented, to avoid introducing a redundant/second deadline.
- Self-review findings: `recordTimeoutAudit` is wired at all five call sites (no unwired helper) and `preflightTimedOut`/`preflight-timeout` are consumed end-to-end; no error swallowing (a timeout still returns the structured 504); no duplicate truth (top-level `timed_out_phase` is the sole pre-persistence timeout signal; `reload.timed_out_phase` remains the distinct post-persistence signal); no secret exposure (audit detail is phase + apply ID only; the `timed_out_phase` schema field is non-sensitive metadata); no weakened or deleted assertions; no generated assets touched (`internal/admin/assets/dist` untouched); comments explain the pre- vs post-persistence distinction.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws03-s04`.

---

## WS03_ABSOLUTE_DEADLINE / Slice 04 — vertical timeout and cancellation tests

- Parent SHA: `9a61cc03a2f6f6f9eb6aafd14b437b9855078931`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws03-absolute-deadline`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; single-slice implementation. Test-only slice — no production code changed.
- Files changed:
  - `internal/app/config_apply_single_budget_test.go` (new): three coordinator-level vertical tests proving the single-budget and cancellation invariants that Slices 01–03 implemented. Reuses the existing package test helpers (`validConfigRaw`, `testPreflight`, `servingSnapshot`, `mockStreamPreflighter`, `mustParse`, `PlannedRestartStore`) and the deterministic `waitMargin`/`SubmitReload` seams; no production helper or test helper was added or duplicated.
- Production path verified: HTTP admission binds `StartedAt`/`Deadline`/`RequestContext` (Slice 01) → coordinator `ApplyRaw` → `preflightContext` derives one bounded `pctx` (Slice 02) → preflight gates → `applyCandidate` reuses `reqCtx.Deadline` verbatim for `server.ReloadRequest.Deadline` and the synchronous wait (Slice 02), and the async finalizer owns reload/restoration on `c.BaseCtx` (Slice 03). The new tests drive the real `ConfigApplyCoordinator.ApplyRaw` end to end (not a hand-seeded ledger or a direct helper call), so they exercise that production path rather than restating it.
- Behavior proved:
  - Single absolute budget: preflight and the reload wait share ONE deadline. A slow-but-successful preflight consumes ~150ms of a 200ms serving budget; the reload never delivers within the budget; the transaction must return near the original deadline (~215ms), not preflight+budget (~365ms, the reset defect).
  - Client cancellation before persistence aborts pre-persistence work (phase attributed), writes nothing, and enqueues no reload — proving cancellation propagates through `reqCtx.RequestContext` into `pctx`.
  - Client cancellation after persistence cannot abandon the reload: because `applyCandidate` runs the finalizer on `c.BaseCtx` (the request context is nil'd after `preflightContext` derivation), the transaction still terminalizes to an `applied_live` result and keeps the persisted write.
- Tests added: `TestManagedApplyEnforcesSingleReloadTimeoutBudget` (package integration — single end-to-end reload-timeout budget; the centerpiece of the commit subject); `TestManagedApplyClientCancelBeforePersistLeavesDiskUntouched` (package integration/failure-injection — barrier-driven client cancel during preflight; disk untouched, no reload); `TestManagedApplyClientCancelAfterPersistStillTerminalizes` (package integration — barrier-driven client cancel after the reload is enqueued; reload still terminalizes on the process context).
- Commands run: `go test -count=1 -v -run <three tests> ./internal/app/` (all PASS); `go test -count=3 -run <three tests> ./internal/app/` (stable, no flakiness); `go test -count=1 ./internal/app/` (ok jul/internal/app 306.105s); `go test -count=1 ./internal/config/... ./internal/admin/...` (ok); `go vet ./internal/app/` (clean); `gofmt -d` on an EOL-normalized copy of the new file (clean); `git diff --check` (clean).
- Commands unavailable: `go test -race` — `CGO_ENABLED=0`, no C toolchain (`-race requires cgo`); deferred to the exact-SHA CI race matrix. `gofmt -l` reports repo-wide entries because the Windows/OneDrive working tree is CRLF while gofmt normalizes to LF; EOL-normalized `gofmt -d` on the new file is clean and `git diff --check` is clean. On Windows/OneDrive the test binary cleanup emits a benign `unlinkat ... app.test.exe: Access is denied` after a passing run (exit 1 from cleanup only); every `ok` package line above is a genuine pass.
- Deviations from the runbook §4.13 test list: (1) The single-budget serving timeout is scaled from the runbook's 150ms to 200ms with ~150ms preflight so the one-budget return (deadline-anchored, ~215ms) and the reset return (~365ms) are far enough apart to assert without wall-clock flakiness; the return is anchored to the absolute admission deadline, so the low side is stable and only scheduling jitter can push it up, hence the generous 300ms upper bound. The single-budget test uses `time.After(preflightSpend)` to *simulate consumed budget* (deliberate slow work), which is distinct from the prohibited "sleep to approximate a concurrency interleaving"; the actual goroutine interleavings in the cancellation tests are barrier-driven (`entered`/`persisted`/`proceed`/`stopReload` channels). (2) The §4.13 handler-level HTTP 504/`timed_out_phase` and Console phase-rendering evidence was already delivered by Slice 03 (`TestManagedWriteRoutesRecordTimeoutAudit` across five routes; the `preflight-timeout` Console outcome tests) and by Slice 01 (`managed_apply_deadline_test.go`); this slice adds the still-missing single-budget and cancellation vertical evidence rather than re-adding covered cases. The per-gate barrier preflight matrix (WASM/stream/listener/startup-resource/admin-policy) was left out of scope: the handler-build gate is already covered by `blockingPreflight` and per-gate barriers would balloon this test-only slice without proving a distinct single-budget invariant.
- Self-review findings: no production code changed, so no unwired helper, no error swallowing, and no duplicate truth introduced; the duplicate `mustParse` I initially drafted was removed in favor of the existing package helper; no existing test was weakened or deleted; no secrets/config bytes are logged (assertions read only `OK`/`Persisted`/`Reload`/`TimedOutPhase`); no generated assets touched; comments describe the one-budget-vs-reset discriminator and the barrier design.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-review-ws03`.

---

## WS03_ABSOLUTE_DEADLINE / Blocker fix — regenerate stale embedded Console bundle

- Parent SHA: `afa24d2a71e76adf418f0eab6c926620bcf0ce61`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws03-absolute-deadline`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Blocker Fixer mode; single bounded blocker-fix commit. No production Go or Console source changed.
- Review addressed: `.github/copilot-audit-state/reviews/WS03_ABSOLUTE_DEADLINE.md` — Blocker 1 (and only Blocker 1).
- Blocker 1 (stale embedded Console bundle): FIXED. Slice 03 changed `internal/admin/ui/src/api/client.ts`, `internal/admin/ui/src/lib/applyOutcome.ts`, and `internal/admin/ui/src/features/config/ConfigPanel.tsx` (defects 7 and 8), but the git-tracked, `-tags console`-embedded `internal/admin/assets/dist/**` was never regenerated, so a `go build -tags console` binary served the old bundle and the CI drift guard would fail at head. Ran the source gates (typecheck, lint, test) then `pnpm --dir internal/admin/ui run build`; the rebuild renamed `assets/index-Dy4JYU3L.js` → `index-DTVTuTBo.js` and `assets/CodeEditor-BLPwjrUU.js` → `CodeEditor-CKCqIv9n.js` (plus their `.map`s and `index.html`), exactly the divergence the review predicted. The new content-addressed `index-DTVTuTBo.js` now embeds the defect 7/8 source, so the shipped Console classifies the top-level `timed_out_phase` 504 as `"timeout"` and renders the `preflight-timeout` outcome.
- Production path verified: `go build -tags "console" ./cmd/jul` succeeds with the regenerated embedded assets. Reproduced the CI "drift guard (source profile only)" comparison locally: the committed asset filename set (excluding `*.map`) after this commit equals a fresh `pnpm run build` output set — clean. `git diff --check --cached` on the staged assets is clean.
- Files changed: `internal/admin/assets/dist/index.html`, `internal/admin/assets/dist/assets/index-DTVTuTBo.js`(+`.map`), `internal/admin/assets/dist/assets/CodeEditor-CKCqIv9n.js`(+`.map`) — replacing the stale `index-Dy4JYU3L.js`/`CodeEditor-BLPwjrUU.js` variants. Content-identical, line-ending-only churn in `index-BXqB6tqy.css`, `query-BdSLN-Ub.js`, and `vendor-DB0PqBAn.js` was normalized away by Git (no hash change, not committed). Generated bundle only — no source edited by hand.
- Tests added: none (regenerated-artifact blocker; the defect 7/8 behavior is already proven by the Slice 03 Console tests `client-write.test.ts` "classifies a pre-persistence preflight timeout 504 as kind timeout" and `apply-outcome.test.tsx` preflight-timeout cases, which run against the same source now embedded).
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui run test` (37 files, 454 tests pass); `pnpm --dir internal/admin/ui run build` (rebuilt bundle; new content hashes as above); `go build -tags "console" ./cmd/jul` (ok); local drift-guard reproduction (committed==built filename set, `*.map` ignored — clean); `git diff --check --cached` (clean).
- Commands unavailable: exact-SHA multi-OS CI not run locally (the drift-guard failure was reproduced and now shown clean locally instead); `go test -race` still `-race requires cgo` under `CGO_ENABLED=0`.
- Deviations: none — the fix is exactly the review's required remediation (rebuild source gates → build → commit regenerated assets → confirm console build + drift guard) and touches nothing outside `internal/admin/assets/dist`.
- Self-review findings: only Blocker 1 addressed; no non-blocking suggestions implemented; no source (Go/TS) changed; no manual edits to the generated bundle; no secrets/TOML/token material in the assets; the source gates were run before rebuilding per the audit instructions.
- Independent review status: pending (re-review of WS03).
- Reviewer blockers: Blocker 1 remains recorded until the re-review clears it.
- Blocker-fix SHA: recorded in the excluded continuity state.
- Accepted SHA: pending re-review and exact-SHA CI.
- Next execution file: `/jul-audit-review-ws03`.

---

## WS04_CONSOLE_EXACT_ID / Slice 01 — discriminated lookup contract and shared polling hook

- Parent SHA: `0b36689d5dda5b3c978272921cf576bf51891c1e`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws04-console-exact-id`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; single-slice implementation. Console-only slice — no Go source changed; the embedded Console bundle was regenerated because three Console source files changed.
- Files changed:
  - `internal/admin/ui/src/api/client.ts`: replaced the ambiguous `fetchManagedApply` return type `Promise<ManagedApplyRecord | null>` with the discriminated `Promise<ManagedApplyLookup>` (`{ kind: "record"; record } | { kind: "missing" }`). A 404 now returns `{ kind: "missing" }`; a 200/202 returns `{ kind: "record", record }`; a malformed ID (400) or other transport failure still throws `ApiError` (no error swallowing). Updated the doc comment to describe the discriminated result. The `ManagedApplyRecordSchema.deadline` and top-level `timed_out_phase` fields required by Step 1 already existed from WS03 and were intentionally not duplicated.
  - `internal/admin/ui/src/features/config/ConfigPanel.tsx`: the exact-ID poll query's `queryFn` now maps the discriminated lookup back to `record | null` (`lookup.kind === "record" ? lookup.record : null`), keeping every downstream terminal/expiry branch and its behavior unchanged for this slice. The panel's adoption of the shared hook (and removal of the duplicated poll-attempt reducer/ref state) is Slice 02's scope.
  - `internal/admin/ui/src/features/history/HistoryPanel.tsx`: the rollback poll query's `queryFn` maps the discriminated lookup to `record | null` the same way, preserving terminal-only resolution (a missing record is never mistaken for success).
  - `internal/admin/ui/src/lib/useManagedApplyRecord.ts` (new): the centralized exact-ID polling hook plus its pure, unit-tested decision functions — `parseApplyInstance`, `isBootComponentMismatched`, `computeEffectiveDeadlineMs`, `computePollDelayMs`, and `deriveManagedApplyStatus` — and the cadence/margin/grace/ceiling constants. The hook fetches immediately, stops on a terminal record, honours the ledger `deadline` (+5 s margin) or a 60 s compatibility ceiling when none exists, tolerates the first three 404s as a visibility grace period, marks a persistently-missing record `expired` only when the awaited ID's boot component provably differs from a supplied current instance, and exposes an explicit `retry`. It never converts a missing/expired/errored lookup into success.
  - `internal/admin/ui/src/test/managed-apply-record.test.tsx` (new): focused tests for the discriminated lookup contract, all five pure functions, and the hook wiring.
  - `internal/admin/assets/dist/**`: regenerated embedded Console bundle (`index-DTVTuTBo.js` → `index-4ao33rmA.js`, `CodeEditor-CKCqIv9n.js` → `CodeEditor-B_z7-VeZ.js`, plus their `.map`s and `index.html`) so the `-tags console` binary and the CI drift guard reflect the changed source. Generated only — not hand-edited.
- Production path verified: the exact-ID observation path is `saved_not_live` apply/rollback → panel poll query → `fetchManagedApply(id)` → `GET /api/config/applies/{id}` (no-store) → discriminated `ManagedApplyLookup` → panel terminal/expiry logic. Both existing panel call sites were re-traced and updated so a 404 still resolves to "keep waiting / never success". The new hook is a shared primitive that Slice 02 wires into ConfigPanel/HistoryPanel (per the runbook's slice split: Slice 02 stores the full terminal record and removes duplicated poll state, Slice 03 adds advisory/expiry rendering); its pure decision functions are the tested source of truth for cadence, deadline, grace, and the "never terminal from missing" invariant.
- Behavior implemented: absence of a managed-apply record is now an explicit `missing` variant rather than `null`, removing the risk of a `null` being coerced into a success branch; a single reusable, deadline-aware polling contract with unit-tested time logic is available for the panels to adopt.
- Tests added: in `managed-apply-record.test.tsx` — `fetchManagedApply — discriminated lookup` (4 cases: 200→record, 202→record, 404→missing, 400→throws; client contract/unit); `parseApplyInstance / isBootComponentMismatched` (boot-id extraction, legacy/malformed→null, mismatch only when both known and differ; unit); `computeEffectiveDeadlineMs` (record deadline preferred, fallback, ceiling, ceiling-anchored-to-now; unit); `computePollDelayMs` (fast/slow cadence boundary; unit); `deriveManagedApplyStatus` (idle/error/terminal/polling/expired/missing-grace including the never-terminal-from-missing invariant; unit); `useManagedApplyRecord — hook wiring` (idle when no id and no fetch; terminal record → status terminal; initial 404 → missing-grace and never terminal; React-Query renderHook integration).
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui run test` (38 files, 479 tests pass — +25 new); `pnpm --dir internal/admin/ui run build` (rebuilt bundle; new content hashes above); `go build -tags "console" ./cmd/jul` (ok); `git diff --check` (clean).
- Commands not run: Go package tests (`go test ./internal/admin/... ./internal/app/... ./internal/config/...`) and `go test -race` — no Go source changed in this Console-only slice; residual risk is limited to the regenerated embedded bundle, which is covered by the successful `-tags console` build and the local drift-guard reproduction (dist filename set equals the fresh build output). Exact-SHA multi-OS CI not run locally.
- Deviations from this plan: (1) Step 1's `deadline` and `timed_out_phase` schema fields already exist from WS03; they were not re-added to avoid duplicate truth. (2) The runbook allows optionally supplying the current process instance for boot-component expiry; the console does not yet expose a current instance ID, so `useManagedApplyRecord` takes `currentInstanceID` as an optional parameter and degrades to deadline-bounded polling when it is absent — no data source was fabricated, and expiry-by-restart remains available for the wiring slice to supply. (3) The two existing panel call sites map the discriminated lookup back to `record | null` rather than adopting the new hook in this slice; hook adoption and the reducer de-duplication are explicitly Slice 02's scope, so this slice keeps panel behavior byte-for-byte equivalent while delivering the contract and primitive.
- Self-review findings: `fetchManagedApply` still throws for non-404 failures (no error swallowing); the discriminated union removes ambiguous `null` without duplicating truth (the `deadline`/`timed_out_phase` fields were reused, not re-declared); the new hook is not an unwired defect but the runbook-sanctioned shared primitive for Slice 02, and it is exercised by renderHook tests plus exhaustive pure-function unit tests; no existing assertion was weakened or deleted (all prior panel integration tests still pass unchanged); no secrets/TOML/token material is logged or serialized (the hook reads only `id`, `state`, and `deadline`); the embedded bundle was regenerated via the build tool, never hand-edited; doc comments were updated to match the new return contract.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws04-s02`.

---

## WS04_CONSOLE_EXACT_ID / Slice 02 — retain full managed apply records

- Parent SHA: `8af1247909bfd0e9573dda6b415c0e67f5affee8`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws04-console-exact-id`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; single-slice implementation. Console-only slice — no Go source changed; the embedded Console bundle was regenerated because three Console source files changed. This entry also removed a stray `</content>` tool-wrapper artifact that Slice 01 accidentally committed as the journal's last line.
- Files changed:
  - `internal/admin/ui/src/features/config/configMutationMachine.ts`: Step 3. Extended `AppliedState` with `terminalRecord: ManagedApplyRecord | null` (the full exact-ID terminal ledger record, retained whole including finalization provenance rather than projected down to only the nested apply result). Replaced the `mergeTerminal` action (which carried only a `Partial<ApplyResult>`) with `mergeTerminalRecord` (which carries the full `ManagedApplyRecord`); its reducer guard now additionally requires `record.id === applyId` and `record.state === "terminal"`, so a pending, cross-operation, id-skewed, or unrelated record can never merge. Removed the dead duplicated poll-attempt reducer state (`pollAttempts` field, `incrementPollAttempts`/`resetPollAttempts` actions, and the `startOperation` reset) — production never dispatched those; the poll counter lives solely in the React binding's ref.
  - `internal/admin/ui/src/features/config/useConfigMutationMachine.ts`: exposed `terminalRecord` on `PanelAppliedState`, added the dedicated `mergeTerminalRecord(record)` binding (correlates against the current operation + apply id via `appliedRef` and dispatches the new action), and removed the now-unused functional-updater merge branch of `setAppliedState` (its only caller — the panel finalize effect — now calls `mergeTerminalRecord`). `postApplyPollAttemptsRef` is now seeded from `0` instead of the deleted reducer field, and is documented as the sole home of the poll counter.
  - `internal/admin/ui/src/features/config/ConfigPanel.tsx`: Step 4. Broadened the exact-ID ledger fetch from only saved-not-live (202) applies to *any* managed result carrying an `apply_id` (`managedApplyID = applied?.apply_id`), so an immediate terminal result (200/409/503) also performs at least one exact-ID fetch to retain finalization provenance. `pendingApplyID` (saved-not-live only) still gates the pending/expired banner, the Reset/Apply disabled state, and `pollingExpired`, so an immediate result's supplemental fetch never delays, downgrades, or "expires" the already-shown runtime banner. A short read-after-write 404 grace (`immediateRecordMissesRef`, bounded by the shared `MISSING_GRACE_ATTEMPTS`) is tolerated for the immediate case while the saved-not-live path keeps its stop-on-404 (gone-after-restart) semantics. The finalize effect now calls `mergeTerminalRecord(record)` (retaining the full record) and is idempotent via the new `terminalRecord !== null` guard; editor reconciliation side-effects remain gated on `pendingApplyID`.
  - `internal/admin/ui/src/test/config-mutation-machine.test.ts`: rewrote the terminal-merge test for `mergeTerminalRecord` (proving id-skew, pending-state, wrong-id, and stale-generation records are all dropped, and that the full record — including `history_snapshot_id`/`finalization_error` — is retained on merge); asserted `terminalRecord` is null for a fresh `applyResult`; removed the obsolete poll-attempt-budget test and the `pollAttempts` assertion for the deleted reducer state.
  - `internal/admin/ui/src/test/console-v2-write.test.tsx`: added an HTTP-integration test proving an immediate live apply (200, `applied_live`, top-level `apply_id`) fetches its exact ledger record through the read-after-write 404 grace to retain finalization provenance, while the "Applied and live" banner shows immediately and is never downgraded to "Saved — final outcome pending".
  - `internal/admin/assets/dist/**`: regenerated embedded Console bundle (`index-4ao33rmA.js` → `index-Cc1ic_MV.js`, `CodeEditor-B_z7-VeZ.js` → `CodeEditor-CqewpWTL.js`, plus their `.map`s and `index.html`) so the `-tags console` binary and CI drift guard reflect the changed source. Generated only — not hand-edited.
  - `docs/audit/AI-IMPLEMENTATION-JOURNAL.md`: this entry; also deleted the stray `</content>` last line committed by Slice 01.
- Production path verified: managed apply → `applyRaw/applyPatch/applyStage` mutation `onSuccess`/`onError` records the correlated result (`setAppliedState`) → panel derives `managedApplyID`/`pendingApplyID` from `applied.apply_id` and `reload.outcome` → the `["config-apply-record", managedApplyID]` query calls `fetchManagedApply(id)` → `GET /api/config/applies/{id}` → discriminated `ManagedApplyLookup` → the finalize effect merges a *terminal, id-matched* record via `mergeTerminalRecord` into the reducer, which stores the full record and merges its `result`. For saved-not-live the merge flips the pending banner to the terminal outcome and drives editor reconciliation; for an immediate terminal result the merge only retains provenance without touching the banner. The reducer merge is the single source of truth for the correlation invariant (generation + held apply id + record id + terminal state); the panel effect is idempotent so the merge cannot re-trigger itself.
- Behavior implemented: (1) the full exact-ID terminal ledger record is retained on the applied state, not a lossy projection of only the nested result, so finalization provenance (`history_snapshot_id`/`history_error`/`finalization_error`) survives for a later advisory render (Slice 03); (2) immediate terminal managed results now also reconcile against their exact ledger record (at least one fetch, with a bounded read-after-write grace) instead of never fetching it; (3) the reducer no longer keeps a duplicate poll-attempt counter.
- Tests added or changed: `configMutationReducer > mergeTerminalRecord retains only a matching-generation, matching-id terminal record` (unit; id-skew/pending/wrong-id/stale-gen all dropped, full record retained); `configMutationReducer > applyResult records only for the current generation` (unit; extended to assert `terminalRecord` starts null); `configMutationReducer > startOperation ...` (unit; dropped the deleted `pollAttempts` assertion); removed `configMutationReducer > poll-attempt budget increments and resets` (deleted dead state); `manages saved-not-live managed applies > fetches the exact ledger record for an immediate live apply to retain finalization provenance` (HTTP integration; immediate 200 + grace 404 + terminal record; banner stays "Applied and live").
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui run test` (38 files, 479 tests pass); `pnpm --dir internal/admin/ui run build` (rebuilt bundle; new content hashes above); `go build -tags "console" ./cmd/jul` (exit 0); `git add -A internal/admin` + `git diff --check --cached` (clean, only benign LF→CRLF normalization warnings on the two new generated bundles).
- Commands not run: Go package tests (`go test ./internal/admin/... ./internal/app/... ./internal/config/...`) and `go test -race` — no Go source changed in this Console-only slice; residual risk is limited to the regenerated embedded bundle, covered by the successful `-tags console` build and the local drift-guard reproduction (dist filename set equals the fresh build output). Exact-SHA multi-OS CI not run locally.
- Deviations from this plan: (1) The panel did NOT fully adopt the Slice 01 `useManagedApplyRecord` hook; the runbook's Step 3/Step 4 require storing the full record, de-duplicating the reducer poll counter, and fetching immediate results — all achieved by keeping the panel's existing bounded poll query and removing the dead reducer counter. Wholesale hook adoption plus the advisory/expiry rendering it enables is Slice 03's scope; doing it here would have intersected Slice 03 and risked the 479 existing panel assertions. The shared `MISSING_GRACE_ATTEMPTS` constant is imported from the hook module to avoid a duplicate magic number. (2) The `mergeTerminalRecord` binding is a dedicated method rather than an overloaded `setAppliedState` functional updater, because the full record cannot be reconstructed from the panel-facing `PanelAppliedState` alone; `SetApplied` was narrowed to `Omit<PanelAppliedState,"terminalRecord"> | null` since the functional-updater form is no longer used.
- Self-review findings: no unwired helpers (`mergeTerminalRecord` is called by the finalize effect; `terminalRecord` is stored by the reducer and surfaced through `PanelAppliedState` for Slice 03; the imported `MISSING_GRACE_ATTEMPTS` is used in the immediate-grace branch); no error swallowing (`fetchManagedApply` still throws for non-404 transport failures; a 404 remains a `missing`/null that is never a success); no duplicate truth (the dead reducer poll counter was removed so only the ref remains; the terminal record is stored once and its result merged rather than duplicated); tests were not weakened — the removed poll-attempt test covered deleted dead state and the merge test was strengthened (more rejection cases + provenance retention), and all 479 pass; no secret/TOML/token exposure (only `id`/`state`/`result`/provenance strings are read, never logged); the embedded bundle was regenerated via the build tool, never hand-edited; stale comments were removed (the duplicated AC-09 poll comment and the obsolete `appliedRef`/`setAppliedState` doc lines) and the reducer's top-of-file field list updated.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws04-s03`.

---

## WS04_CONSOLE_EXACT_ID / Slice 03 — render finalization and expiry truthfully

- Parent SHA: `74d217c0ff6dc4596ecdb4b217f95983e3f01b0b`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws04-console-exact-id`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; single-slice implementation. Console-only slice — no Go source changed; the embedded Console bundle was regenerated because two Console source files and one test file changed alongside two new source/test files.
- Files changed:
  - `internal/admin/ui/src/lib/finalizationAdvisory.ts` (new): Step 5. The single shared `deriveFinalizationAdvisory(record)` deriving AC-14 finalization provenance (`history_error` / `finalization_error`) into a `FinalizationAdvisory` (`{ title, messages, historySnapshotID? }`), or `null` when the record is absent or its sidecar finalized cleanly. It is advisory only — never a failure or readiness signal. Replaces HistoryPanel's local, string-only helper so both panels share one wording and one invariant.
  - `internal/admin/ui/src/features/history/HistoryPanel.tsx`: removed the local `finalizationAdvisory` string helper and now imports the shared `deriveFinalizationAdvisory` + `FinalizationAdvisory` type. `finalizationNotice` state is now the advisory object; both render sites (the combined degraded+finalization banner and the standalone finalization banner) render the advisory title, each message on its own line, and the history snapshot id when present.
  - `internal/admin/ui/src/features/config/ConfigPanel.tsx`: Steps 5–7. (5) Derives the finalization advisory from the retained full terminal record (`appliedState.terminalRecord`, Slice 02) and renders it as a non-blocking `role="status"` banner beside — never replacing — the apply outcome. (7) Adds a deadline-aware "Finalization expected by <local timestamp>." hint while a saved-not-live apply is still resolving (deadline read from the pending 202 ledger record), plus a local-time formatter that falls back to the raw ISO string on an unparseable value. (6) Rewrites the expired banner to say the result "was not available by its deadline. This is not a success confirmation.", show the exact apply id, and offer "Retry status" (re-checks the exact-ID record without minting a new operation, preserving the original result), "Open Runtime Overview", and (when `config:raw` is held) "Reload authoritative configuration" (re-seeds the editor from disk without clearing the result). The original result is never cleared and "Applied and live" is never shown for an expired saved-not-live apply.
  - `internal/admin/ui/src/test/finalization-advisory.test.ts` (new): unit tests for `deriveFinalizationAdvisory` — null/clean → null, history-only, finalization-only, both + history-snapshot passthrough, and the advisory-only (ok=true, degraded sidecar is never an error) invariant. Records are built through `ManagedApplyRecordSchema.parse` so the `ApplyResult` transform shape is exact.
  - `internal/admin/ui/src/test/console-v2-write.test.tsx`: added a ConfigPanel HTTP-integration test proving the deadline hint shows while pending (record carries `deadline`) and the shared finalization advisory + history snapshot render once the terminal record merges, with the hint then gone; strengthened the existing "404 after restart" expiry test to assert the new not-a-success-confirmation copy, the exact apply id, and the "Retry status" action; added a HistoryPanel HTTP-integration test proving a fully-live rollback whose history sidecar degraded renders the shared advisory (title + message + snapshot), never as a rollback failure.
  - `internal/admin/assets/dist/**`: regenerated embedded Console bundle (`index-Cc1ic_MV.js` → `index-BUNQJY0g.js`, `CodeEditor-CqewpWTL.js` → `CodeEditor-CJ13moDL.js`, plus their `.map`s and `index.html`) so the `-tags console` binary and the CI drift guard reflect the changed source. Generated only — not hand-edited.
- Production path verified: managed apply/rollback → panel poll query `["config-apply-record"|"rollback-apply-record", id]` → `fetchManagedApply(id)` → `GET /api/config/applies/{id}` → discriminated `ManagedApplyLookup` → the finalize effect merges a terminal, id-matched record (ConfigPanel via `mergeTerminalRecord`; HistoryPanel via `setFinalizationNotice`). ConfigPanel now reads `appliedState.terminalRecord` and the pending record's `deadline` from that same query for the advisory and the deadline hint; the expired banner reads `pendingApplyID` and re-drives the same query on "Retry status". HistoryPanel derives the advisory from the exact terminal record inside its existing terminal-only resolution effect. Both consume the one shared `deriveFinalizationAdvisory`.
- Behavior implemented: (5) one shared, advisory-only finalization derivation rendered in both panels; (6) ConfigPanel's missing/expired state is now explicit and truthful — exact id, "not a success confirmation", Retry status / Open Runtime Overview / Reload authoritative configuration, original result preserved, never "Applied and live"; (7) a deadline-aware "Finalization expected by …" hint while pending and a past-deadline message after expiry.
- Tests added or changed: `deriveFinalizationAdvisory` (6 unit cases: null, clean, history-only, finalization-only, both+snapshot, advisory-only-never-error); `manages saved-not-live managed applies > shows the deadline hint while pending and a finalization advisory once terminal` (HTTP integration); `manages saved-not-live managed applies > does not claim success when the exact apply-id record is gone (404 after restart)` (strengthened: not-a-success-confirmation copy + exact id + Retry status); `HistoryPanel rollback flow > renders the shared finalization advisory after a live rollback whose sidecar degraded` (HTTP integration).
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui run test` (39 files, 487 tests pass — +8 new); `pnpm --dir internal/admin/ui run build` (rebuilt bundle; new content hashes above); `go build -tags "console" ./cmd/jul` (exit 0); `git diff --check` (clean).
- Commands not run: Go package tests (`go test ./internal/admin/... ./internal/app/... ./internal/config/...`) and `go test -race` — no Go source changed in this Console-only slice; residual risk is limited to the regenerated embedded bundle, covered by the successful `-tags console` build and the local drift-guard reproduction (dist filename set equals the fresh build output). Exact-SHA multi-OS CI not run locally.
- Deviations from this plan: (1) `FinalizationAdvisory.title`/`messages` follow the runbook's Step 5 skeleton wording ("Configuration applied, but recovery/audit finalization degraded"; "Configuration history degraded: …"; "Managed apply finalization degraded: …") rather than HistoryPanel's prior sentence form, unifying the two panels on one voice; no existing test asserted the old wording, so nothing was weakened. (2) "Reload authoritative configuration" is wired to the existing `refreshEditorAfterFailure(appliedState.operationID)` (a read-only disk re-seed) rather than a new operation, because the runbook requires the original result NOT be cleared, and `startOperation` clears it — this keeps the result banner intact while reloading the editor, and is gated on `config:raw` (`!rawForbidden`) as the "if safe" condition.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws04-s04`.

---

## WS04_CONSOLE_EXACT_ID / Slice 04 — verify exact-id completion across panels

- Parent SHA: `a539d0f27891d0266283f8bcbd27abecd7e86a95`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws04-console-exact-id`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; single-slice implementation. Test-only slice — no production source, no Go source, and no embedded Console bundle changed; only two existing Console test files were extended, so no asset rebuild and no asset-drift gate were required.
- Files changed:
  - `internal/admin/ui/src/test/managed-apply-record.test.tsx`: added two `useManagedApplyRecord` hook-wiring integration tests closing the remaining §5.11 hook matrix gaps — (a) "polls through an initial 404 then a pending record to the terminal record" proves the read-after-write 404 grace, the pending poll, and terminalization are one continuous lifecycle that resolves to terminal without ever being reported missing/expired; (b) "manual retry resets terminal lookup state and re-fetches the record" proves an explicit `retry()` after a completed terminal lookup clears the terminal state and re-observes the exact id (terminal → retry → pending → polling), so a stale terminal is never left reported. Imported `act` from `@testing-library/react` to wrap the synchronous `retry()` state transition.
  - `internal/admin/ui/src/test/console-v2-write.test.tsx`: (a) strengthened the existing immediate-live ConfigPanel test ("fetches the exact ledger record for an immediate live apply …") to additionally assert the retained finalization provenance renders as the shared advisory beside the "Applied and live" banner (title, the finalization-degraded message, and the `snap-42` history snapshot id) — closing the §5.11 "immediate 200 → ledger advisory history failure" ConfigPanel gap; (b) added a ConfigPanel test "renders the preflight timeout phase and claims nothing was persisted" proving a 504 pre-persistence preflight timeout (`timed_out_phase`) renders the preflight-timeout banner naming the exact phase, states nothing was persisted, never claims "Applied and live"/"Saved — final outcome pending", and never polls an exact-id ledger record (the `/api/config/applies/` handler throws if hit) — closing the §5.11 "timeout phase rendered" gap; (c) added a HistoryPanel test "closes the dialog on a delayed degraded rollback that first polled pending" proving a degraded (committed, `ok=true`, `applied_degraded`) outcome that only becomes terminal after a pending poll keeps the dialog open while pending and then closes it with the persistent degraded banner — closing the §5.11 "delayed degraded rollback closes dialog" gap; (d) added a HistoryPanel test "keeps polling through missing (404) grace and never closes until the terminal record" proving a not-yet-visible exact-id record (404s) keeps the dialog open with the pending notice (never a success) and only the correlated terminal record resolves and closes it — closing the §5.11 "missing result keeps polling through grace" gap.
  - `docs/audit/AI-IMPLEMENTATION-JOURNAL.md`: this entry.
- Production path verified: no production code changed. The added tests exercise the same real paths hardened in Slices 01–03: (1) the hook path `useManagedApplyRecord` → `fetchManagedApply(id)` → `GET /api/config/applies/{id}` → discriminated `ManagedApplyLookup` → `deriveManagedApplyStatus`, including the `retry()` reset of `startedAtRef`/`missingCountRef` + `query.refetch()`; (2) the ConfigPanel managed-apply path `applyRaw.onError` classifying a 504 as `ConfigApplyOutcomeError{kind:"timeout"}` → `appliedState.errorKind==="timeout"` → `deriveApplyOutcome({preflightTimedOut, timedOutPhase})` → the preflight-timeout banner, with no ledger poll issued because nothing was persisted; (3) the ConfigPanel immediate-live path retaining `terminalRecord` via `mergeTerminalRecord` and rendering `deriveFinalizationAdvisory`; (4) the HistoryPanel rollback path `["rollback-apply-record", pendingApplyID]` → `fetchManagedApply` → terminal-only resolution effect closing the dialog for a delayed degraded outcome and holding it open through 404 grace.
- Behavior implemented: none new — this slice adds/hardens focused Console tests that verify exact-id completion across both panels (§5.11 matrix) and the never-claim-success-from-missing/expired/pending/unrelated invariant; it changes no production behavior.
- Tests added or changed: `useManagedApplyRecord — hook wiring > polls through an initial 404 then a pending record to the terminal record` (hook integration); `useManagedApplyRecord — hook wiring > manual retry resets terminal lookup state and re-fetches the record` (hook integration); `ConfigPanel apply flow > fetches the exact ledger record for an immediate live apply to retain finalization provenance` (HTTP integration; strengthened with advisory-render assertions); `ConfigPanel apply flow > renders the preflight timeout phase and claims nothing was persisted` (HTTP integration); `HistoryPanel rollback flow > closes the dialog on a delayed degraded rollback that first polled pending` (HTTP integration); `HistoryPanel rollback flow > keeps polling through missing (404) grace and never closes until the terminal record` (HTTP integration).
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean, after replacing an `async` `act` wrapper with a synchronous one to satisfy `@typescript-eslint/require-await`); `pnpm --dir internal/admin/ui run test` (39 files, 492 tests pass — +5 net: two new hook tests, one new ConfigPanel test, two new HistoryPanel tests, and one existing ConfigPanel test strengthened in place); `git diff --check` (clean after trimming one stray EOF blank line).
- Commands not run: `pnpm --dir internal/admin/ui run build` and the asset-drift gate — no production source changed, so the embedded bundle is intentionally untouched; rebuilding would be a no-op or introduce spurious hash churn. Go package tests (`go test ./internal/admin/... ./internal/app/... ./internal/config/...`) and `go test -race` — no Go source and no embedded asset changed, so there is nothing new for them to cover; residual risk is nil for the Go build. `go test -race` remains unavailable locally (CGO disabled). Exact-SHA multi-OS CI not run locally.
- Deviations from this plan: (1) The §5.11 HistoryPanel "expiry allows close but not repeat apply" case is NOT added as a full HTTP-integration test: HistoryPanel's rollback poll expiry is a fixed 20-attempt count at a 1500 ms cadence (~30 s), which cannot be driven to expiry deterministically within a fast, sleep-free integration test, and the audit rules forbid sleeps-as-synchronization and slow/flaky waits. The expiry decision itself is already covered at the pure-function level by the hook's `deriveManagedApplyStatus` "expired" cases (missing past deadline, pending past deadline, boot-mismatch after grace) in `managed-apply-record.test.tsx`, and the expiry UI affordance (cancel enabled, confirm disabled while pending) is a straightforward prop-driven render of the existing `ProvisionalRollback`; adding a real-time expiry integration test would require fake timers threaded through React Query's interval, which is out of scope for this test-hardening slice and would not exercise additional production wiring. Recorded as a residual concern below. (2) I strengthened the existing immediate-live ConfigPanel test in place (adding advisory-render assertions) rather than adding a duplicate test, to avoid duplicate truth; no assertion was weakened or removed.
- Self-review findings: no unwired helpers (test-only slice; every new test drives a real production path end-to-end); no error swallowing (the preflight-timeout test asserts no ledger poll occurs and the missing-grace test asserts a 404 never becomes success); no duplicate truth (the immediate-advisory coverage extends the existing test rather than cloning it); no weakened tests — all 492 pass and the only pre-existing test touched gained assertions; no secret/TOML/token exposure (tests read only `id`/`state`/`deadline`/`history_snapshot_id`/`timed_out_phase`/error strings); no generated-asset change (the embedded bundle is deliberately untouched because no source changed); comments accurately describe each test's invariant.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Remaining concerns: the §5.11 HistoryPanel "expiry allows close but not repeat apply" full-flow integration test is intentionally omitted (see deviation 1); the shared `useManagedApplyRecord` hook remains a unit-covered helper that the panels do not yet consume as their live poll path (a Slice 02/03 recorded deviation), so its integration tests prove the hook contract but not panel adoption — both are items for the independent WS04 review.
- Next execution file: `/jul-audit-review-ws04`.

---

## WS04_CONSOLE_EXACT_ID / Blocker fix 01 — wire both panels onto the shared exact-ID poll hook

- Parent SHA: `0e5381864e424c1d47ed7f9484a37ff055d6304a`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws04-console-exact-id`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Blocker Fixer mode; bounded blocker-fix session addressing the numbered blockers in `.github/copilot-audit-state/reviews/WS04_CONSOLE_EXACT_ID.md` (verdict REJECT). Console-only change — no Go source changed; the embedded Console bundle was regenerated because two Console source files and one test file changed.
- Review blockers addressed:
  - **Blocker 1 (hook unwired — both panels ran divergent hand-rolled poll loops).** Replaced ConfigPanel's `["config-apply-record", managedApplyID]` `useQuery`/`refetchInterval` and HistoryPanel's `["rollback-apply-record", pendingApplyID]` `useQuery`/`refetchInterval` with a single `useManagedApplyRecord(id)` call each. Both panels now observe the exact-ID lifecycle through the one shared hook; the two bespoke poll queries and their query keys are gone (`grep` for both keys now returns zero matches).
  - **Blocker 2 (attempt-count-bounded, not deadline-driven).** Polling now stops on the hook's deadline logic (`computeEffectiveDeadlineMs`: ledger `deadline` +5 s margin, or a 60 s compatibility ceiling when none exists), not fixed attempt counts. Deleted `POLL_MAX_ATTEMPTS`, `POLL_FAST_POLLS`, `POLL_SLOW_INTERVAL_MS` from ConfigPanel and the `1500` ms / `20`-attempt literals plus `rollbackPollAttemptsRef` from HistoryPanel. `pollingExpired` is now `pendingApplyID … && managedApply.status === "expired"` (ConfigPanel) and `pendingApplyID !== null && rollbackManaged.status === "expired"` (HistoryPanel).
  - **Blocker 3 (ConfigPanel stopped on first 404 for the saved-not-live path; inconsistent 404 semantics).** Both panels now share the hook's read-after-write 404 grace (`MISSING_GRACE_ATTEMPTS`) before any expiry, so a transient saved-not-live 404 no longer trips the "not a success" banner. ConfigPanel's `pendingApplyID !== undefined → return false` immediate stop and its `immediateRecordMissesRef` grace clock were removed; the 404 semantics are now identical across panels because they are the same hook.
  - **Blocker 4 (no production-path test exercised a shipped poll).** Added/adjusted `console-v2-write.test.tsx` cases that drive the real panels through the shared hook: a saved-not-live short 404 that finalizes without expiring (grace), a ConfigPanel deadline-driven expiry (past-deadline pending record → "Final result still unavailable"), and a HistoryPanel deadline-driven expiry (past-deadline pending record → "final rollback result is still unavailable"). The pre-existing `managed-apply-record.test.tsx` renderHook tests are now genuine production-path evidence because both panels consume the hook.
- Files changed:
  - `internal/admin/ui/src/features/config/ConfigPanel.tsx`: consume `useManagedApplyRecord(managedApplyID)`; derive `applyRecord`/`pollingExpired`/`pendingDeadline` and the finalize-merge effect from the hook's `record`/`status`; "Retry status" now calls `managedApply.retry()`. Removed the bespoke poll query, the attempt-count constants, `immediateRecordMissesRef`, and the `fetchManagedApply`/`MISSING_GRACE_ATTEMPTS` imports (kept `POLL_FAST_INTERVAL_MS`/`LEGACY_OVERVIEW_MAX_POLLS` + `postApplyPollAttemptsRef` for the unrelated best-effort legacy stream-overview poll).
  - `internal/admin/ui/src/features/history/HistoryPanel.tsx`: consume `useManagedApplyRecord(pendingApplyID ?? undefined)`; derive `rollbackRecord`/`pollingExpired` from the hook and drive the terminal-only resolution effect from `rollbackRecord`. Removed the bespoke poll query, `rollbackPollAttemptsRef`, the `pollingExpired` state (now derived), and the `fetchManagedApply` import.
  - `internal/admin/ui/src/test/console-v2-write.test.tsx`: split the old pure-404 "gone after restart" ConfigPanel test into a grace-tolerance test (`rl_grace`: 404×2 → terminal, never expires) and a deadline-expiry test (`rl_8`: pending past-deadline → expiry banner, exact id, Retry status, never "Applied and live"); made the deadline-hint test's pending-record `deadline` far-future (`2099-…`) so the hint (not a wall-clock-dependent past-deadline expiry) is the observed pending state; added a HistoryPanel deadline-expiry test (`rl_rbexp`).
  - `internal/admin/assets/dist/**`: regenerated embedded Console bundle (`index-BUNQJY0g.js` → `index-Clkbpio6.js`, `CodeEditor-CJ13moDL.js` → `CodeEditor-BJniLeGR.js`, plus their `.map`s and `index.html`) so the `-tags console` binary and the CI drift guard reflect the changed source. Generated only — not hand-edited.
- Production path verified: saved-not-live apply/rollback → `managedApplyID`/`pendingApplyID` → `useManagedApplyRecord(id)` → `fetchManagedApply(id)` → `GET /api/config/applies/{id}` (no-store) → discriminated `ManagedApplyLookup` → `deriveManagedApplyStatus` (idle/polling/missing-grace/terminal/expired) → panel terminal-merge / pending / deadline-hint / expiry rendering. Both panels are now the hook's only two production consumers; there is no remaining second poll path.
- Behavior implemented: one shared, deadline-bounded, grace-tolerant exact-ID poll drives both panels; a saved-not-live 404 tolerates the grace window before expiry; expiry is decided by the ledger deadline (or the 60 s ceiling), never a fixed attempt count; the "never success from missing/expired" invariant is preserved end-to-end.
- Tests added or changed: `manages saved-not-live managed applies > tolerates a brief read-after-write 404 on the saved-not-live path then finalizes` (HTTP integration; grace, never expires); `manages saved-not-live managed applies > expires on the ledger deadline (not an attempt count) and never claims success` (HTTP integration; past-deadline pending → expiry, exact id, Retry status); `manages saved-not-live managed applies > shows the deadline hint while pending …` (adjusted: far-future deadline for determinism); `HistoryPanel rollback flow > expires a pending rollback on its ledger deadline without claiming success` (HTTP integration).
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui run test` (39 files, 494 tests pass — +2 net: the pure-404 test split into two, plus one new HistoryPanel expiry test); `pnpm --dir internal/admin/ui run build` (rebuilt bundle; new content hashes above); `git status --short` (only the changed sources + regenerated dist + the excluded continuation pack).
- Commands not run: Go package tests and `go test -race` — no Go source changed in this Console-only fix; residual risk is limited to the regenerated embedded bundle, covered by the source suite and the local drift-guard reproduction. `go test -race` remains unavailable locally (CGO disabled). Exact-SHA multi-OS CI not run locally.
- Deviations: the hook's optional `currentInstanceID` (boot-mismatch expiry) is still not supplied by either panel — the console exposes no current-instance/boot ID source, so a persistently-missing record with no deadline degrades to the bounded 60 s compatibility ceiling rather than a faster restart-detected expiry. No data source was fabricated; this is the same non-blocking limitation the reviewer noted, unchanged by this fix.
- Self-review findings: no unwired helpers remain — `useManagedApplyRecord` is now each panel's sole poll path; no duplicate truth — the panel-side cadence/grace/expiry constants and refs were deleted, leaving the hook as the single source; no error swallowing — `fetchManagedApply` still throws for non-404 failures and a 404 is never a success; no weakened tests — the only pre-existing test changed (the deadline hint) gained a deterministic far-future deadline and the pure-404 test was split into two stronger production-path tests, and all 494 pass; no secret/TOML/token exposure; the embedded bundle was regenerated via the build tool, never hand-edited.
- Independent review status: pending (re-review of the same workstream).
- Reviewer blockers: to be re-evaluated by the independent WS04 re-review; not self-cleared.
- Blocker-fix SHA: recorded in the excluded continuity state after commit.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-review-ws04`.

---

## WS05_PATCH_CANDIDATE / Slice 01 — candidate API client and honest preview contract

- Parent SHA: `b85369b8c5838b0a55608ed0c69eb50c85c32151`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws05-patch-candidate`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; first slice of WS05 (truthful structured-patch candidate presentation). Console-only change — no Go source changed; the embedded Console bundle was regenerated because Console source changed.
- Files changed:
  - `internal/admin/ui/src/api/client.ts`: added `PatchCandidateSchema` / `PatchCandidate` and `fetchPatchCandidate(ops, baseVersion)` calling the config:raw-gated `POST /api/config/patch/candidate`, preserving the server error contract as typed errors — 403 (no config:raw) and other transport failures → `ApiError` with status; 409 (stale preview) → `ConfigConflictError` carrying `current_version`; 400 (invalid op) → `ConfigRejectedError` with the issues. Deprecated the never-populated `candidate` field on `PatchResultSchema` (retained only for wire back-compat; no consumer) and corrected the misleading "returns the candidate TOML" doc comments on the op-description block, `patchConfig`, and `patchConfigBatch` to state the preview returns the diff only.
  - Removed the phantom `candidate: res.candidate` from the patch handoff in every structured-editor producer (the preview never returns a candidate): `internal/admin/ui/src/lib/useRunPatch.ts`, `features/apps/AppDetail.tsx`, `features/apps/AppEditor.tsx`, `features/routes/AuthEditor.tsx`, `features/routes/LocationRateLimitEditor.tsx`, `features/routes/RouteDetail.tsx` (both `runPatch` sites), `features/routes/RouteEditor.tsx`, `features/routes/RouteEditors.tsx`, `features/routes/TranscodeQuickEdit.tsx`, `features/security/LocationWAFEditor.tsx`, `features/traffic-controls/TrafficControlEditor.tsx`, `features/wizard/WizardPanel.tsx`.
  - `internal/admin/ui/src/test/patch-config.test.ts`: added a `fetchPatchCandidate` describe block (success + 403/409/400 typed-error cases).
  - `internal/admin/assets/dist/**`: regenerated embedded Console bundle (`index-Clkbpio6.js` → `index-DsxCn_tl.js`, `CodeEditor-BJniLeGR.js` → `CodeEditor-C3g8un52.js`, plus their `.map`s and `index.html`) so the `-tags console` binary and the CI drift guard reflect the changed source. Generated only — not hand-edited.
- Production path verified:
  1. Preview path (runtime unchanged): structured editor → `patchConfig` / `patchConfigBatch` → `POST /api/config/patch(/preview)` → `handleConfigPatch(Preview)` → `previewPatchOps` in `internal/admin/patch_http.go`, which marshals `{ok, summary, diff, base_version}` and intentionally omits the candidate TOML (N-01) → `setPendingDraft({kind:"patch", ops, baseVersion, previewDiff})` → `ConfigPanel`. Because the server never sent `candidate`, dropping `candidate: res.candidate` is a no-op at runtime; it removes a false dependency, not data.
  2. New candidate path (added here, consumed by slice 02): `fetchPatchCandidate(ops, baseVersion)` → `POST /api/config/patch/candidate`, gated on `rbac.ConfigRaw` in `internal/admin/route_catalog.go` → `handleConfigPatchCandidate`, which honors `base_version` (409 `conflictResponse` on mismatch), applies the ops to a clone, and returns `{ok:true, candidate, base_version}`; a failed op is a 400 `validationErrorResponse`. The client maps each of these to the matching typed error.
- Behavior implemented: the preview contract no longer claims to carry a candidate; no producer copies the (always-undefined) preview candidate into the handoff; a new config:raw-gated `fetchPatchCandidate` returns the true candidate TOML for the source view, to be consumed by `ConfigPanel` in slice 02.
- Tests added or changed: `fetchPatchCandidate > posts ops + base_version and returns the candidate TOML on success` (unit; asserts endpoint, request body, and parsed `candidate`/`base_version`); `fetchPatchCandidate > throws ApiError(403) when the token lacks config:raw` (unit/failure injection); `fetchPatchCandidate > throws ConfigConflictError with current_version on a 409 stale preview` (unit/failure injection); `fetchPatchCandidate > throws ConfigRejectedError on a 400 invalid operation` (unit/failure injection).
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui run test` (39 files, 498 tests pass — +4 `fetchPatchCandidate`); `pnpm --dir internal/admin/ui run build` (rebuilt bundle; new content hashes above); `go build ./...` (pass); `go test -count=1 ./internal/admin/...` (pass — validates the embedded bundle); `git diff --check` (clean apart from the benign working-tree LF→CRLF notice).
- Commands not run: `go test ./internal/app/... ./internal/config/...` — no Go source changed and those packages do not embed the Console bundle, so there is nothing new for them to cover (residual risk nil). `go test -race` — unavailable locally (CGO disabled, no C toolchain). Exact-SHA multi-OS CI — not run locally.
- Deviations from this plan: (1) Kept `candidate` on `PatchResultSchema` deprecated rather than deleting it; the runbook's Step 2 shows a candidate-free schema but explicitly permits "leave it deprecated but do not depend on it", and removal would be a wire/compat change beyond this bounded slice — no consumer depends on it now. (2) The runbook's Step 2 rewrites only `useRunPatch`, but 12 producers (that hook plus 11 inline editors) copied the phantom `res.candidate`; per the code-block-interpretation rule ("trace every production call site") the same honest change was applied to every site so the "stop depending on the preview candidate" invariant holds uniformly. Producer tests assert only `kind`/`ops`, so none were weakened. (3) `fetchPatchCandidate` is added in this slice but its production consumer (ConfigPanel source-view relabeling) is slice 02 per the runbook split; it is covered by focused client tests here and is not an accidental unwired helper. (4) Kept the optional `PendingDraft.candidate` field as the slot slice 02 will populate from `fetchPatchCandidate`; no producer writes it now, and `ConfigPanel` still reads it and falls back to the current raw config exactly as before, so there is no runtime regression.
- Self-review findings: no error swallowing (`fetchPatchCandidate` rethrows typed errors; non-typed failures become `ApiError` carrying the status); no secret/TOML/token exposure (the endpoint is config:raw-gated server-side; the client forwards only `ops` + `base_version` and parses `candidate`/`base_version` — no bearer/token/actor/source metadata is handled); no duplicate truth (the phantom preview-candidate dependency was removed from all 12 producers and the deprecated schema field has no consumer); doc comments were corrected to match the real server contract; no stale comments introduced; the embedded bundle was regenerated via `pnpm run build`, never hand-edited; the audit closure report was not touched.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws05-s02`.


---

## WS05_PATCH_CANDIDATE / Slice 02 — ConfigPanel candidate state and truthful labels

- Parent SHA: `7833d8099b5f4afb9c69bb0b94cb87d8435af214`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws05-patch-candidate`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; second slice of WS05 (truthful structured-patch candidate presentation). Console-only change — no Go source changed; the embedded Console bundle was regenerated because Console source changed.
- Files changed:
  - `internal/admin/ui/src/features/config/ConfigPanel.tsx`: consume the Slice-01 `fetchPatchCandidate` client. Added a `["config-patch-candidate", baseVersion, ops]` `useQuery`, enabled only for a config:raw operator in patch mode whose draft has no candidate yet (`isPatchMode && !rawForbidden && patchDraft.candidate === undefined`), `retry:false`, `staleTime:Infinity`. On success an effect pins the true candidate into the draft (`setPatchDraft({...patchDraft, candidate})` + `setDraft(candidate)`), flipping the source view from persisted-baseline to candidate-readonly. Replaced the three-state `sourceView` (`live`/`candidate`/`diff-only`) with the four truthful states `persisted-editable` / `candidate-readonly` / `persisted-baseline` / `diff-only` and their honest labels; "Live configuration" is gone. A 403 on the candidate fetch (`candidateForbidden`) degrades to diff-only; a 409 (`candidateStale`) renders a non-success "Preview is stale" notice; any other failure (`candidateFailed`) renders a "Candidate unavailable" notice. Apply is gated behind `candidatePending` so a config:raw operator cannot apply while the candidate is loading/stale/failed, while a diff-only operator (no config:raw, or a candidate 403) still applies because the server recomputes the ops atomically. Updated the source-label header styling/comment to key on `persisted-editable` and corrected the seed comment to say the editor shows the persisted baseline until the candidate is fetched.
  - `internal/admin/ui/src/test/console-v2-write.test.tsx`: retargeted the three existing source-view assertions to the new tone names/labels (`persisted-editable` "persisted configuration" — asserts it never says "live configuration"; `candidate-readonly` "proposed candidate"; `diff-only` unchanged text but the negative assertion now checks `candidate-readonly`). Added two production-path tests: one where a config:raw operator with a candidate-less patch handoff drives the real `POST /api/config/patch/candidate` fetch (asserts the request pins `ops`+`base_version`, the editor flips to the candidate text, the label becomes `candidate-readonly`, and Apply becomes enabled), and one where the candidate fetch returns 409 (asserts the `persisted-baseline` label, the "Preview is stale" notice, no `candidate-readonly`, and Apply stays disabled).
  - `internal/admin/assets/dist/**`: regenerated embedded Console bundle (`index-DsxCn_tl.js` -> `index-BUAyNVum.js`, `CodeEditor-C3g8un52.js` -> `CodeEditor-CTLZY3bn.js`, plus their `.map`s and `index.html`) so the `-tags console` binary and the CI drift guard reflect the changed source. Generated only — not hand-edited.
- Production path verified: structured editor handoff (ops + previewDiff + baseVersion, no candidate after Slice 01) -> `ConfigPanel` seeds the persisted baseline and labels it `persisted-baseline` -> `config-patch-candidate` query -> `fetchPatchCandidate(ops, baseVersion)` -> config:raw-gated `POST /api/config/patch/candidate` (`handleConfigPatchCandidate`, base_version pinned -> 409 on mismatch, 400 on a failed op, 403 without config:raw) -> on 2xx the candidate is pinned into the draft and the label flips to `candidate-readonly`; on 409 the label stays `persisted-baseline` with a "Preview is stale" notice and Apply is disabled; on 403 the panel presents diff-only. Apply patch remains gated by `candidatePending` for config:raw operators.
- Behavior implemented: the source view now names exactly what it shows (persisted-editable, candidate-readonly, persisted-baseline, or diff-only) and never mislabels persisted bytes as the proposed candidate or claims "live"; the protected candidate is fetched and displayed read-only; a stale preview is surfaced honestly and blocks apply; a config:raw operator cannot apply before the candidate resolves, while a diff-only operator still applies via atomic server-side recomputation.
- Tests added or changed: `ConfigPanel apply flow > requires explicit confirmation before applying` (adjusted: asserts `persisted-editable`/"persisted configuration" and no "live configuration", Console integration); `ConfigPanel patch flow > applies a structured patch atomically ...` (adjusted tone names to `candidate-readonly`, Console integration); `ConfigPanel patch flow > allows structured patch review when raw config is forbidden` (adjusted negative assertion to `candidate-readonly`, Console integration); `fetches the protected candidate and shows it read-only for a config:raw operator` (new; Console integration driving the real candidate fetch + apply enablement); `keeps the persisted baseline and blocks apply when the candidate preview is stale` (new; Console integration / failure injection on a 409).
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui run test` (39 files, 500 tests pass — +2 candidate tests); `pnpm --dir internal/admin/ui run build` (rebuilt bundle; new content hashes above); `go build ./...` (pass); `go test -count=1 ./internal/admin/...` (pass — validates the embedded bundle); `git diff --check` (clean).
- Commands not run: `go test ./internal/app/... ./internal/config/...` — no Go source changed and those packages do not embed the Console bundle, so there is nothing new for them to cover (residual risk nil). `go test -race` — unavailable locally (CGO disabled, no C toolchain). Exact-SHA multi-OS CI — not run locally.
- Deviations from this plan: (1) The runbook example query key uses a `stablePatchOpsKey(...)` helper that does not exist in the codebase; I passed `patchDraft.ops` directly instead, relying on React Query's deterministic key hashing (stable-stringify with sorted keys), which is the repository-native equivalent and avoids an unused one-off helper. (2) The runbook's Step 3 shows `patchDraft !== null` guards in the `enabled`/`candidatePending` expressions; TypeScript's const-alias narrowing already proves `patchDraft` non-null after `isPatchMode`, so the explicit checks were flagged by `@typescript-eslint/no-unnecessary-condition` and removed — behavior is identical. (3) In addition to the two truthful patch-mode candidate/diff states the runbook names, the raw-editable state was relabeled from "Live configuration" to "Persisted configuration — editable" per §6.7's explicit instruction not to say "Live configuration" unless serving and persisted versions are proven equal.
- Self-review findings: no unwired helpers — the Slice-01 `fetchPatchCandidate` is now wired to its real production consumer and both new tests drive it end-to-end; no error swallowing — the candidate query is `retry:false` and each error class (403/409/other) maps to a distinct, non-success UI state, and Apply is disabled while unresolved; no duplicate truth — the four-state `sourceView` is the single source label and the persisted-baseline state shows the actual on-disk bytes rather than a fabricated candidate; no weakened tests — the three retargeted assertions gained stronger negative checks (no "live configuration", no `candidate-readonly` where inappropriate) and all 500 pass; no secret/TOML/token exposure — the client forwards only `ops`+`base_version` and the endpoint is config:raw-gated server-side; comments were corrected to match the new behavior; the embedded bundle was regenerated via `pnpm run build`, never hand-edited; the audit closure report was not touched.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws05-s03`.


---

## WS05_PATCH_CANDIDATE / Slice 03 — verify protected candidate presentation

- Parent SHA: `4c97e5c55c9d039db3dec785bd07c81c75797528`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws05-patch-candidate`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; third and final implementation slice of WS05 (truthful structured-patch candidate presentation) — the tests-and-hardening slice. Test-only change: no production Console or Go source changed, so the embedded Console bundle (`internal/admin/assets/dist`) was intentionally NOT rebuilt (unchanged bundle ↔ unchanged source; the admin drift guard `go test ./internal/admin/...` still passes).
- Files changed:
  - `internal/admin/ui/src/test/patch-config.test.ts`: added two client-boundary tests proving the honest preview contract (N-01) — `patchConfig` and a new `patchConfigBatch` describe each assert that when the preview response omits `candidate`, the parsed result exposes `candidate === undefined` (never a fabricated value) while still returning the diff/`base_version`. Imported `patchConfigBatch` for the batch-preview case.
  - `internal/admin/ui/src/test/console-v2-write.test.tsx`: enhanced the shared `CodeEditor` mock to reflect the real `readOnly` prop as the textarea's `readonly` attribute (no existing test regressed; raw-mode editors remain editable). Strengthened two existing production-path tests and added two more so every §6.9 required test exists:
    - "allows structured patch review when raw config is forbidden": added a `candidateCalls` counter branch and a final `expect(candidateCalls).toBe(0)` — proves the panel never requests the protected candidate endpoint without config:raw.
    - "fetches the protected candidate and shows it read-only for a config:raw operator": added `expect(editor.readOnly).toBe(true)` — proves the candidate is presented read-only, not just present.
    - new "degrades to diff-only when the protected candidate endpoint returns 403": the candidate endpoint is consulted (counter asserts exactly one call) but returns 403; the source view collapses to `diff-only`, `candidate-readonly` is absent, and the diff-only operator still applies via `/api/config/patch/apply` (server-side atomic recomputation) — no client candidate is trusted for apply.
    - new "keeps the persisted/runtime distinction clear during a managed staged restart": with a `managed_staged` pending-restart status, the raw editor source label stays `persisted-editable`/"persisted configuration" and never says "live configuration", and the staged-restart banner surfaces the staged (`v-staged`) vs serving (`v-serving`) versions distinctly so on-disk and runtime are never conflated.
- Production path verified: these tests drive the real `ConfigPanel` and the real `internal/admin/ui/src/api/client.ts` functions (`patchConfig`, `patchConfigBatch`, `fetchPatchCandidate` via the panel's `config-patch-candidate` query, `applyPatchBatch`, `fetchPendingRestart`) against a mocked `fetch` router — the same HTTP contracts the server exposes (`/api/config`, `/api/config/patch`, `/api/config/patch/preview`, `/api/config/patch/candidate`, `/api/config/patch/apply`, `/api/config/pending-restart`). The candidate-fetch query enablement (`isPatchMode && !rawForbidden && patchDraft.candidate === undefined`), the four-state `sourceView` label logic, the `candidatePending` apply gate, and the `PendingRestartBanner` staged/serving projection are all exercised end-to-end; no helper is called directly in place of production wiring.
- Behavior implemented: no production behavior changed — this slice adds/strengthens the WS05 §6.9 test evidence that the source presentation is truthful: preview responses carry no candidate; the protected candidate is fetched read-only only for a config:raw operator; a 409 keeps the persisted baseline and blocks apply; a 403 degrades to diff-only; the candidate endpoint is never requested without config:raw; persisted bytes are never labelled as the candidate; raw mode says persisted (not "live"); and a staged restart keeps the persisted/runtime distinction explicit.
- Tests added or changed: `patchConfig > resolves with an undefined candidate when the preview omits it (the production case)` (new; unit/client boundary); `patchConfigBatch > previews a batch and resolves with an undefined candidate when omitted` (new; unit/client boundary); `ConfigPanel apply flow > allows structured patch review when raw config is forbidden` (strengthened: candidate endpoint never called; Console integration); `ConfigPanel apply flow > fetches the protected candidate and shows it read-only for a config:raw operator` (strengthened: `editor.readOnly` asserted; Console integration); `ConfigPanel apply flow > degrades to diff-only when the protected candidate endpoint returns 403` (new; Console integration / failure injection); `ConfigPanel apply flow > keeps the persisted/runtime distinction clear during a managed staged restart` (new; Console integration).
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui exec vitest run src/test/patch-config.test.ts src/test/console-v2-write.test.tsx` (2 files, 53 tests pass); `pnpm --dir internal/admin/ui run test` (39 files, 504 tests pass — +4 net); `go test -count=1 ./internal/admin/...` (pass — validates the unchanged embedded bundle); `git diff --check` (clean; only the two test files modified).
- Commands not run: `pnpm --dir internal/admin/ui run build` — no production Console source changed, so the embedded bundle must stay byte-identical (rebuilding a test-only slice would be an unwarranted asset change); residual risk nil. `go test ./internal/app/... ./internal/config/...` — no Go source changed and those packages do not embed the Console bundle; residual risk nil. `go test -race` — unavailable locally (CGO disabled, no C toolchain). Exact-SHA multi-OS CI — not run locally.
- Deviations from this plan: none. The runbook §6.9 lists ten required tests; six already existed from Slices 01–02 (raw-authorized candidate fetch, source label "proposed candidate", 409 blocks apply, persisted baseline never labelled candidate, raw mode says persisted-not-live, and the diff-only-without-raw review). This slice added the four missing ones (preview omits candidate ×2, 403→diff-only, staged-restart distinction) and hardened two existing ones (read-only assertion; candidate-never-called-without-raw) rather than duplicating coverage.
- Self-review findings: no weakened tests — every change adds an assertion or a new case; the shared `CodeEditor` mock enhancement is additive (all 504 tests pass, raw-mode editors remain editable). No unwired helpers — all new tests exercise the real panel/client production path. No error swallowing — the 403/409 paths assert distinct non-success UI states and apply gating. No secret/TOML/token exposure — the tests assert the candidate is withheld from previews and never requested without config:raw. No stale comments. No generated-asset change — the embedded bundle was deliberately left untouched because no source changed, and the admin drift guard confirms it. The audit closure report was not touched.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-review-ws05`.

---

## WS06_HISTORY_OBSERVABILITY / Slice 01 — history provenance projection and collision timestamps

- Parent SHA: `22babc9972e9af7d0d8ebd7232f9745dd412c09d`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws06-history-observability`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; first slice of WS06 (history, observability, permission, and contract hardening). Production Console source changed, so the embedded bundle (`internal/admin/assets/dist`) was rebuilt via the repository-authoritative `pnpm run build` and committed with the source.
- Files changed:
  - `internal/admin/history.go`: extended `historyEntry` with the optional, redacted provenance projection (`apply_id`, `operation`, `mode`, `outcome`, `actor`, `reason`, `previous_version`, `candidate_version`) plus a per-entry `metadata_error`; `list()` now reads each snapshot's sidecar via the existing `getMeta` and projects it onto the entry, degrading a single malformed sidecar to `MetadataError` instead of failing the whole listing. Fixed `parseHistoryID` to strip the actual `_N` collision suffix appended by `snapshot()` (the old parser stripped a non-existent `-N`), guarded by `isDecimalDigits` and a re-parse of the prefix so an unexpected underscore is never mistaken for a counter.
  - `internal/admin/history_test.go`: added `TestParseHistoryID` (plain / `_1` / `_10` / non-digit suffix / digit-suffix-with-bad-prefix / unparseable / empty) and `TestHistoryListParsesCollisionSuffix` (a `_1`-suffixed snapshot keeps its parsed time and sorts newest-first).
  - `internal/admin/history_metadata_test.go`: added `TestHistoryListProjectsMetadata` (well-formed sidecar projects onto the listing entry, no `MetadataError`) and `TestHistoryListMalformedSidecarDegradesOneRow` (a corrupt sidecar degrades only its own row and never fails `list()` or contaminates a sibling).
  - `internal/admin/ui/src/api/client.ts`: added the matching optional provenance fields (`apply_id`, `operation`, `mode`, `outcome`, `actor`, `reason`, `previous_version`, `candidate_version`, `metadata_error`) to `HistoryEntrySchema`; all optional so older raw-only snapshots still parse.
  - `internal/admin/ui/src/features/history/HistoryPanel.tsx`: added a new "Origin" column projecting the redacted provenance — a `ReasonBadge` (recovery snapshot is a prominent danger badge, pre-apply is muted), the operation, outcome, actor, and apply id; a malformed sidecar shows an inline "Metadata unavailable" notice; a recovery row is visually distinct via a danger row tint.
  - `internal/admin/ui/src/test/console-v2-write.test.tsx`: added `HistoryPanel provenance projection > renders the redacted provenance and marks a recovery snapshot distinctly` — a recovery, a pre-apply, and a malformed-sidecar entry render together; the recovery row carries the distinct tint and the malformed row degrades without failing the listing.
  - `internal/admin/assets/dist/*`: regenerated embedded Console bundle (new hashes `index-Cwzi-po6.js`, `index-DcvuO_7e.css`, `CodeEditor-Dz1P3_tb.js`); `index.html` references resolve; the admin drift guard (`go test ./internal/admin/...`) reproduces cleanly.
- Production path verified: `GET /api/history` and `GET /api/config/history` → `handleHistoryList` → `history.list()` → per-entry `os.Stat` + `parseHistoryID` + `getMeta` projection → JSON `historyEntry[]` → Console `fetchHistory` → `HistoryEntrySchema` parse → `HistoryPanel` "Origin" column. The sidecar metadata is produced by the already-accepted `RecordManagedHistory` → `snapshotWithMeta` writer (AC-05); this slice only reads and projects it, redacted and low-cardinality — no raw TOML, secret, or credential is ever serialized. `parseHistoryID` also backs the `Time` field and `snapshotWithMeta`'s `CreatedAt` default.
- Behavior implemented: history listings now carry safe provenance (who/what/why/outcome/apply-id/versions) so the Console can attribute a snapshot in one round-trip and visibly flag emergency recovery snapshots; a malformed metadata sidecar degrades one listing row rather than failing the entire history view; and a `_N` collision-suffixed snapshot reports its true timestamp instead of the zero time.
- Tests added or changed: `TestParseHistoryID` (new; unit — collision-suffix and malformed-name parsing); `TestHistoryListParsesCollisionSuffix` (new; package integration — list() time recovery + ordering on a real dir); `TestHistoryListProjectsMetadata` (new; package integration — sidecar projection); `TestHistoryListMalformedSidecarDegradesOneRow` (new; package integration / failure injection — corrupt sidecar degrades one row); `HistoryPanel provenance projection > renders the redacted provenance and marks a recovery snapshot distinctly` (new; Console integration).
- Commands run: `go test -count=1 -run 'History|ParseHistoryID' ./internal/admin/...` (ok); `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean); `pnpm --dir internal/admin/ui exec vitest run src/test/console-v2-write.test.tsx` (33 tests pass); `pnpm --dir internal/admin/ui run test` (39 files, 505 tests pass — +1); `pnpm --dir internal/admin/ui run build` (rebuilt bundle, new hashes above); `go test -count=1 ./internal/admin/...` (ok — validates the regenerated embedded bundle); `go test -count=1 ./internal/app/... ./internal/config/...` (ok — no regression); `gofmt -d` on the three changed Go files (no diff); `git diff --cached --check` (clean — only expected CRLF warnings).
- Commands not run: `go test -race ./internal/admin/... ./internal/app/...` — unavailable locally (CGO disabled, no C toolchain); residual risk low — this slice adds no new concurrency (read-only projection over the existing single-flight-guarded history dir). Exact-SHA multi-OS CI (Console drift guard + race matrix) — deferred to CI; residual risk is the standard cross-platform drift/race verification pending at the accepted SHA.
- Deviations from this plan: none. The runbook's `Operation ApplyOperation` typed field, `MetadataError`, the `getMeta`-based projection, and the `parseHistoryID`/`isDecimalDigits` rewrite were adopted as written; the Console projection reuses the existing panel styling conventions rather than introducing new components.
- Self-review findings: no unwired helpers — `isDecimalDigits` backs `parseHistoryID`, and every new struct field is populated in `list()` and consumed by the Console column; no error swallowing — a sidecar read/decode error is surfaced per-row as `MetadataError` (and rendered), never dropped, while `list()` still succeeds; no duplicate truth — the projection reads the single `getMeta` sidecar rather than re-deriving provenance; no weakened tests — all additions are new assertions and the full Go/Console suites stay green; no secret/TOML/token exposure — only redacted, low-cardinality provenance is projected; comments updated to describe the `_N` suffix accurately; the embedded bundle was regenerated via `pnpm run build`, never hand-edited; the audit closure report was not touched.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws06-s02`.

---

## WS06_HISTORY_OBSERVABILITY / Slice 02 — bounded finalization metrics and restoration/finalization reporting

- Parent SHA: `dfc1282f3958248c6e87827797145dcf98bac0f2`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws06-history-observability`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; WS06 Slice 02 (metrics and error reporting). Backend Go only — no `internal/admin/ui` source and no `internal/admin/assets/dist` were touched.
- Files changed:
  - `internal/observability/metrics.go`: converted `managedApplyFinalizationErrors` from an unlabeled `prometheus.Counter` to a `*prometheus.CounterVec` labeled by the bounded `component` (restoration/pending/registry/callback_panic), keeping the established `jul_managed_apply_finalization_errors_total` name so there is one finalization-error truth instead of a parallel metric; added the `managedApplyRegistryEntries` gauge (`jul_managed_apply_terminal_registry_entries`) and the `managedApplyLookup` counter (`jul_managed_apply_terminal_lookup_total{result}`), both registered on the private registry. New wrappers: `ObserveManagedApplyFinalizationError(component)` (empty → "unknown"), `SetManagedApplyRegistryEntries(n)`, `ObserveManagedApplyLookup(result)` (empty → "unknown").
  - `internal/observability/cardinality_test.go`: updated the frozen `TestMetricLabelPolicy` map — `jul_managed_apply_finalization_errors_total` now `{component}`, added `jul_managed_apply_terminal_registry_entries` (nil) and `jul_managed_apply_terminal_lookup_total` (`{result}`) — and exercised all three hooks in `exerciseAllMetrics`, keeping the cardinality-regression guard authoritative.
  - `internal/observability/managed_apply_metrics_test.go` (new): value/label assertions for the component-labeled finalization-error counter, the registry gauge (last-write-wins), and the bounded lookup counter (including empty → "unknown" normalization).
  - `internal/app/config_apply.go`: added the `ReportManagedApplyError func(applyID, phase string, err error)` coordinator hook; replaced the `logRestorationFailure` no-op with a delegator that reports phase "restoration"; added `logPendingRegistrationFailure` (phase "pending") and called it at the post-persistence pending-registration failure site so the failure is explicit at its origin, not only threaded onto the terminal result.
  - `internal/app/serve.go`: wired `coordinator.ReportManagedApplyError` (structured `log.Error` + `metrics.ObserveManagedApplyFinalizationError(phase)`); passed `"callback_panic"` to the existing finalization-panic increment and updated the retained-ledger gauge after the panic hook's out-of-band terminal `Complete`.
  - `internal/app/managed_apply_finalizer.go`: passed component `"registry"` to `reportFinalizationError` (both callers are registry claim/complete failures) and published `SetManagedApplyRegistryEntries(f.registry.TerminalCount())` after every terminal completion.
  - `internal/app/managed_apply_finalizer_test.go`: added a scrape assertion that the terminal completion published `jul_managed_apply_terminal_registry_entries == 1` from the real registry depth through the production finalizer wiring (plus a `scrapeMetricLine` exposition helper).
  - `internal/app/config_apply_report_error_test.go` (new): package-integration coverage that the real `ApplyRaw` saved_not_live path routes a pending-registration failure to `ReportManagedApplyError` with phase "pending", and a deterministically forced restoration write failure (via the `beforeRestore` barrier corrupting the on-disk digest) reports phase "restoration"; both use the production hook wiring, no sleeps, and drain the withheld reload so the finalizer goroutine exits.
  - `internal/app/admin_deps.go`: wired `deps.ObserveManagedApplyLookup = subsystems.Metrics.ObserveManagedApplyLookup` in `BuildAdminDeps`.
  - `internal/admin/server.go`: added the `ObserveManagedApplyLookup func(result string)` field to `Deps` (bounded result; no apply ID/actor/IP; nil-safe).
  - `internal/admin/api_managed_apply.go`: `handleManagedApplyGet` now records exactly one bounded lookup outcome per request (invalid/missing/pending/terminal) through the nil-safe `observeManagedApplyLookup` helper.
  - `internal/admin/api_managed_apply_test.go`: added `TestManagedApplyGet_LookupMetric` (four classifications) and `TestManagedApplyGet_LookupMetricMissingRegistry` (nil ledger → "missing"), driven through the full `routes()` stack.
- Production path verified: (1) Finalization/restoration errors — HTTP apply → `ConfigApplyCoordinator.ApplyRaw`: the async finalizer restoration write failure → `logRestorationFailure` → `ReportManagedApplyError(id,"restoration",err)`; the post-persistence pending-registration failure → `logPendingRegistrationFailure` → `ReportManagedApplyError(id,"pending",err)`; both wired in `serve.go` to `log.Error` + `metrics.ObserveManagedApplyFinalizationError(component)`. The finalizer's registry claim/complete failures → `reportFinalizationError` → `ObserveManagedApplyFinalizationError("registry")`; the finalization-callback panic hook → `ObserveManagedApplyFinalizationError("callback_panic")`. (2) Registry gauge — `managedApplyFinalizer.Finalize` and the serve.go panic hook publish `SetManagedApplyRegistryEntries(registry.TerminalCount())` after each terminal `Complete`. (3) Lookup counter — `GET /api/config/applies/{id}` → `handleManagedApplyGet` → `s.deps.ObserveManagedApplyLookup(result)`, wired from `BuildAdminDeps` to `Metrics.ObserveManagedApplyLookup`.
- Behavior implemented: managed-apply restoration and finalization failures are now observable at their origin through a structured log and a bounded, component-labeled Prometheus counter (no longer a no-op or a single unlabeled counter); the retained terminal-ledger depth is exported as a gauge; and exact-ID apply lookups are counted by bounded outcome. Every label is a fixed low-cardinality enum — never an apply ID, actor, source IP, path, or version.
- Tests added or changed: `TestManagedApplyFinalizationErrorComponentLabel`, `TestManagedApplyRegistryEntriesGauge`, `TestManagedApplyLookupCounter` (new; unit — metric values/labels/normalization); `TestReportManagedApplyErrorPendingRegistration`, `TestReportManagedApplyErrorRestoration` (new; package integration — real coordinator production wiring, deterministic, no sleeps); `TestReportManagedApplyErrorNilHookSafe` (new; unit — nil-hook contract); the finalizer exactly-once test gained a gauge scrape assertion (package integration); `TestManagedApplyGet_LookupMetric`, `TestManagedApplyGet_LookupMetricMissingRegistry` (new; HTTP integration through `routes()`); `TestMetricLabelPolicy`/`exerciseAllMetrics` strengthened (cardinality guard). No existing assertion was weakened.
- Commands run: `go test -count=1 ./internal/observability/...` (ok); `go test -count=1 ./internal/admin/... ./internal/app/...` (ok — admin 4.187s, app 427.822s); `go test -count=1 ./internal/config/...` (ok); `go test -count=1 -run 'TestReportManagedApplyError|TestManagedApplyFinalizerExactlyOnce' ./internal/app/` (ok); `git diff --check` (clean — only the known LF→CRLF working-copy warnings); `gofmt -d` (LF-normalized) on all changed Go files (no real diff — only the trailing-newline artifact of the CRLF-normalization pipe).
- Commands not run: `go test -race ./internal/admin/... ./internal/app/...` — unavailable locally (CGO disabled, no C toolchain), deferred to the CI race matrix. Residual risk low: the gauge/lookup/error paths reuse existing mutex-guarded primitives (the registry's internal lock via `TerminalCount()`/`Complete`, the metrics registry's own synchronization) and add no new unsynchronized shared state. Exact-SHA multi-OS CI deferred.
- Deviations from this plan: (1) Reused the established `jul_managed_apply_*` metric namespace instead of the runbook's suggested `jul_config_apply_*` names, so the finalization-error, history, registry, and lookup metrics stay in one family and there is no duplicate finalization-error truth. (2) The finalization `component` value set is {restoration, pending, registry, callback_panic} — the runbook listed {history, audit, registry, callback_panic}; "restoration" and "pending" are the actual coordinator phases this slice reports, and are the values emitted by production, so unused values were not invented. (3) The lookup metric omits the runbook's "forbidden" value because `handleManagedApplyGet` has no ownership gate that would produce it. (4) `internal/app/config_apply_report_error_test.go` was already present untracked at implementation time (not present at preflight `git status`); it tests exactly this slice's new coordinator behavior, compiles, and passes against the implementation, so it was adopted as the slice's coordinator-level evidence rather than duplicated.
- Self-review findings: no unwired helpers — every new metric wrapper and the `ReportManagedApplyError`/`ObserveManagedApplyLookup` hooks are invoked on the real production path and asserted by tests; no error swallowing — the previously no-op restoration failure and the previously result-only pending-registration failure are now surfaced through log + metric; no duplicate truth — the finalization-error counter was labeled in place rather than duplicated, and the gauge reads the single registry's `TerminalCount()`; no weakened tests — the frozen cardinality policy was tightened (added label + two metrics) and all suites stay green; no secret/TOML/token/actor exposure — all labels are bounded enums; comments updated to describe the new behavior; no generated Console bundle or closure report touched.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws06-s03`.

---

## WS06_HISTORY_OBSERVABILITY / Slice 03 — remove duplicate mutation-machine truth and document the result-query permission contract

- Parent SHA: `2a8ab911cd94a5731063973bf82189b7a72ca990`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws06-history-observability`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; WS06 Slice 03 (Step 5 mutation-state cleanup + Step 6 permission-contract documentation). Console TypeScript + docs only — no Go source changed; the embedded Console bundle was rebuilt from source.
- Files changed:
  - `internal/admin/ui/src/features/config/useConfigMutationMachine.ts`: added `confirmAdminForOperation(generation)` — the single write path for the admin-confirmation state: it dispatches `confirmAdmin` (reducer authoritative) AND mirrors `confirmedAdminOperationRef` for the synchronous confirm-dialog read; `cancelOperation` now also resets `postApplyPollAttemptsRef` to 0 (parity with `startOperation`) so a superseded/abandoned operation never inherits a stale poll budget; updated the doc header to state the reducer is the single source of truth and the refs are only ever written through the dispatching methods.
  - `internal/admin/ui/src/features/config/ConfigPanel.tsx`: routed both admin-confirmation writes (the admin-change confirm dialog and the stage-restart confirm dialog) through `confirmAdminForOperation(operationID)` instead of assigning `confirmedAdminOperationRef.current` directly; removed the three redundant post-success `confirmedAdminOperationRef.current = null` resets in the `applyRaw`/`applyPatch`/`applyStage` `onSuccess` handlers (they had no reducer counterpart; the per-generation reducer model plus `startOperation`/`cancelOperation` clearing already governs confirmation lifetime, and every stage/confirm dialog that reads the ref is preceded by `startOperation` or is a legitimate same-operation retry). The panel now only READS the ref (stage-dialog correlation) and writes it exclusively via the hook method.
  - `internal/admin/ui/src/features/config/configMutationMachine.ts`: fixed the stale `startOperation` action comment that claimed the reducer action "reset[s] the poll budget" — the poll budget is a React-binding ref, not reducer state, and is reset by the hook.
  - `internal/admin/ui/src/test/use-config-mutation-machine.test.tsx` (new): hook-level tests proving `confirmAdminForOperation` lands the confirmation in reducer state AND mirrors the ref, that `startOperation` clears both, and that `cancelOperation` resets the post-apply poll ref.
  - `docs/specs/console-rbac.md`: added the `/api/config/applies/{id}` row to the read matrix (`status:read` **or** `config:apply`, all four roles ✓) and an explanatory paragraph documenting the any-of authorization and the secret-free projection — completing Step 6's "RBAC permission matrix docs" obligation (the route authorization + Go tests already landed and were accepted in WS01 Slice 03/04).
  - `internal/admin/assets/dist/*` (generated): rebuilt the embedded Console bundle from source (`index-*` and `CodeEditor-*` chunks re-hashed via deterministic content-hash propagation; verified reproducible across three builds and that the committed bundle was already in sync with HEAD source, so no pre-existing drift was absorbed).
- Production path verified: React confirm-dialog `onConfirm` → `confirmAdminForOperation(operationID)` → `dispatch({type:"confirmAdmin",generation})` (reducer `adminConfirmedGeneration` becomes authoritative) + `confirmedAdminOperationRef.current = generation` (synchronous mirror) → the stage-restart dialog's `onConfirm` reads `confirmedAdminOperationRef.current === operationID` to re-send `confirm_admin=true` for the same operation. Operation lifecycle: `startOperation`/`cancelOperation` (editor edit, reset, dialog cancel, discard) bump the generation and clear both the reducer field and its ref mirror and the poll ref. Step 6: `GET /api/config/applies/{id}` → `routes()` → the catalog entry's `AnyPermissions:{StatusRead,ConfigApply}` → `requireAnyPermission` (unchanged; verified present with its accepted WS01 tests), now reflected in the RBAC matrix docs.
- Behavior implemented: the config-write mutation machine has a single source of truth — the reducer. The admin-confirmation state is no longer maintained by an ad-hoc mutable ref in parallel with a never-dispatched reducer field; every imperative update dispatches its reducer action and the ref only mirrors the reducer for async reads. `cancelOperation` now clears the post-apply poll budget. No user-visible runtime behavior changed (admin re-confirmation on same-operation retries, generation-guarded staleness, and bounded legacy polling are all preserved); the change removes duplicate/dead state and a stale comment. The result-query any-of permission contract is now documented in the RBAC matrix.
- Tests added or changed: `use-config-mutation-machine.test.tsx` — "confirmAdminForOperation dispatches confirmAdmin and mirrors the ref" and "cancelOperation resets the post-apply poll ref" (new; unit/hook — `renderHook`+`act`, asserts reducer state AND ref mirror). No existing test weakened; the reducer's `confirmAdmin`/generation-scoping tests and the `console-v2-write` admin-confirm integration tests remain intact and green.
- Commands run: `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui run lint` (clean, --max-warnings=0); `pnpm --dir internal/admin/ui run test` (40 files, 507 tests passed); `pnpm --dir internal/admin/ui run build` (deterministic; verified reproducible and drift-clean against HEAD source); `go build -tags console ./cmd/jul` (embedded bundle compiles); `go test -count=1 ./internal/admin/... ./internal/app/... ./internal/config/...` (ok — admin 2.758s, app 426.195s, config 1.515s); `git diff --check` (clean).
- Commands not run: `go test -race ./internal/admin/... ./internal/app/...` — unavailable locally (CGO disabled, no C toolchain), deferred to the CI race matrix. Residual risk negligible: this slice changes only Console TypeScript, generated assets, and docs; it adds no Go concurrency. Exact-SHA multi-OS CI deferred.
- Deviations from this plan: (1) Step 6's route authorization and Go route tests were already implemented and accepted in WS01 Slice 03/04 (`AnyPermissions:{StatusRead,ConfigApply}` + `requireAnyPermission` + `TestManagedApplyGet_AnyPermissionAuthorizes`/`TestManagedApplyGet_ForbiddenBodyListsAcceptedPermissions`), so this slice completed only the outstanding "RBAC permission matrix docs" deliverable rather than re-implementing the route; the prescribed commit subject (mutation-state truth) is retained as the headline change. (2) Chose reducer-authoritative with ref-mirror (the runbook's first listed option and its explicit "admin confirmation ref must dispatch confirmAdmin" instruction) rather than deleting the reducer field; the pre-existing `isAdminConfirmedFor` accessor and the `confirmAdmin` action are now live truth (dispatched in production) and were kept, so no reducer action is dead. (3) The three redundant post-success ref resets were removed (not re-routed through a new clear action) because the per-generation reducer model plus `startOperation`/`cancelOperation` already governs confirmation lifetime and no post-success read of the ref can match an operationID without an intervening `startOperation`; the `console-v2-write` admin-confirm integration tests confirm no behavioral regression.
- Self-review findings: no unwired helpers — `confirmAdminForOperation` is invoked at both confirm-dialog sites and asserted by the new hook test; no error swallowing; duplicate truth removed — admin confirmation now has one authoritative home (the reducer) with the ref as a pure mirror; no weakened tests — a new hook test was added and all 507 Console tests plus the focused Go packages stay green; no secret/TOML/token/actor exposure — the docs row and paragraph explicitly note actor/source IP/token digest remain audit-API only; the stale "reset the poll budget" reducer comment was corrected; the generated bundle was rebuilt from source deterministically (no hand-edit of `assets/dist`), verified in sync with HEAD before the change so no unrelated drift was folded in; the audit closure report was not touched.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-ws06-s04`.

---

## WS06_HISTORY_OBSERVABILITY / Slice 04 — verify history and observability contracts (tests and review)

- Parent SHA: `0303b228d96abea86f09ca17ce913fcad28989cc`
- Resulting SHA: recorded in the excluded continuity state after commit (not embeddable in the committed entry).
- Branch: `audit/ws06-history-observability`
- Agent role/context: GitHub Copilot (Claude Opus 4.8), Jul Audit Implementer mode; WS06 Slice 04 (§7.9 tests-and-review). Purely additive test slice — no production Go, no Console source, no generated `internal/admin/assets/dist` bundle, and no audit closure report were touched; the two changed files are test files only.
- Files changed:
  - `internal/app/managed_apply_finalizer_test.go`: added `TestManagedApplyFinalizerHistoryDisposition` proving the terminal finalizer emits `jul_managed_apply_history_total{operation,result}` with the correct bounded snapshot disposition for each terminal outcome, driven through the real production `managedApplyFinalizer.Finalize` path — "recorded" (committed apply with a non-empty prior configuration), "skipped" (committed apply with no prior configuration to snapshot), and "failed" (committed apply whose trusted history write fails, forced deterministically by pointing the admin server's `HistoryDir` at a regular file so `os.MkdirAll` fails cross-platform). Added the `scrapeLabeledMetric` helper that reads the public `/metrics` exposition handler and matches a specific label set (sorted-key series string) so a `package app` test can assert a labeled counter without reaching into the metrics package's private registry.
  - `internal/admin/ui/src/test/console-v2-write.test.tsx`: added `HistoryPanel provenance projection > renders a legacy raw-only entry with no provenance sidecar` — a snapshot carrying only id/time/size (older release, no metadata) still lists and renders an em dash in the Origin column, is NOT mistaken for a malformed sidecar ("Metadata unavailable" absent), and shows no Recovery/Pre-apply badge, closing the §7.9 UI "old entry without metadata still renders" requirement.
- Production path verified: (1) history disposition — `coordinator.OnManagedApplyComplete = managedApplyFinalizer.Finalize` → `Finalize` claims the transaction, calls `admin.Server.RecordManagedHistory`, and maps the result to `observeHistory(operation, "recorded"|"skipped"|"failed")` → `metrics.ObserveManagedApplyHistory` → `jul_managed_apply_history_total{operation,result}`; the new test exercises `Finalize` directly (the exact production terminal entry point) and scrapes the public exposition. (2) legacy UI render — `GET /api/config/history` → `fetchHistory` → `HistoryEntrySchema` (all provenance fields optional) → `HistoryPanel` `ProvenanceCell` renders an em dash when `hasProvenance` is false and no `metadata_error` is present.
- Behavior implemented: none — this slice adds no product behavior. It verifies the WS06 §7.9 contract surface: the history-snapshot disposition metric (recorded/skipped/failed) is now asserted through the production finalizer, and the Console's backward-compatible rendering of legacy raw-only snapshots is now explicitly pinned. The remaining §7.9 items were already proven by focused tests landed in WS06 Slices 01–03 and were confirmed present rather than duplicated: History (metadata present, raw-only legacy snapshot, malformed sidecar, recovery snapshot, collision suffix, pruning removes sidecar — `history_test.go`/`history_metadata_test.go`/`managed_history_test.go`); Metrics (no unbounded labels, registry gauge, pending/terminal/missing lookup, finalization error components — `cardinality_test.go`/`managed_apply_metrics_test.go`/`api_managed_apply_test.go`/`managed_apply_finalizer_test.go`); State machine (no duplicate poll source, start/cancel resets scoped state, stale generation cannot mutate, admin confirmation scoped to the exact generation — `config-mutation-machine.test.ts`/`use-config-mutation-machine.test.tsx`); UI (metadata columns, recovery badge, metadata error warning — `console-v2-write.test.tsx`).
- Tests added or changed: `TestManagedApplyFinalizerHistoryDisposition` (new; package integration + failure injection — real `Finalize` path, deterministic history-write failure via a file-as-directory, no sleeps, labeled-exposition assertions); `HistoryPanel provenance projection > renders a legacy raw-only entry with no provenance sidecar` (new; Console integration). No existing assertion was weakened or deleted — the diff is 159 insertions and 0 deletions across two test files.
- Commands run: `go test -count=1 -run 'TestManagedApplyFinalizer' ./internal/app/` (ok); `pnpm --dir internal/admin/ui run typecheck` (clean); `pnpm --dir internal/admin/ui exec vitest run src/test/console-v2-write.test.tsx` (34 tests pass, +1); `pnpm --dir internal/admin/ui run lint` (clean, --max-warnings=0); `pnpm --dir internal/admin/ui run test` (40 files, 508 tests pass, +1); `go test -count=1 ./internal/admin/... ./internal/observability/... ./internal/config/...` (ok); `go test -count=1 ./internal/app/...` (ok — 426.739s); `git diff --check` (clean — only the known LF→CRLF working-copy notices); `gofmt` on the changed Go file with CRLF normalized (clean; the lone reported line was the trailing-newline artifact of the normalization pipe, and the on-disk file ends in a single LF).
- Commands not run: `go test -race ./internal/admin/... ./internal/app/...` — unavailable locally (CGO disabled, no C toolchain), deferred to the CI race matrix. Residual risk negligible: this slice adds only tests and no new production concurrency; the new Go test reuses the existing mutex-guarded registry/metrics primitives. Exact-SHA multi-OS CI (Console drift guard + race matrix) deferred to CI.
- Deviations from this plan: (1) The §7.9 metrics label family is `jul_managed_apply_history_total{operation,result}` (established in WS02) rather than the runbook's suggested `jul_config_apply_history_total`, matching the accepted metric namespace so the disposition test asserts the real production metric. (2) Most §7.9 required tests already existed from Slices 01–03; per the tests-and-review scope this slice added only the two genuinely missing focused tests (history disposition metric; legacy raw-only UI render) instead of duplicating existing coverage.
- Self-review findings: no unwired helpers — `scrapeLabeledMetric` backs the new disposition test and is exercised; no error swallowing — both new tests assert on values/absence; no duplicate truth introduced — existing §7.9 coverage was confirmed and reused, not re-implemented; no weakened tests — additive only (0 deletions); no secret/TOML/token/actor exposure — the disposition metric labels are the bounded operation/result enums and the failed-case forcing uses a filesystem shape, never sensitive data; no stale comments; no generated bundle rebuilt (correct — no Console source changed, so `assets/dist` must stay untouched) and the audit closure report was not touched.
- Independent review status: pending.
- Reviewer blockers: none recorded.
- Blocker-fix SHA: n/a.
- Accepted SHA: pending independent review and exact-SHA CI.
- Next execution file: `/jul-audit-review-ws06`.

